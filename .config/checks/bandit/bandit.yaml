# Bandit security-gate config — separated from pyproject.toml (SoC).
# Run: tools/ci/scripts/run-bandit.sh
#
# Catch real security issues with zero tolerance for HIGH/MEDIUM findings.
# No local `nosec` or rule-skip compatibility surface is accepted here.

skips:
  []

assert_used:
  skips: []
