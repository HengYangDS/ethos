# coverage.py config — owned by .config/checks/coverage/ (not pyproject.toml).
# CI and ETHOS proof invoke this file through the repository Nox test sessions.
[run]
branch = True
patch = subprocess
source =
    src/ethos

[report]
show_missing = True
skip_covered = False
precision = 2
