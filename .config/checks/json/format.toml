schema = "ethos-json-format-v1"
owner = "ethos-quality-gate-governance"
formatter = "python-stdlib-json"
default_mode = "pretty"
indent = 2

rule = [
  { globs = ["system/schemas/**/*.json", "evidence/**/*.json"], mode = "compact" },
  { globs = ["tests/fixtures/source-budget-v2/*.json"], mode = "compact" },
]
