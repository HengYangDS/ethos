owner = "ethos-quality-gate-governance"
summary = "Native uv and npm audits cover both declared dependency locks."
runner = "uv run --frozen --offline python -m nox -s vulnerabilities"
timeout_seconds = 120
