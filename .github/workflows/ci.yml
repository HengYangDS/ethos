name: ETHOS CI
"on":
  pull_request: null
  push:
    branches:
      - dev
      - main
      - proposal/**
  workflow_dispatch: null
permissions:
  contents: read
env:
  PYTHONWARNINGS: error
  UV_LINK_MODE: copy
  UV_CACHE_DIR: build/runtime/tool-cache/uv
  ETHOS_CI_TOOL_CACHE_DIR: build/runtime/tool-cache/ci-tools
jobs:
  host-conformance:
    name: ${{ matrix.os }} / Python ${{ matrix.python }}
    runs-on: ${{ matrix.os }}
    env:
      UV_PYTHON_INSTALL_DIR: ${{ github.workspace }}/build/runtime/python
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - windows-latest
          - macos-latest
        python:
          - "3.12"
          - "3.13"
          - "3.14"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          fetch-depth: 0
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: 26.9.0
          cache: npm
      - uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4
        with:
          version: 0.12.16
          python-version: ${{ matrix.python }}
      - name: Provision native Python image supply
        run: uv python install --no-bin ${{ matrix.python }}
      - name: Install locked runtimes
        run: npm ci --ignore-scripts
      - name: Synchronize locked Python environment
        run: uv sync --locked --group dev
      - name: Execute wheel conformance
        run: uv run --frozen python -m nox -s host_conformance
      - name: Upload host conformance receipt
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: host-conformance-${{ matrix.os }}-py${{ matrix.python }}
          path: build/evidence/local-install/smoke.json
          if-no-files-found: error
  quality:
    name: quality gates
    runs-on: macos-latest
    env:
      ETHOS_COMMIT_ALLOWED_SIGNERS: ${{ vars.ETHOS_COMMIT_ALLOWED_SIGNERS }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          fetch-depth: 0
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: "3.14"
      - uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4
        with:
          version: 0.12.16
      - name: Bootstrap Python and OpenSpec
        run: tools/ci/scripts/bootstrap-python.sh
      - name: Admit pushed commit range
        if: github.event_name == 'push'
        run: uv run --frozen --offline python -B -I -m ethos.cli hook commit-range --target-ref "${{ github.ref }}" --proposed-head "${{ github.sha }}" --remote-head "${{ github.event.before }}" --remote origin --root . --json
      - name: Admit pull request commit range
        if: github.event_name == 'pull_request'
        run: uv run --frozen --offline python -B -I -m ethos.cli hook commit-range --target-ref "refs/heads/${{ github.event.pull_request.base.ref }}" --proposed-head "${{ github.event.pull_request.head.sha }}" --remote-head "${{ github.event.pull_request.base.sha }}" --remote origin --root . --json
      - name: External links
        run: tools/ci/scripts/with-python-runtime.sh -- uv run --frozen --offline python -B -I -m ethos.cli prove --host --execute --gate external-links --expect-head "$(git rev-parse HEAD)" --json
      - name: Hosted provider observation envelope
        run: uv run --frozen --offline python -m nox -s hosted_observation
      - name: HEAD-bound hosted verification receipt
        run: tools/ci/scripts/run-head-bound-proof.sh
      - name: Upload proof receipt
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: ethos-proof-receipt
          path: |
            build/evidence/quality/proof/
            build/evidence/quality/tests/pytest/junit*.xml
            build/evidence/quality/tests/coverage/coverage.xml
          if-no-files-found: error
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: python-distributions
          if-no-files-found: error
          path: |
            build/artifacts/python/
            build/evidence/release/
  verify:
    name: repository proof
    runs-on: ubuntu-latest
    needs: quality
    if: ${{ always() }}
    steps:
      - name: Require successful hosted execution and artifact publication
        env:
          QUALITY_RESULT: ${{ needs.quality.result }}
        run: test "$QUALITY_RESULT" = success
  package:
    name: package artifacts
    runs-on: ubuntu-latest
    needs: quality
    if: ${{ always() }}
    steps:
      - name: Require successful hosted execution and artifact publication
        env:
          QUALITY_RESULT: ${{ needs.quality.result }}
        run: test "$QUALITY_RESULT" = success
