---
subject: ethos:artifact-topology-hotpath-absorption-20260720
role: plan
state: active
relations:
  source_lane: work/artifact-topology-hotpath-repair-20260714
  carrier: openspec/changes/archive/2026-07-20-artifact-topology-hotpath-absorption
---

# Artifact-Topology Hotpath Absorption — 2026-07-20

Status: active, local-only authority carrier.

Purpose: prove or reject semantic absorption of one orphaned, dirty Work Lane
before any exceptional retirement; reduce topology only through the native
preserve-retire and clear transitions.

See also: [Direct Absorption Wave 1](direct-absorption-wave1-20260718.md),
[Mutation Rules](../../rules/mutation.md), and
[Evidence Rules](../../rules/evidence.md).

## Exact source boundary

The sole source is `work/artifact-topology-hotpath-repair-20260714` at
`194b2ec91891631f178ef7c71d73da35e77c6b13`.  At the current local
observation it is linked, missing a lease, diverged from accepted truth, and
has one staged file: `evidence/parity/generic-shadow.json`.  Its exact staged
patch digest is
`408937fa6e4993c208189856427fec823fc3d4ecf52a0e24d87d03d775ac26d6`.

Existing preserved packages remain recovery evidence only.  They do not prove
absorption and are not cleared by this carrier.

## Semantic basis to verify

The source's product behavior is bounded generated-artifact topology auditing:
deny a generated directory once, prune its descendants, and preserve the
declaration-driven CEL policy boundary.  Accepted history has since replaced
the historical implementation with narrower current contracts, including
artifact scan bounding, declarative CEL grouping, cached topology decisions,
and current focused regressions.  The relevant accepted commits include
`d6ba2b817d726b611cbc44eba2982133318fbe10`,
`7c877872ac425c3e438fddb4876b6ffc58361548`, and
`03ff8bb33b4134cecf9028c4eb63ddf10d62f79e`.

This carrier must prove that current accepted behavior still enforces the
source's useful invariants.  Historical tree inequality, path overlap, prior
preservation, or a passing generic suite alone is insufficient.

## Required transition

After this carrier's own focused proof, exact-HEAD proof, candidate land, and
accepted-root closeout, it may record exactly one fresh
`lane_resolution/preserve-retire` decision for the source.  The native apply
must re-observe the exact source head, linked path, dirty state, missing lease,
and accepted Chronicle before it preserves the current staged delta and removes
only that branch and worktree.

Only after the resulting acceptance proof establishes that the retained patch
is superseded may a separate accepted Chronicle invoke `lane_resolution/clear-
preservation` for the exact package manifest.  The receipt remains durable.

## Boundaries

This excludes every other Work Lane, any valid lease, raw Git or SQLite
deletion, broad worktree pruning, GitHub/GitLab mutation, hosted CI claims, and
clearing any package without a manifest-bound later decision.
