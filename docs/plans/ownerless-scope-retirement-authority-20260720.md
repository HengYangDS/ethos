---
subject: ethos:ownerless-scope-retirement-authority-20260720
role: plan
state: archived
relations:
  carrier: openspec/changes/archive/2026-07-19-ownerless-scope-retirement-authority-20260720
  follows: ownerless-semantic-absorption-20260719
---

# Ownerless Scope-Recovery Retirement Authority

## Exact target

This authority carrier concerns only
`work/openspec-scope-recovery-admission-20260715` at
`93ed27378889068a61ebf4edd876be4797484211`.  The fresh local observation
before this carrier found the lane linked and clean, with no normalized lease
or claim.  It is therefore owner-uncertain and cannot use ordinary landed or
superseded retirement.

## Semantic basis

The ownerless lane's unique intent was the narrow repair admission for one
selected, Git-tracked malformed Change-local `scope.toml`; malformed scope is
not coverage and widened or unselected requests remain uncovered.  That intent
was replayed independently on the current baseline, proven at
`8139759251a9df713da2a8659f2ecda1d8701aa8`, landed to `candidate/dev`, and
locally closed out to accepted `dev` at the same SHA.

The historical tree is not byte-identical to accepted truth because it also
contains its own dated evidence, claim, archive carrier, and historical
projection.  Consequently, `lane retire superseded` correctly remains
inapplicable: tree equivalence is not semantic absorption.

## Authority and boundary

After this carrier's own exact-HEAD proof, candidate land, and accepted-root
closeout, the dated Chronicle selects `lane_resolution/retire` for this one
clean ownerless lane.  Native application must still re-observe the exact
source ref, clean state, missing-lease state, and observation digest; require
break-glass plus explicit irreversible confirmation; and write a local receipt.

This does not authorize raw ref deletion, deletion of any other lane, takeover
of a valid lease, preservation as a substitute for absorption, remote push,
GitLab availability, or hosted-CI claims.

## Recovery

The target is clean, so no preservation package is needed for its uncommitted
state.  Git history remains recoverable by the exact SHA above until any
separate repository-retention policy changes it.  A later historical audit can
recreate a detached worktree from that SHA; it cannot reinterpret the removed
lane as a currently owned work surface.
