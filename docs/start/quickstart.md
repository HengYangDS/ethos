---
subject: docs:start
role: workflow
state: active
relations:
  canonical_for: first run
---

# Quickstart

Run the public command plane:

```bash
ethos status
ethos plan --changed
ethos prove
```

Use `--json` for stable machine output.
