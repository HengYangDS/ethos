# Accepted closeout interrupted-sync recovery — 2026-07-26

## Facts

- Native accepted-root closeout had already advanced the accepted ref from
  `fe5b44be26a0033131699fc4828e3dc6a86e0b19` to
  `da094c54ac22a5623542517bde447b6985aa6c05` when synchronization of the
  accepted checkout failed with `accepted_worktree_sync_failed`.
- The retained external failure receipt records that event. It is forensic
  input to this Change and remains outside the product worktree.
- The recovery subject is limited to the exact prior-index/clean-worktree
  residue and one verified stale `index.lock`; ordinary dirty accepted roots
  remain blocked.

## Decision

Create the active `accepted-closeout-interrupted-sync-recovery` Change and its
separate Claim rather than retargeting the archived native-lane-resolution
Claim. The recovery accepts a candidate runner only when it is the promoted
accepted head or its descendant, so the runner can contain recovery code
without altering the accepted historical fact.

The recovery uses native atomic no-replace relocation for the lock and fails
closed when the operation or any receipt, Git, digest, identity, quarantine, or
postcondition binding differs.

## Archive boundary

The official archive is part of the final canonical-specification HEAD. To
avoid a self-referential false completion, the active Change keeps only source
implementation and failure-boundary tests as archive-gated checkboxes. The
post-archive proof, candidate promotion, accepted-root recovery, normal
closeout, publish-readiness, and lane-retirement sequence is operational
acceptance: its command JSON is retained outside the product tree and is not
claimed by this Chronicle until it has actually executed.

## Boundary

This record does not claim completed implementation, executed proof, candidate
land, accepted-root recovery, publication, hosted execution, remote mutation,
or foreign-lane cleanup. Those require fresh evidence after the final committed
head is known.
