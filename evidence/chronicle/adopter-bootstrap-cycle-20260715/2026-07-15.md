# Adopter profile bootstrap-cycle repair — 2026-07-15

## Claim

ETHOS can admit the first material-path declaration for a legacy adopter only
when exactly one official OpenSpec Change has just been created and therefore
has status `no-tasks`.

## Boundary

The fallback selects that sole Change only for the already-tracked
`.ethos/profile.toml` profile-only write. It does not make `no-tasks` a normal
active lifecycle Change, provide ordinary material-path coverage, admit an
adjacent path, or relax the tracked, absent-declaration, and singleton checks.

## Evidence

- A leased Work Lane based on candidate `37f4c098f5034e4322ee4c06fba1294a909a4733`
  carries the source, regression, OpenSpec, and claim changes.
- Focused scope/lifecycle regression: 29 passed.
- Repository formatter and Ruff checks passed for the changed Python files.
- `quality source-budget --json` returned `clean` after test consolidation;
  no source-budget limit was raised.
- Official `openspec validate --all --strict --json` passed all 10 items, and
  `ethos openspec --lifecycle --json` returned `clean`.

## Decision

This Chronicle records pre-commit local validation only. It does not claim
parity receipt freshness, executed proof, candidate landing, accepted closeout,
local installation, remote publication, or hosted CI. Those remain later
head-bound transition evidence.
