---
subject: ethos:adopter-material-scope-binding-completion-20260714
state: active
created: 2026-07-14
claim: adopter-material-scope-binding-completion-20260714
---

# Authoritative adopter material scope binding — 2026-07-14

## Scope

This active Change productizes the ETHOS-owned companion contract that binds
adopter-declared material paths to an official OpenSpec Change. It extends the
already accepted universal lifecycle behavior: `plan` and `prove` remain on the
official OpenSpec lifecycle; this record does not replace or duplicate that
lifecycle.

## Boundary

`[openspec].material_paths` and Change-local `scope.toml` are portable,
repository-governance declarations. The companion is not official OpenSpec
workflow-schema metadata. Lifecycle scope is not a code-correctness gate, and
Superpowers or any other method package is not a governance carrier.

`lane prewrite`, `plan --changed`, and `prove` consume the same lifecycle scope
read model. An uncovered material path reports
`openspec_material_path_uncovered:<path>`. The only bootstrap admission is an
official active Change's exact absent `scope.toml`; after creation it must cover
itself. Missing or invalid unrelated companions remain diagnostics rather than
global coverage blockers.

## Evidence plan

Focused regressions pass with `127 passed` after keeping pytest cache outside
the repository root. Ruff format and lint pass on every changed Python source.
The official OpenSpec strict validation passes (`1/1` Change), ETHOS lifecycle
is clean, and the source-budget debt record is within the declared bound. The
next evidence transition is the committed-HEAD executed proof; it is not
claimed by this pre-commit Chronicle.

## Source budget

The additive implementation is bound to the named
`adopter-material-scope-binding-completion-20260714` source-budget debt record.
The record preserves the repository baseline and requires the
`adopter-material-scope-compression` follow-on wave; it is not a permanent
exception or a substitute for product simplification.

## Non-claims

This is local Work-Lane evidence only. It makes no claim of DDWG landing,
accepted-root closeout, lane retirement, remote publication, hosted CI, or
authority over any foreign lane.
