# Chronicle: adopter material-scope duplicate lane resolution

event: lane_resolution/preserve-retire

## Authority and observation

The user directed that the observed scope-binding divergence be handled rather
than left as parallel residue.  This is a bounded local resolution decision for
`work/adopter-material-scope-binding-integration-20260714`; it does not infer
ownership from a provider or agent label and does not authorize remote
publication.

At observation, accepted `dev` and `candidate/dev` resolve to
`1d481e889fd02d258909708ed6f4c8c3f7791e8e`. The selected lane resolves to
`24a5f44e6dc31992603219f948c00ade317caf2b`, is clean, has a distinct live
lease holder, and is diverged from accepted truth.  It duplicates the already
archived scope-binding claim but retains three unabsorbed commits. Ordinary
holder retirement and superseded retirement are therefore inapplicable.

## Decision

`lane_resolution/preserve-retire` is selected. The resolution must first create
and verify the exact recovery package (bundle, tracked patch, untracked archive
when present, and manifest), then remove only the freshly observed linked lane
and its ref. The preserved package remains local retained evidence until a
separate clear decision; it is not cache residue.

Evidence basis:

- `evidence/claims/adopter-material-scope-binding-completion-20260714.toml`
- `openspec/changes/archive/2026-07-14-adopter-material-scope-binding-completion/`
- `/private/tmp/ethos-divergence-status-pre-resolution.json`
- `/private/tmp/ethos-scope-integration-retire-dry-2d1684596f95a15443f5d519fa537aca02d16aa1.json`

## Boundary

Any changed lane HEAD, lease, dirtiness, or worktree binding invalidates the
first-phase decision and requires a fresh observation. This decision authorizes
neither accepting the divergent implementation into product truth nor remote
GitLab publication.
