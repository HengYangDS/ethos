# Adopter OpenSpec lifecycle — 2026-07-14

- **Status:** active local evidence for the owned recovery lane; no landing,
  archive, retirement, or remote publication is asserted.
- **RED/GREEN:** before the change, a valid adopter `plan` skipped the mocked
  lifecycle reporter and returned clean; the dedicated regression then passed
  after both `plan` and `prove` invoke official lifecycle unconditionally.
- **Focused verification:** CLI lifecycle contracts plus adopter apply/scaffold
  tests passed **89** tests with pytest cache disabled; Ruff format/check passed
  for the changed Python files.
- **Quality boundary:** types, module layout, code size, source budget, and
  generated-artifact checks passed in this lane before commit.
- **HEAD-bound execution:**
  `ethos prove --execute --expect-head 4ee13511ce4621fe0aea1fd7dd992c375d3293c8 --json`
  completed with `state=proven`, **21** gates, and evidence digest
  `794d3ad391368e9582dede15a10fc2c89f9eab5844541759b7cec4838f761972`.
- **Authority:** lifecycle remains OpenSpec/repository-governance truth, not a
  code-correctness gate or Superpowers/method-package carrier.
- **Archive:** official
  `openspec archive adopter-openspec-lifecycle-20260714 --yes --json` archived
  the completed Change as
  `openspec/changes/archive/2026-07-14-adopter-openspec-lifecycle-20260714`
  and merged its requirement into `openspec/specs/repository-governance/spec.md`;
  no land, publication, or remote claim is made.
- **Freshness:** on commit `7b6acbd3f31096fffcee49b076c7bc4b1749c436`, the
  current semantic scope (`packages/ethos/src/ethos/surface/cli/root` and
  `tests/unit/cli`) has digest
  `423fbc5b6104e2da200d4806178d379ee619653aac184907bf1a449c74d925ce`.
  This only refreshes the lifecycle Change's source-scope evidence after the
  separately governed material-scope implementation touched those declared
  surfaces; it does not assert a second executed proof.
- **Follow-up:** portable material-path scope admission is implemented under the
  separate active Change `adopter-material-scope-binding-completion-20260714`;
  it does not replace or dilute this universal lifecycle correction.
- **Semantic freshness refresh (2026-07-17):** the P4 catalog and quality-runtime
  change touched the declared `tests/unit/cli` target without altering the
  lifecycle claim's `packages/ethos/src/ethos/surface/cli/root` implementation.
  At `3480e419c818affa57a29fdeab30057190414612`, the declared source scope had
  digest `64019bbcb94bae79e1595e3746e1415146feed72233d880d105ce0aa75b9bfc3`.
  This is an evidence-freshness refresh only; it asserts no new proof, land,
  publication, or lifecycle behavior.
- **Semantic freshness refresh (2026-07-17):** the P4 regression repair updated
  the declared `tests/unit/cli` target's import layout. At
  `4b5bdc64959ad0f7d1883043c4a38146a862d949`, the declared source scope has
  digest `1a8528eacf1e4c40beb71c4abe0991af00d2d1603f2a979f149ab595833c7511`.
  This refresh changes only the scope binding; it asserts no new proof, land,
  publication, or lifecycle behavior.
- **Semantic freshness refresh (2026-07-17):** the CLI governance-envelope test
  now isolates foreign Work Lane inventory while preserving its contract. At
  `bb1489199eaec778e6a3c06ebe07c7813060d603`, the declared source scope has
  digest `8955a99bcbbdf0bc8d82b26cbc3602494420a1dc86638a432d37254ca4dd5e1e`.
  This refresh changes only the scope binding; it asserts no new proof, land,
  publication, or lifecycle behavior.
- **Semantic freshness refresh (2026-07-17):** the primary-command context and
  proof-gate hygiene repair touched the declared
  `packages/ethos/src/ethos/surface/cli/root` and `tests/unit/cli` scope. At
  `3e64338c9d3ba1ff711efaf2f46780eb6be32adb`, that scope has digest
  `27db9fc7cd1d63202b92604dc03612dc9afe95f7deb47c6a3a5affd8248cc1f7`.
  This refresh changes only the scope binding; it asserts no new proof, land,
  publication, or lifecycle behavior.

- **Semantic scope correction (2026-07-18):** this active claim now binds only
  `packages/ethos/src/ethos/surface/cli/root/planning.py`,
  `packages/ethos/src/ethos/surface/cli/root/proof.py`,
  `tests/unit/cli/test_adopter_openspec_lifecycle.py`, and
  `tests/unit/cli/test_contracts_proof.py`. The former directory-wide targets
  admitted unrelated reader and CLI-test churn, which could stale lifecycle
  evidence without a lifecycle semantic change. At base
  `1d575628ff4c66220ca02d5a92296ce2b2ae178e`, this exact scope has digest
  `6b0d290d22089cfaea4630c8e02b74d202fb9f1937b540092751a4a8200a6d89`.
  This is an evidence-scope correction, not a new lifecycle proof, land, or
  publication claim; changes to any listed implementation or regression remain
  fail-closed as `evidence.semantic_scope_stale`.

- **Semantic freshness refresh (2026-07-18):** the proof-evidence isolation
  regression changed `tests/unit/cli/test_contracts_proof.py`, an exact declared
  lifecycle target. At `c60096d191908e3b5601a748f0a7d303d71d6be7`, the behavior-exact scope has digest `8046146fde384b9ac925dedaa129a4798032abe44d40df73b1292c1e097da544`.
  This refresh preserves the same fail-closed boundary; it asserts no new
  lifecycle proof, land, publication, or remote state.

- **Semantic freshness refresh (2026-07-18):** the proof runner regression now
  asserts that each selected gate receives the audited checkout root. At
  `a15e3decb105c47dbe40051e4a6e6a73006a1448`, the exact lifecycle scope has
  digest `289b3f5eef866958a855cfa2bf25ffa67316d704e16dec07061207edbca8ee91`.
  This preserves the same fail-closed boundary; it asserts no new lifecycle
  proof, land, publication, or remote state.

- **Semantic freshness refresh (2026-07-18):** the reconciled bounded-reader and proof-entrypoint changes update the declared exact lifecycle scope. At `d5c5288fb63cc7b7f7954a2197c2133e59f8ca53`, that scope has digest `31e39b08762b38db5ffe429b6468058125d38f28642813d43cc3a2c9f2a4410b`. This refresh binds evidence only; it asserts no new proof, land, publication, or lifecycle behavior.

- **Semantic freshness refresh (2026-07-18):** the proof fixture now accepts bounded status-reader options, preserving the declared lifecycle contract under the bounded reader. At `947cb9d15107900f0df26fe7f142de62a1d7ed33`, the exact scope digest is `9577280f2d5eb5ca9cdfbd5cf75e724a54e1ec2f92ac8ceb97b74399301403db`; this is evidence binding only.
