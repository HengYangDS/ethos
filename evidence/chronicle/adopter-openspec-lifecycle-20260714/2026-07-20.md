---
subject: ethos:adopter-openspec-lifecycle
role: evidence
state: active
created: 2026-07-20
claim: adopter-openspec-lifecycle-20260714
---

# Adopter OpenSpec Lifecycle — 2026-07-20

## Scope

This refresh re-observes the active claim's exact semantic scope on the current
owned Work Lane. The scope remains limited to the plan and proof lifecycle
entrypoints and their dedicated adopter regressions; it excludes historical
carriers, parity evidence, candidate integration, accepted-root closeout,
retirement, remote publication, and hosted CI.

## Evidence

At committed HEAD `8ee1de0d5a27ade55a5e36ca2508d7d1c95c4c3d`, the exact semantic scope has digest
`8de2576492f597f9ed514435087aa4aca857b64b9a080055ceff5578dbe9acf1`. The following focused regression set was executed with pytest
cache and coverage plugins disabled:

```bash
uv run --package ethos pytest -p no:xdist -p no:cov \
  tests/unit/cli/test_adopter_openspec_lifecycle.py \
  tests/unit/cli/test_contracts_proof.py::test_adopter_proof_surfaces_openspec_lifecycle_gap \
  -q
```

The run passed (`4 passed`). It establishes that a fresh generic adopter without
material scope remains not applicable; a material path without an OpenSpec
workspace is rejected; and both `plan` and `prove` surface the same injected
lifecycle gap rather than replacing it with a clean placeholder.

## Boundary

This is a semantic-freshness renewal for the existing claim. It does not assert
an executed repository proof, candidate land, accepted-root closeout, Work Lane
retirement, remote publication, or hosted CI. Those transitions require their
own current, HEAD-bound evidence.
