---
subject: ethos:adopter-openspec-lifecycle
role: evidence
state: partial
---

# Adopter OpenSpec Lifecycle — Loss-Bounded Continuity Packet

## Scope

This packet continues the historical `adopter-openspec-lifecycle-20260714`
episode on a new owned Work Lane. It preserves verifiable historical meaning and
defines the current re-execution boundary. It does not recreate the historical
Codex worktree, mutate historical session storage, or claim that original
checkout-local runtime state was restored.

## Retained Evidence

- Historical Codex session `019f5d91-a4f5-7011-b600-36d055be4c60` remains
  parseable: 29,019 JSONL records, zero malformed records, and stream digest
  `1880ab9c8fce62ae66dbe599b75a54a435da5152bccbb592e3cfb1823f0cf170`.
- Ten historical shell snapshots remain available as host-local context. Their
  individual identities are preserved by the session snapshot index; they are
  not repository proof and are not copied into this product carrier.
- The predecessor OpenSpec carrier remains at
  `openspec/changes/archive/2026-07-14-adopter-openspec-lifecycle-20260714`.
- The predecessor Work Lane ref remains
  `work/adopter-openspec-lifecycle-20260714` at
  `81e70577a92afedd05cd9c94439b31b3062e9f3b`; it is foreign, unleased,
  clean, and diverged from the current candidate baseline.
- The active episode claim remains
  `adopter-openspec-lifecycle-20260714`. Its exact semantic targets are the
  current plan/proof implementations and the two adopter lifecycle regressions.

## Irrecoverable State

- The original host worktree is absent.
- The original checkout-local temporary repro and external bootstrap-verifier
  directories are absent.
- A historical session stream contains compacted windows, so it cannot prove
  complete token-by-token or runtime-by-runtime replay.
- Historical proof, closeout readiness, and verifier receipts are historical
  evidence only. None is current proof for this successor HEAD.

## Current Anchor

- Successor Work Lane:
  `work/adopter-openspec-lifecycle-continuity-20260719`.
- Successor base and initial HEAD:
  `e3ed55ec0ae1603ef9368eafeeed3aff67282608` from `candidate/dev`.
- Accepted root observed at successor creation:
  `dev` = `fe774c994c5641b60f49a3e60a968ed8eba6fbee`.
- The successor has a normalized, holder-bound local lease and the original
  episode claim binding. Those bindings authorize only this successor's local
  lifecycle; they do not transfer authority over the predecessor lane.

## Replay Contract

1. Complete this active OpenSpec Change and bind the same episode claim to it.
2. Run current focused adopter lifecycle regressions before any implementation
   change; create a RED regression and minimal repair only if they expose drift.
3. Execute OpenSpec lifecycle, plan, and HEAD-bound proof on a stable committed
   successor HEAD.
4. Archive this continuation, then re-execute final proof for the archived
   successor HEAD before any candidate or accepted-root transition.

## Boundary

This packet asserts no current proof, candidate land, accepted-root closeout,
remote publication, hosted CI result, or restoration of historical runtime
state. It preserves sufficient semantic, evidence, and authority context for a
new independently verifiable continuation, not an impossible zero-loss recovery.
