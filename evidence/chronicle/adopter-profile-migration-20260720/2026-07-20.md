---
subject: ethos:adopter-profile-migration-20260720
role: evidence
state: active
---

# Adopter Profile Migration Observation

Date: 2026-07-20.

## Fact

An existing documentation adopter used the former profile envelope and
`roots.rules = "."` to express a root-level normative source. The current
strict profile declaration rejected that profile, and public commands leaked
the resulting `ValueError` as a traceback.

## Evidence

- Focused profile, CLI-boundary, type, lint, and canonical-document checks
  passed after adding bounded former-envelope normalization, top-level
  `normative_sources`, and public invalid-profile result translation.
- Direct CLI probes verified structured JSON for `orient`, `report`, `plan`,
  `openspec --lifecycle`, enforcing `prove`, and `land`; `land` reports the
  invalid binding before any protected-branch admission gap.
- Official OpenSpec validation accepted the Change before its archive; the
  archive updated the canonical repository-governance specification and its
  scope now names every changed production, test, documentation, and ratchet
  carrier.

## Boundary

This observation proves ETHOS product behavior in the current Work Lane only.
It does not claim that DDWG has migrated, that a proof has been executed on a
future product HEAD, or that any local or hosted publication has happened.
