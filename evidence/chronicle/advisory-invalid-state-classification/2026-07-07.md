# Advisory invalid-state classification evidence

`ethos report --json` now projects advisory signals through the existing
invalid-state taxonomy. The live protected OpenSpec residue advisory classifies
as `carrier_invalid` while remaining non-blocking with `required_gaps = []`.

## Boundary

This evidence supports read-model classification only. It does not add a new
invalid-state category, make advisory signals blockers, or authorize mutation of
protected branches.
