# Advisory signal readmodel

ETHOS now surfaces non-blocking advisory governance signals in `report` and
`orient` reader views. The signal remains non-blocking: required gaps continue to
drive transition refusal, while advisory signals stay visible for humans and
agents during first-glance governance.
