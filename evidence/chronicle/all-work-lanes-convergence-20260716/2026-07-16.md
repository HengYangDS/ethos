---
subject: ethos:evidence:all-work-lanes-convergence-20260716
role: evidence
state: active
relations:
  supports: all-work-lanes-convergence-20260716
---

# All Work Lanes Convergence Evidence — 2026-07-16

## Decision

The current-session request to take over and close all ETHOS `work/*` state is
accepted only as a bounded local convergence program over the exact 60 legacy
refs frozen in the attached inventory. It is not reusable wildcard authority.
Every normal or exceptional effect remains subject to current ETHOS holder,
lease, HEAD, claim, target-observation, preservation, proof, and lifecycle
checks.

Selected policy:

- preserve dirty or owner-uncertain content before irreversible action;
- respect valid foreign leases and prefer holder-bound completion/handoff;
- replay valid intent in the owned carrier when normal handoff is unavailable;
- prove semantic supersession rather than infer it from branch names;
- integrate and prove all remaining intent before candidate/accepted closeout;
- use accepted two-phase exceptional resolution for missing-lease lanes;
- retain all existing recovery packages and receipts; and
- perform no remote publication.

## Initial observation

The machine inventory is
`evidence/chronicle/all-work-lanes-convergence-20260716/lane-inventory.json`.
It records a read-only snapshot observed at `2026-07-16T13:30:28.617755+00:00`
(`2026-07-16 21:30:28 +08:00`).

Initial counts:

- live `work/*` refs including governance carrier: 61;
- frozen legacy cohort: 60;
- linked Work Lane worktrees including carrier: 57;
- unbound refs: 4;
- normalized valid leases: 4;
- missing leases: 53;
- dirty legacy linked lanes: 12;
- graph closeout candidates: 36, including 7 dirty overlays;
- graph implementation refs: 24;
- post-audit hosted-observation dirty overlay: 1; and
- semantic implementation families: 11.

The original graph audit and the live inventory agree on the 60-ref cohort, but
live HEAD and dirty facts have already moved for active lanes. Therefore the
inventory supports classification and policy only; each effect must use a new
fresh observation.

## Active normalized leases at initial observation

- `work/all-lanes-convergence-20260716` at
  `ecd9c0214738cd1db3439a0a81de334c8f378b3a`; dirty only from admitted carrier
  writes; holder `agent:codex:thread:ethos-all-lanes-convergence-20260716`.
- `work/expert-review-remediation-20260716` at
  `5a379f4d18e501db3b74cd5115cecdc01864d041`; clean; holder
  `agent:codex:thread:ethos-remediation-20260716`.
- `work/hosted-observation-targets-20260716` at
  `ecd9c0214738cd1db3439a0a81de334c8f378b3a`; dirty; holder
  `agent:codex:thread:complete-all-20260716`.
- `work/npm-supply-governance-20260716` at
  `84a1861c1af49d5cf10b66ad82db8b7a03e8a0cf`; dirty; holder
  `agent:codex:thread:019f696e-23fa-7d92-a972-60f39593bbd7`.

No other holder is impersonated by this decision. The hosted lane had a live
full-suite pytest process in the earlier process audit; process status must be
rechecked before any handoff. The npm lane was in an active-to-archive dirty
transition. The expert lane advanced after the graph audit. These facts make
normal holder coordination the first path, not opportunistic cleanup.

## Classification

The graph audit proves two disjoint sets covering all 60 legacy refs:

- 36 graph-level landed/superseded closeout candidates; and
- 24 refs retaining direct or semantic implementation intent.

The 24 implementation refs map exactly once into 11 families. The live dirty
hosted-observation overlay is an additional implementation surface assigned to
the hosted runtime/supply family. Exact membership and disposition are in the
inventory; human-readable strategy is in the program and implementation plans.

## Prewrite evidence

The carrier ran exact prewrite admission for the two plans, OpenSpec metadata,
proposal, design, tasks, scope, specification delta, Chronicle, inventory, and
claim. The admitted result was:

```text
command: ethos lane prewrite <11 exact paths>
         --editor-root <carrier> --require-editor-root --json
result: ok=true, state=admitted, required_gaps=[]
```

## Pre-archive validation checkpoint

Stable committed HEAD before this evidence update:
`2e75d422d04cf61f5bd15da57cc1b861dcb1d238`.

```text
ethos plan --changed --json
ok=true, state=planned, action_count=3, required_gate_count=0,
required_gaps=[]

ethos openspec --lifecycle --json
ok=true, state=clean, archive_preflight.state=ready,
archive_preflight.required_gaps=[]

ethos quality claims --json
ok=true, state=clean, required_gaps=[]

 tools/ci/scripts/run-markdown-lint.sh
111 files, 0 errors

inventory invariant check
61 live refs, 60 legacy refs, disjoint 36/24 partition, exact 11-family
coverage; result=exact
```

The task checklist is complete on the current worktree. The Chronicle digest is
refreshed in the claim after this update. Official archive is the next tracked
transition; parity refresh and executed proof intentionally bind the archived
semantic tree and its later stable HEAD rather than this active-carrier HEAD.

## Evidence still to append before this governance carrier archives

- strict OpenSpec lifecycle result;
- final claim digest refresh; and
- compiled change-plan result.

After official archive and an archive commit, the archived semantic tree must
receive a committed generic parity refresh. The final stable governance HEAD
must then receive HEAD-bound executed proof. The governance carrier must land,
complete sanctioned accepted-root
closeout, and retire before any missing-lease exceptional effect. Holder-bound
handoff/replay, red/green implementation evidence for the 11 families,
resolution decisions, preservation receipts, later candidate/accepted closeout,
and final cohort retirement belong to accepted-governance successor records.
Remote publication is not part of this record.

## Official OpenSpec archive

```text
openspec archive all-work-lanes-convergence-20260716 --yes --json
archivedAs=2026-07-16-all-work-lanes-convergence
specsUpdated=true, added=1, modified=0, removed=0, renamed=0

openspec validate --all --strict --json
9 specs passed, 0 failed
```

The accepted repository-governance specification now contains the cohort-bound
convergence requirement in this lane's Git tree. The claim carrier is rebound
to the archived OpenSpec path. Generic parity must now be refreshed against this
archived semantic tree before executed proof.

## Portability repair after initial executed proof

The first executed proof on `dd28ccffe5f02d75a4a00ae45c457b27578cb889`
completed 17 of 21 gates and correctly failed four gates. The failures did not
invalidate the cohort classification; they exposed a carrier portability defect:

- both newly added plan documents lacked required documentation front matter;
- the implementation plan contained host-local absolute worktree paths; and
- the evidence inventory serialized host-local absolute paths and expanded JSON
  source lines beyond the configured budget.

The repair preserves exact branch, HEAD, lease, dirty-status, and digest facts,
but replaces host-local paths with a branch-bound worktree locator and serializes
the inventory compactly. The two plans gain portable registry metadata. The
archived semantic decision remains unchanged; this is a post-archive evidence
portability repair. Parity and executed proof must be rerun on the resulting
HEAD.

The claim and archived scope use semantically equivalent compact TOML, and the
generic parity shadow is compact JSON. These carrier-only reductions keep the
source budget within policy without discarding any recorded fact.
