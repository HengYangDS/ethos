---
subject: ethos:artifact-topology-detached-residue-absorption-20260724
state: active
created: 2026-07-24
claim: artifact-topology-detached-residue-absorption-20260724
---

# Artifact-Topology Detached Residue Absorption — 2026-07-24

## Exact target observation

The sole source is `work/artifact-topology-hotpath-20260714` at
`70defe82f306708badf1cfabe0c3f8fa917287fa`.  Before normalization it was a
registered detached worktree with an absent branch ref, four unstaged tracked
files, no matching active-session checkout, no process path user, and no
current lease or Claim.  Its binary working patch SHA-256 is
`6a84df8a7b28d703f82f1bcac0ee61e8534874438e25c7376c38d8c81f5a404a`.

The historical branch was reconstructed at the exact detached HEAD and
attached to the same registered worktree without changing the working bytes.
Current lane status classifies it as linked, dirty, missing-lease,
missing-Claim, `ancestor_of_accepted`, and
`unpreserved_worktree_delta`.  Reconstruction did not transfer ownership or
authorize a lifecycle effect.

## Semantic judgment

The dirty work is an earlier artifact-topology hot-path implementation.  Its
useful goals are bounded traversal, reusable immutable policy evaluation, and
focused regression coverage.  Current accepted source owns those goals through
the bounded artifact reader introduced by `7c877872a`, the immutable
whole-decision cache introduced by `03ff8bb33`, shared CEL contract ownership,
and current topology/CEL tests.

The identity-keyed weak-reference projection cache is not replayed because the
current whole-decision cache is narrower and avoids extra lifetime state.  The
directory-collapse hunk is not replayed because current exact denied-leaf
reporting provides stronger evidence specificity while still pruning allowed
homes and irrelevant paths.  Thus useful behavior is semantically absorbed;
the historical implementation is explicitly superseded rather than silently
discarded.

## Exceptional resolution policy

`lane_resolution/preserve-retire`

Only after this carrier passes focused proof, exact-HEAD proof, candidate land,
and accepted-root closeout may the native resolver record and apply one fresh
decision for this exact source branch.  Apply must re-observe the exact head,
worktree binding, dirty paths, patch digest, missing lease and Claim, accepted
Chronicle, and path occupancy before preserving the dirty bytes and removing
only this branch and worktree.

## Recovery and clearing policy

The preservation package is recovery evidence, not proof of absorption.  It
must remain until a later accepted Chronicle binds its exact manifest and
selects `lane_resolution/clear-preservation` after comparison with current
accepted semantics.  The completion receipt remains durable.

## Non-claims

This decision does not affect another Work Lane, take over a valid owner,
replay historical implementation, mutate remote refs, claim hosted execution,
clear an unknown package, or authorize raw Git/state deletion.
