---
subject: evidence:assistants-playbook-routing-package
state: active
created: 2026-07-09
claim: assistants-playbook-routing-package
---

# Assistant Playbook Routing Package Evidence — 2026-07-09

## Claim

ETHOS removed the suffix-flat `ethos.assistants.playbook_utils` module from the
active assistant surface and moved its playbook command-routing logic into the
existing semantic subpackage `ethos.assistants.skills.routing`. Callers now import
the public `command_capability_gaps` symbol from the concrete semantic module.
No compatibility import shell or package-root facade was retained.

## Scope

- Deleted `packages/ethos/src/ethos/assistants/playbook_utils.py`.
- Added `packages/ethos/src/ethos/assistants/skills/routing.py` for playbook
  command capability routing.
- Updated `packages/ethos/src/ethos/assistants/playbooks.py` and focused tests
  to import the concrete semantic module.
- Shrunk `.config/checks/module-layout/policy.toml` ratchet debt from 18 to 17
  and suffix-module baseline from 17 to 16 after refreshing onto current candidate.
- Refreshed `.agents/skills/activation.toml` projection-generator digest because
  `ethos.assistants.playbooks` changed.

## Verification

Commands run from `work/assistants-playbook-routing-package` at implementation
time:

```text
uv run --group dev ruff check packages/ethos/src/ethos/assistants/skills/routing.py packages/ethos/src/ethos/assistants/playbooks.py tests/unit/assistants/test_playbooks.py
# All checks passed

uv run --group dev ruff format --check packages/ethos/src/ethos/assistants/skills/routing.py packages/ethos/src/ethos/assistants/playbooks.py tests/unit/assistants/test_playbooks.py
# 3 files already formatted

uv run --group dev pytest -q tests/unit/assistants/test_playbooks.py tests/unit/coverage/test_skill_package_edges.py tests/unit/cli/test_contracts_proof.py::test_projection_drift_reports_registry_and_generator_digest_state
# 8 passed

uv run --package ethos ethos quality module-layout --json
# ok=true; state=clean; suffix_module_count=16; debt_count=17; required_gaps=[]

uv run --package ethos ethos quality projection-drift --json
# ok=true; state=clean; required_gaps=[]

.agents/skills/ethos-quality-gate-governance/scripts/quality_audit.py .
# ok=true; required_gaps=[]

.agents/skills/ethos-skill-portfolio-governance/scripts/portfolio_audit.py .
# ok=true; required_gaps=[]
```

Final HEAD-bound proof and land readiness must be rerun after this evidence and
claim are committed. This record does not claim hosted CI or remote publication.

## Boundary

This lane is a debt-reduction and layout-ratchet shrink, not a new governance
mechanism. It does not clear remaining suffix-module debt, decompose the flat CLI
directory, or change playbook semantics. Those remain separate backlog items.
