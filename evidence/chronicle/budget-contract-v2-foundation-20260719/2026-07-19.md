# Budget Contract v2 Foundation — 2026-07-19

## Claim Boundary

This record binds only the Budget Contract v2 foundation: DR-0008, the staged
design and implementation plan, the stable v1 observation, and the
behavior-preserving move of the existing v1 reducer to its explicit domain
owner. The recorded parity and executed-proof receipts support that bounded
foundation at their named HEADs. This record does not claim typed v2 metric
implementation, calibration, cutover, debt settlement, global compression
completion, candidate integration, accepted-root closeout, remote publication,
or hosted CI.

## Initial Observation

The Work Lane started from `candidate/dev` at
`1fca210fd3550bcd62d127a3f9910ed1ff80523c`. Existing focused source-budget
contract, adapter, projection-rebase, and proof tests passed: 32 tests. The v1
source-budget report remained blocked with 17 required gaps, including 10
expired debt records and seven over-budget axes. The immutable declared
baseline remains `2dab77f169eceb2d45f917358c2a7487e7ac8db6`.

## Non-Actions

No v1 baseline, allowance, expiry, terminal target, gate enforcement, source
inventory, candidate branch, accepted root, foreign Work Lane, remote, or hosted
state was changed by this bootstrap.

## Foundation Decision And Extraction

DR-0008 records the metric-domain vector, non-compensation rule, per-file ELOC
retention, versioned parser/normalization boundary, and separation of model BPE
budgets from repository-source truth. The implementation plan sequences carrier
contracts, native adapters, replay, Debt v2, changed-scope admission, dual
control, DR-0009, cutover, global LOC retirement, and terminal settlement.

The existing v1 reducer moved from `ethos.domain.prove` to
`ethos.domain.source_budget.core`. The command registry and scorecard import the
new owner directly; no forwarding compatibility symbol remains in
`ethos.domain.prove`.

## TDD And Focused Verification

The new domain tests were created before production code. RED was observed as:

```text
ModuleNotFoundError: No module named 'ethos.domain.source_budget'
```

After the minimal extraction and test-owner correction:

- focused Task 1 suite: 99 passed in 21.25 seconds;
- focused docs/report registry suite: 17 passed in 14.89 seconds;
- Ruff over changed Python and tests: clean;
- TOML config lint: clean;
- JSON Schema check: clean;
- `ethos quality module-layout --json`: clean;
- `ethos quality code-size --json`: clean.

At the predecessor pre-commit stage, the standalone v1 source-budget result was
blocked with 17 inherited required gaps: ten expired debt records and the same
seven over-budget axes (Python product, Python tests, shell, TOML, Jinja, Python
total, and global total). Those dirty-worktree metrics were observations only.
The predecessor's stable committed observation and reviewed raw snapshot are
recorded below.

## Evidence Boundary

These focused results prove behavior-preserving ownership extraction and local
contract health only. They do not prove v2 measurement, baseline replay,
calibration, debt migration, dual control, cutover, global LOC retirement,
terminal compression, candidate integration, accepted closeout, remote
publication, or hosted CI.

## Predecessor Candidate Refresh And Stable v1 Observation

The owned Lane was refreshed through `ethos lane refresh-base` rather than a
manual rebase. The replay moved the Change from `d796a2794cccb2a000416b762a39cb67fc21580c`
onto candidate `c5ad10361106b3062820a293cd1948c439f1ebf6`, producing Lane HEAD
`a8b859b6fe98e0e270367bacd8380cb88d35b6a5` without a required gap.

At that clean committed HEAD, the raw v1 command output was written to the
ignored evidence path
`build/evidence/quality/source-budget/v1-foundation-snapshot.json` with SHA-256
`f59405be586e08f709a728f4ba3a767020dcbe836db685240727083dc096950e`.
The observation records:

- v1 state: blocked;
- required gaps: 17;
- global total: 116351;
- inventory digest:
  `e67f62c7da8e43b24804d4b3e5ec10f84f6cd71087d461eeeb8328e4d5d2663a`;
- immutable declared baseline:
  `2dab77f169eceb2d45f917358c2a7487e7ac8db6`.

The raw JSON remains ignored machine evidence; this reviewed Chronicle carries
its stable HEAD, digest, and claim boundary. After refresh, the 99-test focused
suite passed again in 26.89 seconds.

## Predecessor Parity And Executed Proof

The generic parity shadow was regenerated in the admitted Lane and matched all
eight external-versus-embedded command comparisons with zero required gaps and
zero false negatives. The parity evidence was committed at
`28caf8da0f2173ccd9f610ba7da90272ee9d9e42`.

A default executed proof bound to that exact HEAD completed with all 20 required
gates passing. Its evidence digest is
`fe6d69ea302c9697d8cc9e86a03e98ae1b196e0e7c894ff5ea11bec4827ece28`.
The first attempt failed only because controller scratch logs had been placed
flat under `build/runtime/`; moving them into the contract-approved
`build/runtime/work/budget-v2/` root made generated-artifact topology clean and
the same proof passed without product-code changes.

## Successor Recovery And Current Candidate Semantics

After the predecessor documentation HEAD
`b9ef4b8c3ee704f7718521e3ee74763da46a60e8`, candidate integration advanced the
current v1 source-budget implementation with declarative taxonomy and
`campaign_terminal` advisory semantics. The official `ethos lane refresh-base`
replay returned `refresh_base_failed` on the real semantic overlap and
automatically aborted back to the clean predecessor Lane. No manual rebase
continue, skip, raw ref move, or lease edit was used.

A successor Work Lane was started from candidate
`4ddd805872ac5645617a5b290381cfd25c68464f` with the same claim and holder. Before
extraction, `quality source-budget --json` had SHA-256
`1cfb16fd66e99543e848d70d508d09478e8ae7d7cd93e7f00ceb320bc188b857` and
reported:

- command state: `advisory`;
- enforcement: `campaign_terminal`;
- campaign: `global-declarative-compression-program`;
- required gaps: zero;
- campaign growth advisories: ten;
- active debt: 165 of maximum 165, owned by
  `node-runtime-compatibility-20260716`;
- global total: 110369;
- inventory digest:
  `e03aa5b9da647fd244c0864c195d96f3da9bce7807e4dd71821fb066b3b436e5`.

That successor observation does not erase or settle the predecessor's 17-gap
historical record, and it does not claim Budget Contract v2 implementation,
cutover, or compression completion.

## Successor TDD And Ownership Extraction

The successor again moved the tests before production code. RED was observed as
`ModuleNotFoundError: No module named 'ethos.domain.source_budget'` with pytest
exit 2 and one collection error in 0.24 seconds. The minimal GREEN commit is
`b6ecb2f27d07e7196d5037911299777d708d777e`.

Fresh independent verification ran 116 focused source-budget, adapter, report,
campaign, audit, command, CLI, and Lane tests in 19.83 seconds. Eleven reducer
ASTs and the migrated behavior tests matched the candidate implementation. The
command envelope, enforcement, campaign, baseline, terminal, active debt, debt
lifecycle, terminal target, and required-gap semantics remained equal. The new
module split made four additional governed files visible and increased measured
source by 28 ELOC (Python product +7; Python tests +21); the resulting advisory
values and inventory digest changed visibly rather than being hidden or
converted. Post-extraction command JSON had SHA-256
`f04ecf4ce8e784df44522e32a85e9f76962f36dca94ceada6d09ed94aed4ac1f`.

An independent code review reported no Critical, Important, or Minor findings.
The successor then merged predecessor HEAD `b9ef4b8c3ee704f7718521e3ee74763da46a60e8`
as reviewed history while retaining successor code, command policy, tests, and
parity inputs on every semantic conflict. This preserves predecessor ancestry
for ordinary post-closeout Lane retirement. Successor parity refresh and
HEAD-bound proof remain separate pending machine transitions and are not claimed
by this Chronicle section.

## Successor Merge-HEAD Observation, Parity, And Pre-Archive Proof

The successor absorbed predecessor HEAD
`b9ef4b8c3ee704f7718521e3ee74763da46a60e8` in merge commit
`e7602f6352af9565482ce04eac23512080b96aa0`. Its first parent is the reviewed
successor extraction commit `b6ecb2f27d07e7196d5037911299777d708d777e`;
the second parent is the predecessor documentation HEAD. Candidate `candidate/dev`,
`dev`, and `main` were still
`4ddd805872ac5645617a5b290381cfd25c68464f` at this observation.

At clean merge HEAD `e7602f6352af9565482ce04eac23512080b96aa0`,
the raw source-budget output was written to the ignored evidence path
`build/evidence/quality/source-budget/v1-foundation-snapshot.json` with SHA-256
`942dd4db468a61c1ddad0508baf901d1ba17d11a64deb9cbdbb3880e592a73be`.
It records:

- command state: `advisory`;
- enforcement: `campaign_terminal`;
- required gaps: zero;
- campaign growth advisories: ten;
- active debt: 165 of maximum 165;
- global total: 110477;
- governed file count: 1130;
- inventory digest:
  `823d8f5b64c9270cead0b9a1822575d614e538f37a4487502569ac82c184cb0f`.

These merge-HEAD values supersede the earlier pre-extraction observation for
current-state reporting without erasing the predecessor's historical 17-gap
record.

The successor generic parity shadow then matched all eight external-versus-
embedded command comparisons with zero required gaps and zero false negatives.
The parity-only commit is
`dd37e44a89ba133752b9d00e8626d80ba42bd21f`.

Fresh verification at that exact HEAD produced:

- focused source-budget, adapter, report, campaign, audit, command, CLI, Lane,
  and docs-registry suite: 131 passed in 27.22 seconds;
- Ruff over the changed Python and tests: clean;
- TOML config lint and JSON Schema checks: clean;
- claims: 184, clean, with zero advisory gaps;
- module layout and code size: clean;
- source budget: advisory with zero required gaps;
- strict OpenSpec validation: 10 of 10 items passed;
- OpenSpec lifecycle: clean;
- generic parity: clean with zero gaps;
- changed-scope plan: planned with zero required gaps.

A default executed proof bound to
`dd37e44a89ba133752b9d00e8626d80ba42bd21f` completed in state `proven` with
all 20 gates passing, zero required gaps, and evidence digest
`f43c148e5b2d234c07416e7c20d6af20f41379cad9a609431296eaaf152dd469`.
The ignored raw proof receipt has SHA-256
`5e60f168aec70a36c9f8d42442d298efaf9ac14a9e9a9eab9ec2fdfb059f5298`.

This completes Foundation task 3.1 only. It is a pre-archive proof, not an
archive-HEAD proof, and it does not claim archive, candidate landing,
accepted-root closeout, publication, retirement, v2 cutover, v1 global LOC
retirement, or terminal compression.

## Official Archive Transition

After Foundation task 3.1 was checked and the completed carrier was parity-bound
in commit `265cfd94cbfa25453c59b41f5f325da1e5e690e3`, a further executed proof
at that exact HEAD returned `gapped` with
`openspec_completed_change_unarchived:budget-contract-v2-foundation-20260719`.
That designed lifecycle signal propagated to the `repository-audit` and
`unit-architecture` gates. It was not treated as a code regression or bypassed:
the repository contract intentionally refuses to prove a completed Change that
still masquerades as active.

The official OpenSpec 1.6 archive command then ran on Sunday, July 19, 2026,
after holder-bound prewrite admission. It moved the carrier to
`openspec/changes/archive/2026-07-19-budget-contract-v2-foundation-20260719`,
reported `specsUpdated=true`, and added two capability deltas to the canonical
`contracts` and `quality` specs. The active carrier path no longer exists.

The claim state, carrier reference, promotion target, DR-0008 evidence link, and
both plan governance links are updated in the same archive-bound commit. Final
archive parity, archive-HEAD executed proof, candidate landing, accepted-root
closeout, local publication readiness, and both owned Lane retirements remain
subsequent machine transitions and are not claimed by this section.
