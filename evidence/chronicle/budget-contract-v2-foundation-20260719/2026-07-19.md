# Budget Contract v2 Foundation — 2026-07-19

## Claim Boundary

This record binds only the Budget Contract v2 foundation: DR-0008, the staged
design and implementation plan, the stable v1 observation, and the
behavior-preserving move of the existing v1 reducer to its explicit domain
owner. The recorded parity and executed-proof receipts support that bounded
foundation at their named HEADs. This record does not claim typed v2 metric
implementation, calibration, cutover, debt settlement, global compression
completion, candidate integration, accepted-root closeout, remote publication,
or hosted CI.

## Initial Observation

The Work Lane started from `candidate/dev` at
`1fca210fd3550bcd62d127a3f9910ed1ff80523c`. Existing focused source-budget
contract, adapter, projection-rebase, and proof tests passed: 32 tests. The v1
source-budget report remained blocked with 17 required gaps, including 10
expired debt records and seven over-budget axes. The immutable declared
baseline remains `2dab77f169eceb2d45f917358c2a7487e7ac8db6`.

## Non-Actions

No v1 baseline, allowance, expiry, terminal target, gate enforcement, source
inventory, candidate branch, accepted root, foreign Work Lane, remote, or hosted
state was changed by this bootstrap.

## Foundation Decision And Extraction

DR-0008 records the metric-domain vector, non-compensation rule, per-file ELOC
retention, versioned parser/normalization boundary, and separation of model BPE
budgets from repository-source truth. The implementation plan sequences carrier
contracts, native adapters, replay, Debt v2, changed-scope admission, dual
control, DR-0009, cutover, global LOC retirement, and terminal settlement.

The existing v1 reducer moved from `ethos.domain.prove` to
`ethos.domain.source_budget.core`. The command registry and scorecard import the
new owner directly; no forwarding compatibility symbol remains in
`ethos.domain.prove`.

## TDD And Focused Verification

The new domain tests were created before production code. RED was observed as:

```text
ModuleNotFoundError: No module named 'ethos.domain.source_budget'
```

After the minimal extraction and test-owner correction:

- focused Task 1 suite: 99 passed in 21.25 seconds;
- focused docs/report registry suite: 17 passed in 14.89 seconds;
- Ruff over changed Python and tests: clean;
- TOML config lint: clean;
- JSON Schema check: clean;
- `ethos quality module-layout --json`: clean;
- `ethos quality code-size --json`: clean.

The standalone v1 source-budget result remains blocked with 17 inherited
required gaps: ten expired debt records and the same seven over-budget axes
(Python product, Python tests, shell, TOML, Jinja, Python total, and global
total). At this pre-commit stage the dirty-worktree metrics were observations
only. The stable committed observation and reviewed raw snapshot are recorded
below.

## Evidence Boundary

These focused results prove behavior-preserving ownership extraction and local
contract health only. They do not prove v2 measurement, baseline replay,
calibration, debt migration, dual control, cutover, global LOC retirement,
terminal compression, candidate integration, accepted closeout, remote
publication, or hosted CI.

## Candidate Refresh And Stable v1 Observation

The owned Lane was refreshed through `ethos lane refresh-base` rather than a
manual rebase. The replay moved the Change from `d796a2794cccb2a000416b762a39cb67fc21580c`
onto candidate `c5ad10361106b3062820a293cd1948c439f1ebf6`, producing Lane HEAD
`a8b859b6fe98e0e270367bacd8380cb88d35b6a5` without a required gap.

At that clean committed HEAD, the raw v1 command output was written to the
ignored evidence path
`build/evidence/quality/source-budget/v1-foundation-snapshot.json` with SHA-256
`f59405be586e08f709a728f4ba3a767020dcbe836db685240727083dc096950e`.
The observation records:

- v1 state: blocked;
- required gaps: 17;
- global total: 116351;
- inventory digest:
  `e67f62c7da8e43b24804d4b3e5ec10f84f6cd71087d461eeeb8328e4d5d2663a`;
- immutable declared baseline:
  `2dab77f169eceb2d45f917358c2a7487e7ac8db6`.

The raw JSON remains ignored machine evidence; this reviewed Chronicle carries
its stable HEAD, digest, and claim boundary. After refresh, the 99-test focused
suite passed again in 26.89 seconds.

## Parity And Executed Proof

The generic parity shadow was regenerated in the admitted Lane and matched all
eight external-versus-embedded command comparisons with zero required gaps and
zero false negatives. The parity evidence was committed at
`28caf8da0f2173ccd9f610ba7da90272ee9d9e42`.

A default executed proof bound to that exact HEAD completed with all 20 required
gates passing. Its evidence digest is
`fe6d69ea302c9697d8cc9e86a03e98ae1b196e0e7c894ff5ea11bec4827ece28`.
The first attempt failed only because controller scratch logs had been placed
flat under `build/runtime/`; moving them into the contract-approved
`build/runtime/work/budget-v2/` root made generated-artifact topology clean and
the same proof passed without product-code changes.
