# Budget Contract v2 Foundation integration continuation — 2026-07-19

## Claim Boundary

This Chronicle continues only the Budget Contract v2 Foundation integration
episode. It binds candidate-drift recovery, ancestry preservation, the reviewed
baseline-replay correction, current parity, current proof, archive, and local
closeout transitions as they actually occur. It does not claim implementation
of Budget Contract v2 Tasks 2 through 10, v2 cutover, v1 global LOC retirement,
terminal compression settlement, remote publication, or hosted CI.

## Historical Foundation Evidence

- Historical carrier:
  `openspec/changes/archive/2026-07-19-budget-contract-v2-foundation-20260719`.
- Historical successor HEAD:
  `21b4ac47d22fca44155b8d79a0e9529f50f6e1d5`.
- The archive-HEAD executed proof reported 20 gates, zero required gaps, evidence
  digest `49a73041033186575215953470f4f1f9caf3a399ac68940739fc80d498d1ea63`,
  and receipt SHA-256
  `596c06b1dc83a89a2b62b0d2c1804e12f79dfa1afeadfc778517f0c49a9cdccc`.

Those facts are immutable historical evidence. They do not prove any
continuation HEAD and they do not authorize archive rewriting.

## Candidate Drift And Failed Refresh

The historical successor began from candidate
`4ddd805872ac5645617a5b290381cfd25c68464f`. Candidate then advanced to
`b90c409949eb37ae27967003c8fa98e97b78dcec`. The historical successor's land
attempt returned `candidate_base_stale`. The official `ethos lane refresh-base`
encountered a real semantic conflict, returned `refresh_base_failed`, and
automatically restored the old successor clean. No manual rebase continue,
skip, raw ref movement, or lease edit was used.

## First Continuation Admission

The first integration continuation started from `b90c4099` in:

- Lane: `work/budget-contract-v2-foundation-continuation-successor-20260719`;
- holder: `agent:codex:thread:019f77ec-a00d-75e0-9ba3-47a00855b705`;
- lease: `lease:2ab06c73-f71b-45d4-902d-f6933ca14c19`;
- claim: `budget-contract-v2-foundation-20260719`.

It staged a no-fast-forward absorption of historical successor `21b4ac47`.
The only merge conflict was `evidence/parity/generic-shadow.json`; the candidate
projection was retained, and therefore parity is explicitly stale until it is
regenerated after the merge. `system/commands.toml` retains both the
candidate-side `config-quality` gate and the Foundation provider declaration,
including `state_mode = "advisory_gaps"`.

Before that merge was committed, candidate, accepted, and release roots advanced
again to `797e6e48dd8771cae840b3d796cad89db2dcc199`. The current merge will first
be committed to preserve exact `b90c4099` / `21b4ac47` parent order. At that
checkpoint a further successor would have started from `797e6e48`, but successor
creation had not yet occurred. The topology-bearing merge will not be linearized
through a plain refresh replay.

During pre-commit verification, `candidate/dev` advanced again to
`ce337ef9f31be599a91f32804c289e176c782a14` while `dev` and `main` remained at
`797e6e48`. The seven candidate-only commits include detached-worktree
housekeeping and a parity refresh; their changed paths overlap this staged merge
at `system/commands.toml`. The next successor must therefore re-read the exact
candidate head and review that semantic overlap rather than assuming a clean
merge. `797e6e48` remains a historical checkpoint, not a promised future base.

## Superseding Baseline Replay Correction

An independent Git-blob replay of immutable baseline
`2dab77f169eceb2d45f917358c2a7487e7ac8db6` under the current-v1 classifier
produced:

- declared global total: 105342;
- replayed global total: 105060;
- net drift: -282 ELOC;
- JavaScript: 89 -> 90 (+1);
- YAML: 1082 -> 800 (-282);
- diagram: 24 -> 23 (-1);
- governed files: 933;
- inventory SHA-256:
  `f8e85ace7648b60592fbe6e678f78169afa98c6289b0e8bb7d7fbc3961fa1c8d`.

This supersedes the historical Foundation wording `105059` / `283` without
editing the archived Change or its historical Chronicle. The immutable declared
baseline remains unchanged.

## Extraction Plan Correction

The ownership extraction moved the two public reducers,
`source_budget_carrier_report` and `source_budget_report`, plus their private
source-budget helpers. Taxonomy remains declared by
`source_budget_taxonomy(...)`; there is no static category-constant owner to
migrate. The command provider explicitly uses
`state_mode = "advisory_gaps"`: default report mode projects the successful
provider result as `clean`, while the explicit mode projects a successful result
with advisories as `advisory`.

## Pending Native Transitions

The following are not yet claimed by this initial record: the exact merge SHA,
the latest-candidate successor merge, fresh parity, current HEAD-bound proof,
official continuation archive, archive-HEAD proof, candidate land, accepted-root closeout,
local publication readiness, Lane retirement, remote publication, or hosted CI.
Each will be appended only after its own command evidence exists.

## Remote And Hosted Boundary

No remote push, remote ref mutation, hosted CI run, or hosted publication was
performed for this continuation. Local proof and local publication readiness
must remain explicitly separate from those external observations.
