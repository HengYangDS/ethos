# Budget Contract v2 Native Measurement Chronicle

Date: 2026-07-19
Change: budget-contract-v2-native-measurement

## Authoring Boundary

This active Chronicle opens the bounded Task 3 implementation slice from the
accepted Budget Contract v2 design and officially archived Task 2 contracts.
The owned Work Lane is work/budget-contract-v2-native-measurement-20260719,
held by agent:codex:thread:019f77ec-a00d-75e0-9ba3-47a00855b705 under lease
lease:46e623c7-e990-4f59-b59d-abca3642b2a1.

At authoring start the Lane base and accepted/candidate line were
2024f603a9fe90f953c5f1819962a5cecbe24bed. Accepted root remains observe-only.
Foreign Work Lanes, remote publication, and hosted CI are outside this Change.

## Contract Corrections Before RED

Read-only governance and code audits found four plan defects now incorporated
into the T3 carrier rather than deferred as implementation surprises:

1. The original five-module flat measurement directory violates the repository
   new-directory burst limit and would likely exceed the 500-ELOC file ceiling.
   The corrected topology uses measurement/core.py plus a nested two-module
   measurement/native semantic package.
2. The metric registry labels providers only stdlib-3.14 although the product
   supports Python 3.12+, and labels shell posix-v1 although governed source
   uses Bash/Zsh constructs. T3 separates package runtime from measurement
   runtime: successful measurement requires canonical CPython 3.14 plus an
   exact provider descriptor and startup conformance fingerprint. It does not
   claim that Python 3.12 through 3.14 have identical parser behavior.
3. Direct JSON/YAML/Jinja/Shell/C4 fixtures below tests/fixtures would become
   unsupported, unclassified, or incorrectly product-scoped carriers. The
   corpus is one test-domain TOML file materialized under temporary directories.
4. T2 does not retain Git object IDs in the public inventory. T3 can bind exact
   descriptor-read worktree bytes and raw SHA-256, but immutable Git blob/HEAD
   identity and cross-file atomic replay remain Task 4.

PyYAML and Jinja already exist as direct dependencies. T3 adds no dependency;
it only bounds PyYAML to the admitted major so resolution cannot silently cross
the provider contract.

## Intended Measurement Contract

The active design requires strict frozen and self-verifying native, carrier,
coordinate, and snapshot models. Every load envelope has an exact XOR between a
typed success and stable required gaps. Exact raw content SHA-256 remains
separate from normalized/provider/vector identity.

Worktree reads traverse each path component relative to a repository descriptor
with no-follow semantics, require a regular final file, and compare device,
inode, mode, size, mtime, and ctime before and after reading. Those OS facts are
ephemeral drift checks and never enter persistent digests. Any file, parser,
signature, runtime-conformance, or state failure rejects the complete carrier
or snapshot result; partial clean vectors are forbidden. Direct carrier
measurement accepts only classified measured identities. Snapshot aggregation
skips reviewed exclusions while binding the complete manifest and inventory
digests, so exclusions remain visible in snapshot identity.

Native providers cover UTF-8 footprint/control, Python, TOML, JSON, YAML, INI,
Jinja parse-only structure, a finite repository-owned Bash/Zsh-capable shell
lexer, and the finite repository C4 grammar. Duplicate keys, non-finite values,
unsafe or cyclic YAML structures, invalid UTF-8, malformed syntax, unknown
signatures, and unavailable providers are required gaps. Model/BPE tokens remain
confined to agent-runtime budgets.

## Test-First And Evidence Plan

Implementation begins only after kernel and adapter tests fail for the missing
measurement contract and provider APIs. Subsequent RED waves cover contract
forgery, canonical-runtime and conformance identity, normalization, native
semantics, descriptor races,
reversed order, domain movement, and whole-result rejection. No failure is
waived into an expected failure.

The active claim began with governance carriers only because claim validation
rejects future paths and TDD forbids empty production placeholders. It now
promotes every currently existing contract and test owner. Its promotion set
must expand again immediately when later adapter or corpus owners are created
and before closeout.

Before closeout, the Change must bind focused 100 percent statement and branch
coverage for T3 owners, complete v1 source-budget regressions, lint, config,
schema, dependency, module-layout checks, strict OpenSpec and claims lifecycle,
independent review, generic parity, and exact-HEAD proof. Archive, archive-HEAD
proof, candidate land, accepted-root closeout, local publication readiness, and
owned-Lane retirement are separate later transitions. No remote push or hosted
CI result is claimed.

## Lifecycle State

This Chronicle binds authoring intent, scope, safety boundaries, the RED/GREEN
evidence plan, and the independently confirmed native-measurement contract
checkpoint for OpenSpec Tasks 2.1 and 2.2. It does not claim native-provider,
adapter, complete-inventory, parity, proof, archive, land, publication, or
retirement completion.

## Provisional Measurement Contract Review

The superseded pre-remediation contract implementation produced 26 focused tests with 100
percent statement and branch coverage, 115 source-budget regressions, clean
Ruff and type checks, and clean strict OpenSpec/claims/lifecycle checks. Those
prior runs are retained as execution facts, but they do not establish contract
completion.

The independent contract review returned NOT APPROVED with zero Critical and
four Important findings: carrier role/profile was not closed against native
contracts; canonical snapshot construction accepted caller-declared inventory
counts rather than a complete CarrierInventory; builder helpers could perform
sorting, arithmetic, or JSON encoding before primitive admission; and the new
untracked contract owner measured 586 effective lines, above the hard 500-line
logic limit that the tracked-only owner gate had not yet seen.

The empty-inventory portion of the review is rejected against higher-authority
Task 2 truth: an empty present-path inventory is already a required gap. The
remediation instead keeps zero-path failure, permits a non-empty all-excluded
inventory to yield an empty vector, validates the exact classified identity set
from the inventory, and keeps reviewed match/exclusion counts as
adapter/Chronicle-only reporting outside the snapshot schema and canonical
digests. It moves canonical payload/digest support into one bounded helper
module and contracts construction into model-owned classmethods.
The first remediation passed its focused gates but the follow-up independent
review remained NOT APPROVED with zero Critical and two Important findings. A
context-free CarrierMeasurementLoad could admit an internally consistent native
vector that was incomplete relative to the real metric registry, and a
context-free MeasurementSnapshotLoad could bind the real complete inventory
digest while carrying only a clean partial measurement vector. Bare model
validation therefore remains structural only; it is not registry or inventory
admission.

The third review wave also returned NOT APPROVED. Contract and evidence reviews
reported zero Critical and two Important findings; the adversarial security
review reported zero Critical, five Important, and one Minor finding. Mapping
success data was retained instead of an exact typed model, mapping context could
carry nested model_construct forgeries, tuple subclasses could subvert the XOR,
Load subclasses could override __post_init__, and an exact-type model_construct
with mutable lists could be admitted and mutated after load while retaining an
old digest. Some invalid inputs could also leak AttributeError or chained
validation detail rather than one stable load-boundary ValueError.

The fourth re-review remained NOT APPROVED with one Important finding. An
exact-type NativeMeasurement could hold tuple subclasses whose initial value
equality matched the revalidated built-in tuples, so equality-only admission
retained the caller-owned object. The subclass could later change iteration and
expose a partial vector while the old digest remained attached.

The fourth-remediation candidate centralized full Load admission in
measurement/admission.py. Success data and context must use exact concrete model
types; gaps must be an exact built-in tuple of exact strings; and Load envelopes
reject subclasses at class creation. Every Load stores the fully revalidated
canonical model rather than the caller-owned instance, and carrier/snapshot
success replays canonical construction against that stored form. Mapping,
forged, mutable, top-level subclassed, mismatched, or context-free success fails
closed with stable boundary errors, while caller-owned nested container
subclasses are canonicalized away. Failure Loads forbid success context;
NativeMeasurementLoad remains context-free because its complete resolved
contracts are embedded.

The fifth independent contract re-review remained NOT APPROVED with zero
Critical and two Important findings. A redundant multiple-inheritance form could
place both an existing Load and FinalLoad in the direct bases, pass the original
base-membership guard, and replace __post_init__. An exact-type model could also
shadow its instance model_dump with a callable that raised RuntimeError, causing
caller-owned detail to escape instead of one stable Load-boundary ValueError.
The Chronicle description of those two boundaries was therefore ahead of the
candidate and has been corrected here rather than treated as approved evidence.

The post-fifth-review candidate now requires FinalLoad to be the sole direct base
of every allowed Load definition. Exact models are admitted only when their
Pydantic field storage has the exact declared field set with no extra or private
state; revalidation calls BaseModel.model_dump directly rather than an
instance-shadowable method, then stores only the canonical result. Redundant
post-validation equality logic was deleted after the stronger storage boundary
made that branch unreachable. The two reviewer reproductions now return the
stable boundary errors, including no chained caller detail.

A subsequent local boundary audit found that comparing field keys through
set(storage) could re-hash a caller-owned str subclass after insertion and let
its custom exception escape. The candidate no longer hashes any untrusted key:
it first requires each stored key to be an exact built-in str and only then
checks membership in the trusted declared-field mapping. A regression test
switches the key behavior after insertion and now receives the same stable Load
error without caller detail.

The R7 review package was withdrawn before a decision after an independent
reviewer found that an unrelated first base could define a non-cooperative
__init_subclass__, swallow FinalLoad's hook, and create a descendant that
replaced __post_init__. The same local boundary pass found that validating only
the top-level model storage left a nested BaseModel able to carry a non-built-in
field key whose changed hash behavior escaped during Pydantic serialization.

The current candidate replaces cooperative subclass hooks with a metaclass that
rejects descendants of direct Load envelopes before type creation, independent
of base order. Canonical storage admission now lives wholly in
measurement/admission.py and recursively inspects every nested BaseModel and
tuple through built-in accessors before validation. It requires exact declared
field keys, no extra/private state, exact built-in scalar leaves, no mutable or
unknown leaves, and no model/tuple cycles. Only after that closed traversal may
Pydantic produce the canonical payload; data and context therefore cannot run
caller-owned field-key, iterator, instance-method, or inheritance behavior at
the Load boundary. This stronger admission also reports noncanonical context as
typed-context failure before authoritative replay.

The R9 independent re-review remained NOT APPROVED. The simplicity review found
no Critical or Important issue, but two independent contract/data-model reviews
found the same Important boundary: an exact top-level success or context could
contain a nested Pydantic subclass. Storage admission compared that nested
object against its own declared fields instead of the trusted parent-field
annotation, so revalidation could silently truncate the subclass to the base
model. Reintroducing whole-model equality was rejected because caller-owned
nested equality could execute or raise during admission.

The R10 candidate carries each trusted Pydantic field annotation through the
recursive storage walk. A nested BaseModel must now have the exact model type
declared at that field position before any nested storage or serialization is
read. Annotated, union, literal, variadic tuple, and exact scalar leaves are
handled directly; tuple subclasses remain safely canonicalized through the
built-in tuple iterator, while nested model subclasses are rejected with the
stable Load boundary error. A regression uses a nested subclass whose equality
raises and proves that rejection occurs without executing caller equality.

The candidate now has 39 focused contract/integrity tests with 100 percent
statement and branch coverage across 400 statements and 106 branches in
measurements.py, measurement/canonical.py, and measurement/admission.py. The
complete source-budget regression set has 190 passing tests. The canonical
digest surface is now typed-only: each typed helper enters the domain-separated
digest directly, raw list/dict digest helpers were removed, and the tests use an
independent JSON-plus-SHA-256 oracle instead of calling production digest
helpers. New-owner Ruff and format checks are clean in addition to the
repository lint ratchet; types, config, JSON Schema, dependency hygiene, import
boundaries, module layout, code-size, source-budget, strict OpenSpec, lifecycle,
claims, and diff checks also pass. The model owner measures 472 effective lines
below the unchanged 500-line hard ceiling; canonical.py and admission.py measure
99 and 136 effective lines.

The frozen R10 package then received APPROVED decisions from all three bounded
independent reviews: API-to-spec consistency, object-model and deterministic
serialization consistency, and spec-bound simplicity. None reported a Critical
or Important finding. OpenSpec Tasks 2.1 and 2.2 are therefore complete at this
bounded native-measurement-contract checkpoint. The remaining provider,
descriptor, full-inventory, proof, archive, and closeout work remains pending.
The subsequent pre-commit owner formatter changed only layout in four reviewed
Python files; parsed ASTs before and after formatting were identical, focused
coverage remained 100 percent, and no reviewed behavior changed.

Staging the new files then exposed a separate owner-wide quality fact: earlier
full-lint runs used git ls-files and therefore could not include still-untracked
new Python owners in the ignored-rule ratchet. No ratchet ceiling was raised.
The current candidate instead removes every new ARG, EM, TRY, PLR0911, and
PLR0913 finding by centralizing stable Load error construction, separating
validated-success admission from context replay, splitting model/tuple storage
checks, deriving the snapshot vector digest internally, and narrowing test-only
helpers. The repository ratchet is back at its exact pre-change counts; focused
coverage is 400 statements and 106 branches at 100 percent, all 190
source-budget regressions pass, and the three owner ELOC values are 482, 99, and
136. Because this quality refactor changed production AST after the R10
approvals, those approvals remain historical evidence and a frozen R15
confirmatory review was required before the current shape could be claimed
approved. The frozen R15 package (review brief
`a8e5bf9cf23eeba0f625d9ef609ca8e5fdafe4b5db110abad5acb2adf730ec59`,
manifest `d2579c177d9092dbbe2d8ef41d32b10b17dac265042a5090e11aabd7ff099333`)
received APPROVED decisions from all three bounded reviews: API-to-spec
consistency, object-model and deterministic-serialization consistency, and
spec-bound simplicity. None reported a Critical or Important mismatch. The
current ratchet-clean production AST is therefore the independently confirmed
OpenSpec Tasks 2.1 and 2.2 checkpoint; provider and later lifecycle work remain
separate.

A final audit of the R15 gate directory then found that its `current=complete`
marker meant execution had finished, not that every gate was green. The bundled
code-size result was blocked because
`test_source_budget_measurements_integrity.py` measured 812 effective lines
against the unchanged 800-line test ceiling. The source-budget result remained
an allowed advisory with no required gap, and every other parsed R15 owner gate
was green, but the bundle as a whole could not be called passing.

The remediation moved the reviewed golden/domain-separation test and public
module-surface/size test unchanged from the integrity module to the contract
module. It added no file, changed no production byte, removed no assertion, and
left the combined multiset of all 33 top-level test-function ASTs identical to
R15. The three production owners retain their exact R15 SHA-256 values. Focused
coverage remains 39 passing tests, 400 statements and 106 branches at 100
percent; all 190 source-budget regressions pass. The two test owners now measure
357 and 775 effective lines, and the official code-size gate is clean. Because
the two test-file hashes changed after R15, its approvals remain authoritative
for the unchanged production AST and pre-relocation package. The frozen R16
package (review brief
`b5be109fddaa52a70579374dee4b763cf863847076fdd2f4e76e5fff7d56d5d1`,
manifest `bee79ef6f15e89da165b0d8515a9d5fd834d0acdbb37a8e60c0efa104ec52443`,
R15-equivalence record
`a90b0bdecf3128c8b9e9fb8d19ff098c9d9c342333e9a08bff1ce89551d74eae`)
then received APPROVED decisions from all three bounded reviews: API-to-spec
consistency, object-model and deterministic-test consistency, and spec-bound
simplicity. None reported a Critical or Important mismatch. The current
ratchet-clean production and test package is therefore the independently
confirmed OpenSpec Tasks 2.1 and 2.2 checkpoint.

Native-provider research also moved four failure controls upstream before RED:
O_NONBLOCK plus post-read descriptor-entry revalidation, a reviewed constant
conformance digest, YAML 1.2 boolean resolution while retaining graph rejection,
and Jinja comment payload accounting so giant comments cannot launder to zero.
The current GitLab YAML anchor/alias use will be recorded as a deterministic
complete-inventory provider gap rather than silently weakening the YAML grammar.
