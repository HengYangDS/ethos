---
subject: quality:budget-contract-v2:vector-policy-debt
state: active
created: 2026-07-24
claim: budget-contract-v2-vector-policy-debt-20260724
---

# Budget Contract v2 Vector Policy And Debt — 2026-07-24

## Accepted Input

Task 4 is archived, proven, landed, accepted, locally publication-ready, and its
owned Lane is retired at accepted/candidate/main HEAD
`a0e5a8294638c151770da90eea32146d8b8192bc`. It provides immutable Git replay
and a fail-closed v1-authoritative shadow.

The accepted evidence does not provide a complete v2 baseline vector. Historical
v1 profile `v1-continuation-20260719` is a reviewed v1 observation with v2 null.
Selected C1 checkpoint `3468ce78e2b636b9c0516904aa73cde2eb30fa62`
remains blocked by
`source_budget_native_parse_failed:yaml:.config/ci/templates/hosted/gitlab-ci.yml`
and therefore exposes no provider coverage, coordinates, vector digest, or
snapshot digest.

## Decision

Task 5 will land strict reusable v2 contracts, a pure reducer, a sibling config
loader, and a versioned v1/v2 schema composition. The repository declaration
will remain explicitly `inactive`; v1 remains authoritative.

The sole v1 debt successor, `node-runtime-compatibility-20260716`, remains
`unmapped`. No admitted HEAD is inferred from commit order. Its missing bindings
are admitted HEAD, scope digest, inventory digest, baseline snapshot, and
historical replay. Candidate coordinate deltas from the design audit remain
context only, not enforceable allowance:

- `tests.python / lexical_tokens / lexical_token`: `+846`
- `tests.python / normalized_bytes / normalized_byte`: `+10476`
- `tools.shell / lexical_tokens / lexical_token`: `+230`
- `tools.shell / normalized_bytes / normalized_byte`: `+5933`

## Carrier Admission

The bounded active Change passed strict OpenSpec validation, ETHOS lifecycle
validation, config lint, and Claim health. Claim inventory is clean across 241
Claims with zero advisory gaps. The owned Work Lane is bound to
`budget-contract-v2-vector-policy-debt-20260724`; the external binding receipt
SHA-256 is
`3b9cd730a79ae4982208238f153a9eb6808d44d3bbc58311f986f2881a71166f`.

## Boundary

No implementation, proof, archive, candidate land, accepted-root closeout,
publication, Task 6 admission gate, DR-0009, dual control, v2 authority, global
v1 LOC retirement, or terminal settlement is claimed by this initial carrier.

## Implementation Result

Task 5 now defines strict frozen v2 vector, baseline, wave, mapped/unmapped debt,
inactive-policy, and shadow-policy contracts under the semantic
`source_budget/policy/core.py` subpackage. The published source-budget schema is
an explicit composition of the unchanged v1 policy union and the v2 policy
union. The accepted Task 4 observation is public as
`SourceBudgetShadowObservation`; Task 5 reuses it rather than duplicating replay
or snapshot contracts.

The pure `compile_budget_verdict(observations, policy, today)` reducer performs
no filesystem, Git, config, environment, or clock reads. It verifies complete
reviewed Task 4 observations, exact baseline identity, manifest and metric
contract identity, canonical coordinate/unit sets, and mapped-debt replay
bindings before arithmetic. Every coordinate is evaluated independently with:

```text
allowed = baseline + permanent allocation - settled reduction + valid active mapped debt
```

Unmapped, expired, overdue, stale, or replay-mismatched debt contributes zero
allowance and accumulates stable blocking gaps. Inclusive expiry and wave dates
remain active through the named date. Transition overages block, campaign growth
is advisory, terminal overages block, and terminal mode also blocks while valid
debt remains active. Debt results are independent of policy record order.

The tracked `[quality.source_budget_v2]` declaration is deliberately
`state = "inactive"`. It binds the original baseline HEAD
`2dab77f169eceb2d45f917358c2a7487e7ac8db6` and the existing campaign, but
contains no baseline or terminal vector. The node-runtime successor remains
`mapping_state = "unmapped"` with all five missing bindings explicit and no
allowance. The v1 table and loader remain unchanged and authoritative.

## TDD And Validation Evidence

The initial focused RED run produced 18 expected missing-feature failures and 31
passes across 49 tests. Two later reducer tests first failed because only the
first required gap was returned; a dedicated order-independence test first
failed because reversed debt records changed output order. The implementation
then accumulated all stable gaps and canonically evaluated debt records.

Fresh GREEN evidence on July 24, 2026:

- focused Task 4/5 contract, reducer, config, and shadow tests: 51 passed;
- full source-budget kernel/domain/adapter/replay regression selection: 674
  passed;
- Python type policy: zero diagnostics in both `ethos-core` and `ethos`;
- Ruff and format: clean with every ignored-rule ratchet exactly unchanged;
- module layout: zero debt; import contracts: 3 kept, 0 broken;
- targeted TOML/JSON config lint and all JSON Schema metaschema checks: clean;
- docstring contract: 100 percent; no-compat and product-boundary: clean.

Changed production files remain within existing per-file ELOC ceilings:

- source-budget v1 contract core: 127 / 500;
- v2 policy core: 253 / 500;
- config adapter: 80 / 500;
- source-budget domain core: 397 / 500;
- v2 verdict reducer: 325 / 500.

## Current Boundary

This evidence closes implementation and owner-gate Tasks 2 through 4 only. It
does not claim exact-HEAD proof, parity freshness, OpenSpec archive, candidate
land, accepted-root closeout, local publication readiness, remote publication,
hosted execution, Task 6 admission, DR-0009, dual control, v2 authority, global
v1 LOC retirement, or terminal settlement.

## Implementation Commit And Base Refresh

The bounded implementation was committed as
`feat(quality): add vector policy and debt v2`. Candidate/accepted advanced
independently to `972d3cf58f441007f9049224878b0f37a46195c4`, so the owned Lane
used the native `lane refresh-base` transition instead of direct Git mutation.
The refreshed history is candidate-base current with carrier commit
`22476092d` and implementation commit
`587ff68e0062075ba79412a14cc1123e091c043b`; the focused 51-test selection
remained clean after refresh. No foreign Lane was modified or taken over.

## Task 5.1 Pre-Archive Proof Admission

Generic shadow parity was regenerated from implementation/evidence HEAD
`04ff9fe058d176b29e349fa0bd1f1d704dea1a92` and committed as
`f3200ee9b7e0c53bfe55bfe6b8adee720cecae95`. The tracked evidence reports eight
comparisons, zero false negatives, and zero required gaps. This parity receipt
binds that source HEAD only; the final pre-archive HEAD must include this
Chronicle/Claim refresh and a later HEAD-bound parity refresh.

Task 5.1 admits the following final transition on one stable, clean, committed
pre-archive HEAD:

```bash
tools/ci/scripts/run-ethos-lane.sh prove \
  --execute --expect-head "$(git rev-parse HEAD)" --json
tools/ci/scripts/run-ethos-lane.sh prove \
  --execute --full --expect-head "$(git rev-parse HEAD)" --json
```

Official archive is prohibited unless both commands return `ok=true`,
`state=proven`, an empty `required_gaps` list, and the exact same expected HEAD.
Checking Task 5.1 in the carrier records this executable acceptance boundary; it
does not pre-claim proof success. Archive, archive-HEAD parity/proof, candidate
land, accepted-root closeout, local publication readiness, remote publication,
hosted execution, Task 6 admission, or Lane retirement remain unclaimed.
