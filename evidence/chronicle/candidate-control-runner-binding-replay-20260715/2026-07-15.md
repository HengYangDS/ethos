# Candidate control-runner binding replay — 2026-07-15

## Claim

An accepted-root `reference-transaction` hook preserves its accepted checkout
shell and CAS boundary while evaluating an accepted prepared transition through
the clean candidate checkout at the exact promoted HEAD.

## Boundary

- **Accepted shell:** owns Git hook installation, transaction failure, and the
  protected ref boundary.
- **Candidate reducer:** owns only the promoted tree's semantic admission
  evaluation, invoked with the accepted root as control state.
- **Not asserted:** candidate self-authorization, independent control-replacement
  receipts, accepted closeout, remote publication, or hosted CI success.

## Evidence

- Fresh owned lane: `work/candidate-control-runner-binding-replay-20260715`,
  created from candidate HEAD `1d481e889fd02d258909708ed6f4c8c3f7791e8e`
  with a lease bound to this Claim.
- The hook requires the configured linked candidate worktree, configured branch,
  exact `new_value` HEAD, clean tracked and ordinary-untracked state,
  candidate-local runner, and source roots physically under that checkout.
  Binding failure rejects the accepted move; accepted-old source is not a
  fallback.
- Focused regressions passed independently:
  - `test_git_hooks_use_repo_bound_python_runtime`
  - `test_reference_transaction_updates_work_lane_lease_after_commit`
  - `test_closeout_binds_semantic_hook_runner_to_candidate_checkout`
  - `test_raw_ref_move_to_proven_head_is_blocked_without_marker`
  - `test_sanctioned_land_and_closeout_pass_through_armed_hook`
  - `test_candidate_refresh_from_accepted_admitted_without_bypass`
- Shell syntax, Python lint, direct docstring quality, and official OpenSpec
  strict validation passed before this evidence record.

## Decision

The remedy changes runtime provenance, not admission criteria: marker, candidate
proof, topology, policy digest, and external control-replacement obligations
remain independently required. Generic parity and HEAD-bound proof are deferred
until this lane has its own committed HEAD; no prior parity or proof record is
reused for that future state.
