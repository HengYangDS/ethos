# Candidate-Hook Bootstrap Closeout — 2026-07-18

## Claim Boundary

This carrier resolves a local hook-source bootstrap problem in an official
accepted_ff closeout. It does not assert accepted-root advance, lane retirement,
GitHub publication, GitLab recovery, remote synchronization, or hosted proof.

## Observed Boundary

The accepted checkout at `8a1cbb83e93c4417fc569f853d43c55dfdd80943` still
owns its existing tracked hook until a closeout commits. Candidate
`3acf8b38c92ff7c306e813d94679feacf88ac729` contains the release-mirror repair,
but an accepted-root atomic update invokes the incumbent shell hook by default.
A candidate-source repair cannot bootstrap itself if that old hook rejects the
new `main` transition.

## Intended Mechanism

When the candidate changes the tracked `reference-transaction` hook, official
closeout selects its exact `.githooks` directory with a per-command Git
configuration argument for that one `update-ref --stdin` transaction. It does
not edit Git configuration. Candidate hook presence and executability are then
preflight conditions; an unchanged-hook closeout remains on its configured hook
route. Normal raw Git never receives the closeout-only candidate hook route.
The existing candidate-external control receipt remains required for changed
control paths.

## Authoring Evidence

On the uncommitted carrier, the focused suite
`tests/unit/lanes/test_armed_hook_closeout_e2e.py`,
`tests/unit/coverage/test_core_state_claims_edges.py`,
`tests/unit/lanes/test_apply.py`, and
`tests/unit/cli/test_contracts_closeout.py` passed 43 tests. It includes an
armed integration regression with a deliberately legacy accepted hook and an
upgraded candidate hook; raw `main` movement remains blocked while sanctioned
`accepted_ff` closeout advances both protected refs atomically. This run is
implementation evidence, not the pending HEAD-bound proof for the committed
carrier.

## Evidence Boundary

The carrier still requires official archive, an exact HEAD-bound default proof,
candidate land, a new candidate-external receipt, and a later accepted-root
closeout. Until those transitions occur, this is authoring scope rather than an
accepted-root local-closeout claim.

## Non-Actions

No foreign lane, dirty overlay, runtime-managed location, remote, runner,
GitLab process, worktree removal, branch deletion, Git stash, or credential is
modified.
