---
subject: ethos:claim-envelope-unification-compression
role: evidence
state: active
relations:
  supports: evidence/claims/claim-envelope-unification-compression-20260712.toml
---

# Canonical Claim Envelope Unification Chronicle

## Scope

This bounded migration converts every tracked historical top-level claim record
to the single canonical claim envelope. It retains each dated chronicle,
OpenSpec carrier, focused verification reference, promotion target, and
historical provenance while removing the second runtime parsing and output path.

## Verified Slice

- The tracked claims inventory contains 125 canonical envelope records and no
  top-level alternate claim record.
- The focused claim-governance and claim-state edge suites passed: `40 passed`.
- Python format/lint, TOML format/lint, type policy, claims, evidence freshness,
  repository shape audit, OpenSpec lifecycle, and strict OpenSpec validation all passed.
- The global effective source budget is `109,950` and Python effective source is
  `87,908`: respectively 11 and 43 lines below the parent `dev` head.
- One stale promotion path was reconciled to the current OpenSpec adapter
  module before the canonical envelope made that target enforceable.

## Boundary

This record binds a source-format convergence only. It does not restate old
behavioral evidence as a present-state proof, claim hosted enforcement, or
replace the required HEAD-bound proof for promotion. The migration fails closed
if an alternate top-level claim shape is read by the product.
