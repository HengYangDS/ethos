---
subject: evidence:coordination-invalid-state-projection
state: active
---

# Coordination Invalid-State Projection Evidence

## Claim

ETHOS status now projects Work Lane coordination required and advisory gaps into
the shared invalid-state taxonomy without changing advisory signals into blockers.

## Evidence

- Focused regression tests:
  `uv run --group dev pytest tests/unit/lanes/test_lanes.py::test_workspace_status_reports_foreign_work_lanes_without_reading_them tests/unit/governance/test_validation_gates.py tests/unit/kernel/test_invalid_states.py -q`
- Lint and config gates:
  `.config/ci/scripts/run-python-lint.sh && .config/ci/scripts/run-config-lint.sh`
- Live status probe:
  `ETHOS_ROOT=$PWD uv run --group dev ethos status --json`

## Live Probe Result

The live coordination package reports advisory gaps for foreign Work Lanes and
missing leases, and `invalid_states.categories.change_unbounded` contains those
same coordination signals. Required gaps remain empty and coordination remains
non-blocking.
