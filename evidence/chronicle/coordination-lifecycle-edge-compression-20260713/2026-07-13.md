---
subject: quality:coordination-lifecycle-edge-compression
state: accepted
---

# Coordination-Lifecycle Edge Compression — 2026-07-13

## Baseline

- Work Lane: `work/coordination-lifecycle-edge-compression-20260713`.
- Original lane base: `8022d734f14d94f438eae61a7c0b9e8236a50de6`.
- Canonical formatter-clean scoped measure: **767** effective Python lines.

## Cutover

- Converted only pure handoff-context, lease-projection, and prewrite-reason
  partitions to literal tables.
- Removed two duplicate scalar-rejection probes for `string_sequence`; retained
  one named tuple/scalar normalizer contract.
- Kept named handoff, SQLite lease, package, and ref-transition effect tests.
- Formatter-clean scoped result: **736** effective lines, **31 deleted** from
  the canonical baseline (4.04%).

## Verification

- Focused test file: `18 passed`.
- Ruff check and formatter check pass for the scoped file; an obsolete `FBT003`
  file-level suppression was removed as part of the cutover.
- A narrow `--cov=ethos_core.normalization.core --cov-fail-under=100` probe is
  intentionally not an acceptance gate: this lifecycle-edge file covers only
  the shared normalizer's tuple/scalar branch (57% isolated module coverage),
  while the repository coverage gate owns its complete module corpus.
- Focused test, scoped Ruff, source-budget, and strict OpenSpec validation passed.
- Repository-wide lint, coverage, final generic parity, and HEAD-bound proof remain
  required before candidate promotion; the final parity receipt is intentionally
  generated only after this archival carrier is committed.
- Candidate landing, accepted-root closeout, local publication readiness, and Work
  Lane retirement are post-archive governed transitions, not preconditions for
  archival task completion; remote publication remains separately deferred.

## Archive

- Official OpenSpec archive: `openspec/changes/archive/2026-07-13-coordination-lifecycle-edge-compression`.
- The accepted quality requirement now lives in `openspec/specs/quality/spec.md`; the archived carrier preserves the bounded change record.

## Boundary

This claim concerns test representation only. It does not alter production
coordination, lease persistence, handoff protocols, CLI payloads, schemas, or
remote publication policy. GitLab publication remains deferred while unavailable.
