# Cross-Host Handoff Fixture Compression Chronicle

- Scope: bounded command-envelope reuse in
  `tests/unit/cli/lane/test_cross_host_handoff.py`.
- Formatter-clean effective test lines: **402 -> 278** (**-124**).
- Countable claim carrier: **43** effective TOML lines; archive metadata and
  canonical quality requirement are counted by the global source budget.
- Preserved cases: file context, omitted dirty disposition, rejected committed
  disposition, clean export for import/revoke, preserved tracked and untracked
  work, content-address identity change, and import rollback.
- Focused result: `7 passed` for
  `tests/unit/cli/lane/test_cross_host_handoff.py`; Ruff format/lint passed and
  the global `PLR0913` ratchet was restored from 32 to its 31-line ceiling.
- Source-budget report remained clean and moved the active global metric from
  `110,581` to `110,506` before final archive accounting.
- Generic self-shadow parity was refreshed and clean at the committed parity
  head; final proof is re-executed after archive metadata is committed.
- Authority: this Work Lane only; foreign Work Lanes are observe-only.
- Non-evasion: no production logic, quality threshold, baseline, or compression
  debt expansion is permitted.
