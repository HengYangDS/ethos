# Declarative linked-retirement matrix — 2026-07-20

## Fact

- Work Lane and Claim identity: `declarative-lifecycle-matrix-20260720`.
- Logical OpenSpec Change identity: `declarative-lifecycle-matrix`; the date
  belongs only to the archive transition and evidence chronology.
- This carrier compiles renew, resume, handoff-offer, and handoff-accept from
  the tracked workflow declaration and consolidates ordinary linked landed and
  superseded retirement behind one strict request and one generation-bound
  effect.
- The effect uses a SQLite immediate transaction for exact lease identity and a
  Git ref transaction for accepted-ref verification plus exact lane-ref
  deletion.
- Exceptional authority policy remains separate; its admitted exact-revoke
  execution is consolidated onto the canonical primitive.
- Cross-host import is bound to the package target actor. Its content-addressed
  acknowledgement carries the complete destination Lease generation but is
  explicitly a destination-holder assertion, not a signature or remote
  authority proof.
- Content-addressed handoff directories are never overwritten. Exact existing
  content may be reused; invalid, extra, or colliding content blocks in place.
  Failed import revokes its new Lease only after its exact Git carriers are
  proven absent.
- Candidate landing, accepted closeout, remote push, hosted CI, and publication
  are not established by this record.

## Evidence

- The last complete local CI before final review passed under Python 3.14.6
  with warnings treated as errors: `2233 passed`; all `19,010` product
  statements and `5,002` branches reached 100% coverage. The final frozen tree
  is rerun after review remediation; this earlier run is not presented as that
  final proof.
- Python lint and format passed at the existing Ruff ratchets; both governed
  packages reported zero type diagnostics. Config, JSON Schema, shell,
  Markdown, prose, import boundaries, dependencies, docstrings, module layout,
  no-compat, product boundary, governance kernel, vulnerability, secrets,
  generated-artifact, architecture, install, and release-supply gates passed.
- Official OpenSpec 1.6.0 strict validation accepted the date-free logical
  Change `declarative-lifecycle-matrix` without a Change-local issue.
- The official lifecycle projection found no Change-local validation issue;
  archive preflight is rerun after independent reviews and final task closure.
- The post-removal Git diagnostic failure regression now preserves the partial
  transition as `branch_delete_failed_after_worktree_removed` with
  `worktree_removed=true` and `ref_preserved=true`; it no longer escapes into a
  generic lease-cleanup result.
- SQLite schema tests now prove fresh current-table initialization, exact owned
  schema validation, subject uniqueness, idempotence, preservation of unrelated
  shared-state tables, and fail-closed rejection of retired lease shapes or
  noncanonical constraints.
- The current remediation-specific suite passes `168` related tests, while the
  handoff and compact edge subset passes `18`; Python lint/format and JSON
  Schema validation are clean at the current tree.
- Against Change start `cef0f591ceb927239b2c8a743d03e8d90a08ad6f`, tracked
  raw diff is `+4140/-2805` (`+1335` net). OpenSpec and evidence carriers are
  `+676` net; packages are `+114`, tests `+642`, system `-107`, docs `+10`, and
  other tracked carriers are net zero.
- Under the repository `effective_code_lines` metric, packages are `+161` and
  tests `+626`, for a temporary packages-plus-tests net of `+787`. This is not a
  terminal compression result. Product behavior now has one linked-retirement
  owner and no landed package, compatibility resolver, legacy JSON Lease read,
  or dedicated historical regression file; later campaign Changes must delete
  the temporary test/state-matrix growth and restore the terminal budget.

## Campaign advisories

- Official full-corpus validation still reports 15 long-requirement INFO items:
  two in `quality` and thirteen in `repository-governance`.
- Taplo emits four successful-run INFO records; the final campaign policy still
  requires that expected tool chatter be made quiet without hiding failures.
- Public docstring coverage is 100% (`169/169`), while the broader advisory
  inventory still identifies 370 top-level public-looking definitions without
  documentation.
- Source-budget reporting remains advisory for `python_tests`
  (`47726>46865`), total Python (`84696>84024`), and TOML (`12035>11633`).
- Formatter suppression remains a repository-wide campaign debt: current
  Python inventory contains 41 `fmt: off`, 24 `fmt: on`, and 308 `fmt: skip`
  directives. These are not accepted as semantic compression and must be
  removed or reduced to explicitly machine-generated byte fixtures.
- These findings are neither suppressed nor claimed complete. They remain
  mandatory inputs to later compression Changes and block completion of the
  overall campaign.

## Historical boundary

The following earlier claims retain their original Chronicles and become
superseded rather than being rewritten:

- `bounded-lane-retirement-20260714`
- `lane-public-primitives-20260709`
- `retire-landed-apply-verdict-20260709`

The already accepted `retire-owner-boundary` claim remains historical.

## Post-archive boundary

- Final frozen-tree correctness, governance, and ponytail reviews precede the
  official archive.
- The official archive path must be recorded from the observed OpenSpec result
  at execution time. Its logical suffix is `declarative-lifecycle-matrix`; a
  second embedded date or preasserted execution date is not admissible.
- Archive relocation, commit, official candidate-base refresh, one parity
  refresh, HEAD-bound executed proof, and local land are later transitions and
  require their own fresh evidence.
- Remote push, hosted CI, accepted-root publication, and Work Lane retirement
  are not established by this Chronicle.
