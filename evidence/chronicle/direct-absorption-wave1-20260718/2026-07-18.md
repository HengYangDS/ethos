---
subject: ethos:direct-absorption-wave1-20260718
state: active
claim: direct-absorption-wave1-20260718
---

# Direct Absorption Wave 1 — 2026-07-18

## Observation

The accepted baseline is `dev@ea054330fb7c2d4cd92158b522b92a1368b77f08` and
`candidate/dev` has the same HEAD. The owned carrier starts on that baseline.
The recovered direct-candidate cohort was re-read before replay; historical
branches remain source evidence, not merge targets.

## Semantic Decisions

- `work/ci-bootstrap-runtime-20260714@635398f9d182f6a1e8d8afa62d511728224ef50f`
  identified a missing hosted bootstrap boundary. This carrier adds the
  checkout-bound `UV_PROJECT_ENVIRONMENT` and materializes it with `uv sync`
  before owner scripts rely on `uv run --no-sync`.
- `work/git-fsmonitor-test-hermeticity-20260717@253283b5a30d697e292f76c4a6a47f8d79bfcc77`
  identified a missing test-execution boundary. This carrier appends
  `core.fsmonitor=false` to Git's process-local overlay and keeps that safety
  setting when test CLI helpers remove identity-only overlays.
- `work/hosted-security-baseline-20260714@80243ddaf89e46558af14ca0982c4b042a35526b`
  is current-equivalent: accepted `bd69fdf1681ffbc85326942c49cb568b5234b506`
  already removed dynamic checkout-hook execution and the architecture test
  proves the no-`exec` boundary.
- `work/hosted-verify-hermetic-docker-context-20260717@c5f6dc787417176f9451a1fddb08874df8e88722`
  is current-equivalent: the provider regression binds an explicit Docker
  context endpoint when `DOCKER_HOST` is absent.
- `work/coverage-raw-truth-20260715@3729902a407d4aaecc7143b6149a4a93e19ee728`
  is current-equivalent: current policy requires a hard floor of 100 with branch
  coverage, and the owner script applies it after serial and sharded execution.
- The container-contract lanes remain deferred, not absorbed. They propose an
  optional adopter-domain contract and dependent compression program; this wave
  has no current adopter admission or accepted source-budget decision for them.
- The dual-remote successor remains holder-bound and is not touched here.

## Focused Proof

The red-to-green runtime and fsmonitor set passed locally on the owned source
tree: four focused regressions passed and the two edited shell owner scripts
passed `bash -n`. The official OpenSpec carrier is archived. These remain local
focused facts only, not executed proof, accepted closeout, remote publication,
or hosted CI evidence.

## Local Closeout Boundary

The archived carrier tasks are complete: the semantic replay is committed, the
scope correction is archived, the local proof record covers the default 20-gate
promotion floor at `31588c5eedecd9e9b26c06e925bd2a0b44ac8d78`, and generic
parity is recorded separately. That local receipt establishes a closeout
precondition only; it neither proves accepted-root closeout nor retires a source
lane.

## Retirement Boundary

No source Work Lane retires in this active carrier. After commit, proof, land,
and accepted closeout, each proposed retirement re-observes the source branch,
linked worktree, current dirtiness, current lease, and accepted relation and
then uses its native lifecycle command.
