# Direct Closeout And GitHub Publication — Initiation Record

## Boundary

This record opens the owned Work Lane
`work/publication-unblock-and-convergence-20260719` from local baseline
`4ddd805872ac5645617a5b290381cfd25c68464f`.

It binds only the intended contract change: GitHub can receive an ordinary
non-force protected update after executed local proof and accepted closeout,
while long-horizon compression state remains visible as advisory progress. It
does not claim completed implementation, executed proof, candidate landing,
accepted closeout, remote publication, GitLab availability, or hosted CI.

## Observed Problem

The protected-push hook had correctly rejected an attempted non-force GitHub
update because the campaign publication projection treated an active campaign,
unretired step, terminal-budget non-attainment, and active debt as hard gaps.
The hook also failed to pass its supplied remote name to the admission reducer,
so GitHub diagnostics could incorrectly identify the default `origin` remote.

## Decision

Keep campaign declaration integrity, remote branch policy, identity, executed
proof, candidate topology, reconciliation, and fast-forward checks enforced.
Move only campaign terminal-progress facts into a typed advisory list. Preserve
terminal source-budget settlement for full proof and global compression closeout.

## Stop Conditions

Stop if a structural campaign manifest defect becomes advisory, a protected
push loses any existing proof/identity/topology/remote check, a force update is
introduced, GitLab is inferred available, or local proof is called hosted CI.
