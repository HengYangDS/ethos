---
subject: evidence:domain-campaign-closeout-package
state: active
created: 2026-07-09
claim: domain-campaign-closeout-package
---

# Domain Campaign Closeout Package Evidence — 2026-07-09

## Claim

ETHOS removed the suffix-flat `ethos.domain.campaign_closeout` module from the
active domain surface and moved campaign closeout logic into the semantic
subpackage `ethos.domain.campaign.closeout`. The CLI campaign command imports
the public `campaign_closeout_report` symbol from that concrete semantic module.
No compatibility import shell, package-root facade, or `land.py` re-export is
retained.

## Scope

- Moved `packages/ethos/src/ethos/domain/campaign_closeout.py` to
  `packages/ethos/src/ethos/domain/campaign/closeout.py`.
- Added declaration-only `packages/ethos/src/ethos/domain/campaign/__init__.py`.
- Updated `packages/ethos/src/ethos/surface/cli/campaign.py` to import the
  concrete semantic module directly.
- Removed `campaign_closeout_report` from `packages/ethos/src/ethos/domain/land.py`
  exports so `land.py` does not act as a compatibility route for campaign
  closeout.
- Shrunk `.config/checks/module-layout/policy.toml` ratchet debt to 15
  and suffix-module baseline to 14, preserving the current candidate baseline
  while removing the `campaign_closeout.py` suffix debt.

## Replay And Concurrency

This lane originally branched before another lane landed mechanism-comparison
documentation and refreshed generic parity evidence into `candidate/dev`. The
first governed `ethos lane refresh-base --apply --authorize` attempt reported
`refresh_base_failed` while applying the semantic refactor. The worktree returned
to a clean state. A backup ref was created before replay, then the lane-only
semantic patch from the previous candidate base to the campaign-closeout semantic
commit was applied on top of the current `candidate/dev`, explicitly excluding
stale `evidence/parity/generic-shadow.json` so candidate evidence was not
reversed.

## Verification Checkpoints

Implementation-time checks before the final replay:

```text
uv run --group dev pytest -q tests/unit/cli/test_contracts_proof.py::test_campaign_closeout_reports_local_campaign_packages tests/unit/domain/test_report.py::test_scorecard_next_actions_route_module_layout_gaps
# 2 passed

tools/ci/scripts/run-module-layout.sh
# ok=true; state=clean; suffix_module_count=14; debt_count=15; required_gaps=[]

tools/ci/scripts/run-python-lint.sh
# all checks passed

uv run --group dev pytest -q tests/unit/cli/test_contracts_proof.py::test_campaign_closeout_reports_local_campaign_packages tests/unit/cli/test_quality_surface_more.py tests/unit/domain/test_report.py tests/unit/governance/validation/test_gates.py tests/unit/kernel/test_quality.py
# passed

uv run --group dev pytest -q tests/architecture/test_release_assets.py::test_module_layout_gate_is_owned_by_policy_and_runner_surfaces
# passed after updating the expected ratchet baseline

uv run --package ethos ethos quality code-size --json
# ok=true; state=clean; required_gaps=[]
```

Replay checkpoint on the current candidate base:

```text
uv run --package ethos ethos lane prewrite <semantic package paths> --editor-root "$PWD" --require-editor-root --json
# ok=true; state=admitted

uv run --package ethos ethos quality module-layout --json
# ok=true; state=clean; suffix_module_count=14; debt_count=15; required_gaps=[]

uv run --package ethos ethos quality claims --json
# ok=true; state=clean; required_gaps=[]
```

Final claim validation, fresh parity shadow evidence, HEAD-bound proof, land,
and accepted-root closeout must be rerun after this claim is committed. This
record does not claim hosted CI, remote push, or remote publication.

## Boundary

This lane is a semantic module-layout debt reduction. It does not change campaign
closeout command semantics, clear remaining suffix-module debt, decompose the flat
CLI directory, or alter local-vs-remote publication semantics. Those remain
separate backlog items.
