# Equal Dual-Remote Publication Topology — 2026-07-18

## Claim Boundary

This record binds the archived OpenSpec carrier
`openspec/changes/archive/2026-07-18-dual-remote-publication-equivalence-successor-20260718` to the product-level
three-layer publication declaration: local verification/install, GitLab
organization collaboration, and GitHub public distribution. It binds typed
release-policy parsing, named-remote pre-push admission, generated provider
projections, adoption scaffold declarations, and no-push per-target publication
observations.

It does not claim a GitLab or GitHub push, hosted CI result, release artifact,
merge request, branch protection setting, or remote configuration success.

## TDD Evidence

- RED: the topology report field was absent; named remote admission was not
  accepted; GitHub CI still ran `candidate/dev`.
- GREEN: focused release, admission, provider-projection, scaffold, and publish
  tests passed: `75 passed`.
- The focused suite exercises candidate and unknown remote rejection, valid
  GitLab/GitHub topology parsing, local-only candidate CI exclusion, scaffold
  projection, and independent GitLab/GitHub no-push observations.

## Local / Remote Separation

- Local implementation and validation are governed in the owned Work Lane.
- `candidate/dev`, `work/*`, GitLab, and GitHub are distinct state planes.
- `ethos publish` remains read-only: `remote_push = not_performed` and hosted
  CI status is unclaimed even when a tracking ref is synchronized.
- Remote observation and any later GitLab/GitHub publication must be reported
  separately after local candidate closure.
