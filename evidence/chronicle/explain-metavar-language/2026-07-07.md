# Explain metavar language

ETHOS generated CLI help now presents the `ethos explain` positional input as
`GAP-OR-SIGNAL`. The implementation keeps `summary.gap` for compatibility and
continues to delegate semantics to the invalid-state taxonomy projection.

## Boundary

This evidence supports a reader projection repair only. It does not introduce a
new transition command, truth store, or blocking gap class.
