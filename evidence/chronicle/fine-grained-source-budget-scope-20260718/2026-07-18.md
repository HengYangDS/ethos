---
subject: ethos:fine-grained-source-budget-scope-20260718
state: active
claim: fine-grained-source-budget-scope-20260718
---

# Fine-Grained Source-Budget Scope — 2026-07-18

## Decision

`ethos quality source-budget --json` remains the global measurement and debt
contract. It is removed from the default promotion proof because that proof
answers whether one current Change is locally correct and governable; it does
not settle repository-wide compression debt.

## Boundary

The full proof and global compression closeout still require a clean
source-budget report. The scorecard retains every source-budget gap in a
separate advisory `global_compression` layer with a direct repair action. This
decision neither changes an allowance, baseline, terminal target, or debt cap
nor retires a Work Lane.

## Local Closeout Evidence

At `41179ba74926e6bd9e69193810e66d96bee7efb5`, the focused regressions passed
(`25 passed`), configuration lint and the quality audit passed, and strict
OpenSpec validation accepted this change. Default proof readiness reported
`ready` with 20 gates. A generic parity shadow at the same HEAD matched with
no required gaps; its tracked receipt is written before landing.

The separately invoked global source-budget check remains blocked, exactly as
intended for the global compression program: `shell:1954>1943` and
`toml:12569>12516`. This local closeout records that unresolved obligation; it
does not represent it as discharged.
