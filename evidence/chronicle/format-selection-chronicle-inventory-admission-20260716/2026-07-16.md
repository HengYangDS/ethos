# Format Selection Chronicle Inventory Admission — 2026-07-16

## Reproduced gate failure

The executed proof bound to `15e27e3b607661d2224eb0ad933e5af04bc8cb0f`
failed `unit-architecture` because the format-selection owner rejected
`evidence/chronicle/all-work-lanes-convergence-20260716/lane-inventory.json`
as JSON outside a declared carrier home. The failure is reproduced directly by
`tools/ci/scripts/run-format-selection.sh`.

## Bounded correction

The inventory is the only tracked JSON under `evidence/chronicle/`. The policy
therefore declares the exact repository-relative file, rather than allowing a
Chronicle root. This preserves the fail-closed boundary for every other future
Chronicle JSON file.

## Required evidence

The owner script, focused architecture and CLI regression tests, strict OpenSpec
lifecycle validation, generic parity refresh, and a new HEAD-bound proof must
all pass before this Change can be archived and promoted.

## Official archive

The official OpenSpec archive command archived this Change as
`2026-07-16-format-selection-chronicle-inventory-admission` and
applied the quality-spec requirement. The active claim is rebound to that
archived carrier; parity and HEAD-bound proof remain required for promotion.
