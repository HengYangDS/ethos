---
subject: ethos:github-hosted-ci-stale-lane-closeout-20260719
state: active
created: 2026-07-19
claim: github-hosted-ci-stale-lane-closeout-20260719
---

# GitHub Hosted CI Stale Work Lane Decision — 2026-07-19

## Exact Starting Facts

This decision carrier is on
`work/github-hosted-ci-stale-lane-closeout-20260719` at the accepted/base HEAD
`4ddd805872ac5645617a5b290381cfd25c68464f`.

The only target is `work/github-hosted-ci-reconciliation-20260717` at
`6d090e9c1fb0f0e6834d6f3818248923946800d4`. Its linked worktree was observed
clean, including an empty untracked inventory. Its historical claim is missing
and is not invented here. The active same-holder lease is bound as follows:

| Lease fact | Bound value |
| --- | --- |
| Holder | `agent:codex:thread:root` |
| Lease ID | `lease:11fc3869-8d38-4a86-94e5-2e2e6c68fab7` |
| Epoch | `1` |
| Expected HEAD | `6d090e9c1fb0f0e6834d6f3818248923946800d4` |

`git cherry dev work/github-hosted-ci-reconciliation-20260717` reports exactly
10 patch-inequivalent commits. Their semantic disposition is complete:

| Commit | Decision | Semantic intent |
| --- | --- | --- |
| `5febb9cf` | accepted/absorbed | release mirror main implementation |
| `8bb25bb1` | accepted/absorbed | release mirror core boundary |
| `4dcd3c26` | accepted/absorbed | mirror closeout quality-ratchet repair |
| `9e285147` | accepted/absorbed | Node 24.18.0/26.5.0 compatibility, checksums, runner, and CI matrix |
| `6ab554ad` | accepted/absorbed | land reader reuses read-only workspace status |
| `e5674df1` | accepted/absorbed | candidate dirty/proof-gate hygiene |
| `f5a1628c` | accepted/absorbed | proof freshness test/import/ratchet cleanup |
| `1b8a6c44` | obsolete | old parity receipt |
| `4ad38c17` | obsolete | old parity receipt |
| `09d58b4c` | obsolete | old lifecycle semantic-scope freshness, replaced by the exact-scope continuity successor |

Missing product behavior is **0**. The historical lane must never be merged,
rebased, cherry-picked, refreshed, or landed.

## Decision: `lane_resolution/preserve-retire`

After this carrier reaches accepted repository truth, it may support one later,
exact `lane_resolution/preserve-retire` decision for the bound target only. This
Chronicle does not itself execute a decision or apply effect, retire a ref or
worktree, transfer ownership, or mint foreign authority.

The later operator must freshly re-observe the complete target immediately
after acceptance and again before apply. Any change to target HEAD, clean/dirty
state, holder, lease ID, or epoch invalidates this decision. A changed lane
incarnation or unavailable target also blocks it. The later path still requires
break-glass, explicit irreversible confirmation, the accepted Chronicle digest,
and a non-replayable native decision receipt.

## Preservation and Recovery

Before the irreversible effect, verify an exact Git bundle, tracked patch,
explicitly empty untracked inventory and absent-or-empty archive, preservation
manifest digest, and immutable receipt. Recovery can fetch or clone the bundle,
recreate the historical branch, and apply the tracked patch. The receipt and
manifest must identify the same verified package.

## Duplicate-Apply Follow-up

On 2026-07-19, a duplicate resolution apply rewrote a package before an
immutable receipt collision stopped the operation. This record does not fix
that product behavior and does not declare the mismatched package valid. A
separate valid package protects the dirty all-lanes delta; idempotency repair is
a separate governed change.

## Evidence Boundary

This is a local-only governance decision carrier. It claims no resolution
apply, retirement, merge, rebase, cherry-pick, refresh, land, remote probe,
push, hosted execution, hosted-CI success, or publication.
