# Global Declarative Compression Foundation Evidence

Date: 2026-07-12
Work Lane: `work/global-declarative-compression-resume-20260712`
Archived OpenSpec carrier:
`openspec/changes/archive/2026-07-12-global-declarative-compression-program`.

## Claim Boundary

This Chronicle supports only the completed declaration-first compression
foundation: repository-wide measurement and explicit temporary debt, the first
typed declaration/projection compiler, restricted-CEL topology cutover, and the
candidate-train reconciliation that deletes duplicate lane-resolution behavior.
It does not claim terminal source-budget attainment, settlement of either debt
record, completion of T6--T9, remote publication, hosted CI, or an executed
proof on the final archived-carrier HEAD.

## Measured Snapshot

`ethos quality source-budget --json` on the current Work Lane reports 109,566
global effective source lines and 88,155 Python effective source lines. The
declared active-debt allowance is 4,224 effective lines, below the immutable
5,267 cap. The terminal gaps are therefore 35,827 global and 29,433 Python
effective source lines. These values make the remaining compression obligation
visible; they are not a baseline reset or a completion signal.

## Foundation Delivered

- Typed declarations compile into strict immutable projections instead of adding
  another hand-built envelope/read-model path.
- CEL remains restricted to predicates over typed facts, while `graphlib`
  remains the single graph-ordering kernel.
- Candidate-train reconciliation compressed the integrated resolution and
  adoption paths while keeping the temporary debt ledger exact.
- Receipt verification is exposed as an adapter boundary rather than retained as
  a lane-private helper.

## Verification Boundary

The completion evidence for this foundation is limited to the targeted quality,
contract, lifecycle, and source-budget checks recorded in the bound claim. A
fresh HEAD-bound full proof, local candidate landing, parity refresh, accepted
closeout, and remote publication are distinct transitions and are asserted only
after their commands succeed on their respective final HEADs.

## Archive

`openspec archive global-declarative-compression-program-20260711 --yes --json`
archived the completed foundation as
`2026-07-12-global-declarative-compression-program` and fused its three
quality requirements into the canonical specification. Later program waves must
open their own bounded carriers rather than revive this archived record.
