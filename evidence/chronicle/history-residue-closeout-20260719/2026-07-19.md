# History Residue Closeout Chronicle — 2026-07-19

## Status

Active historical authoring record for the successor-v2 carrier. This Chronicle
records bounded tracked work and explicit non-effects. It does not by itself
claim OpenSpec archival, candidate land, accepted-root closeout, local
publication readiness, hosted execution, remote push, or remote completion.

## Bound Carriers

- OpenSpec change: `history-residue-closeout-20260719`
- Current carrier Work Lane: `work/history-residue-closeout-successor-v2-20260719`
- Current recorded authoring HEAD: `b7417bca291e2232765722b539e1ac32a9b9ffce`
- Predecessor observation Work Lane: `work/history-residue-closeout-successor-20260719`
- Predecessor observation HEAD: `a33a93e433bd9944bdb64133901421208923e4af`
- Claim: `evidence/claims/history-residue-closeout-20260719.toml`
- Freshness: historical; this record is not HEAD-bound proof.

## Scope Correction

Earlier revisions combined tracked cleanup, predecessor test observations,
source-budget terminal settlement, and inferred local-state effects. The
successor-v2 carrier separates them:

- tracked source, tests, configuration, docs, OpenSpec, and claims are in scope;
- local-state capabilities are tested only in temporary or copied state;
- real SQLite, lease, proof, and recovery effects are not in scope;
- source-budget is recorded as campaign-terminal observation, not settlement;
- remote and hosted state require separate observation.

## Current Authoring Observations

The continuation recorded these local commands during authoring:

- Admission API and closeout-focused Ruff checks passed, followed by 43 focused
  tests covering `HookAdmissionRequest` and related admission callers.
- Rules migration Ruff checks passed, followed by 47 focused rules, CLI, and
  planning tests. The new public regression proves that a valid TOML document
  containing a table-looking line inside a multiline profile value returns a
  structured migration error instead of leaking `StopIteration` or rewriting the
  source file.
- `ethos quality source-budget --json` at `b7417bca291e2232765722b539e1ac32a9b9ffce`
  returned `required_gaps=[]`, `data.state=clean`,
  `enforcement=campaign_terminal`, one unexpired debt record, seven campaign
  growth advisories, and `terminal_target_met=false`.
- The measured metrics were `python_product=40502`, `python_tests=55093`,
  `python_tools=988`, `python_other=444`, `shell=1832`, `js=90`, `toml=13226`,
  `yaml=799`, `json=305`, `ini=70`, `jinja=670`, `diagram=23`,
  `python_total=97027`, and `global_total=114042`.
- No Python source contains `SCHEMA_VERSION as SCHEMA_VERSION` or
  `SCHEMA_VERSION as _SCHEMA_VERSION`; the retired `ethos/events.py` path remains
  absent.

These focused observations are not a substitute for the still-pending full
suite, final parity, or HEAD-bound executed proof.

## Claim Preservation And Retirement

The predecessor patch deleted 29 claim files without per-claim supersession or
retirement evidence. Successor-v2 restored all 29. This change narrows the active
`assurance-program-20260712` claim to the provider-neutral receipt/admission
contract and archives the `generic-pre-receive-contract-20260712` and
`physical-topology-reference-adapters-20260712` claims because their bundled
provider executables are intentionally absent.

## Local-State Maintenance Boundary

This continuation did not invoke a real maintenance apply. It did not migrate a
real state database, create the real operator recovery archive, delete any lease
or proof record, remove recovery source material, move a ref, or remove a
worktree. Fixture, temporary-repository, copied-state, and replay tests establish
capability behavior only.

OpenSpec validation, archive, candidate land, accepted-root closeout, or publish
readiness must not be interpreted as evidence that any of those local-state
effects occurred.

## Remaining Lifecycle Boundaries

- The Claim remains `active` with `freshness.mode=historical`.
- The OpenSpec carrier remains under `openspec/changes/` until final validation
  and archive.
- The Work Lane is stale behind the current candidate and must use official
  `lane refresh-base`; no hand rebase or merge is authorized.
- Generic parity must be regenerated only after the final candidate refresh.
- Full quality, the complete test suite, HEAD-bound proof, candidate land,
  accepted-root closeout, local publish readiness, remote publication, hosted
  CI observation, and official lane retirement remain separate later receipts.
