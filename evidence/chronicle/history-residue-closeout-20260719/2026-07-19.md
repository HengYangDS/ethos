# History Residue Closeout Chronicle — 2026-07-19

## Status

Archived-carrier historical record for the successor-v3 line. This Chronicle
records bounded tracked work, pre-archive validation, the official OpenSpec
archive receipt, and explicit non-effects. It does not by itself claim
archive-HEAD proof, candidate land, accepted-root closeout, local publication
readiness, hosted execution, remote push, remote completion, or Work Lane
retirement.

## Bound Carriers

- OpenSpec archive:
  `openspec/changes/archive/2026-07-19-history-residue-closeout-20260719`
- Current carrier Work Lane: `work/history-residue-closeout-successor-v3-20260719`
- Successor-v3 candidate/accepted base at lane start:
  `f05bdacd84c0e7ed23daa75079e74eef1725eb99`
- Successor-v3 archive transition input HEAD:
  `da954bf41677792c9a855b38254a6ef09efe3e24`
- Predecessor successor-v2 Work Lane:
  `work/history-residue-closeout-successor-v2-20260719`
- Predecessor successor-v2 archive-HEAD proof HEAD:
  `db81c4df5737e3e17b31638bb1515061acec78b2`
- Claim: `evidence/claims/history-residue-closeout-20260719.toml`
- Freshness: historical; this record is not HEAD-bound proof.

## Scope Correction

Earlier revisions combined tracked cleanup, predecessor test observations,
source-budget terminal settlement, and inferred local-state effects. The
successor-v3 carrier separates them:

- tracked source, tests, configuration, docs, OpenSpec, and claims are in scope;
- local-state capabilities are tested only in temporary or copied state;
- real SQLite, lease, proof, and recovery effects are not in scope;
- source-budget is recorded as campaign-terminal observation, not settlement;
- remote and hosted state require separate observation.

## Current Authoring Observations

The continuation recorded these local commands during authoring:

- Admission API and closeout-focused Ruff checks passed, followed by 43 focused
  tests covering `HookAdmissionRequest` and related admission callers.
- Rules migration Ruff checks passed, followed by 47 focused rules, CLI, and
  planning tests. The public regression proves that a valid TOML document with
  a table-looking line inside a multiline profile value returns a structured
  migration error instead of leaking `StopIteration` or rewriting the source.
- The predecessor successor-v2 line used two official base refreshes. After
  its archive-HEAD proof passed on `db81c4df`, candidate, accepted, and main
  advanced to `f05bdacd` during that proof. A third official refresh failed
  closed on a real canonical-spec conflict, returned `refresh_base_failed`,
  restored the clean predecessor HEAD, and did not leave a rebase in progress.
- Successor-v3 was then started through official `ethos lane start` from
  `f05bdacd` with the same claim and holder. Fifteen pre-archive semantic commits
  were replayed after a 179-path prewrite; the already-upstream quality patch
  and stale archive/parity commits were not replayed. The OpenSpec archive was
  rerun on the current base instead of hand-merging canonical specs.
- Predecessor generic parity was regenerated on `3fb50b53`, committed as the
  evidence-only child `06d1d96a`, and reported `gap_count=0` with no false
  negatives. Successor-v3 parity remains a separate post-archive transition.
- The complete Python suite at `06d1d96a` passed `2477` tests with statement and
  branch coverage both at `100.00%`. The local-state audit, quality audit, and
  skill-portfolio audit all returned `ok=true` with no required gaps.
- The successor-v3 archive-tree recheck reported `195` claims and zero gaps.
  Archive query resolved the dated carrier, and strict OpenSpec validation
  passed all `9/9` canonical specs.
- `ethos quality source-budget --json` returned `required_gaps=[]`,
  `state=advisory`, one active unexpired debt record, seven campaign growth
  advisories, and `terminal_target_met=false`.
- The successor-v3 archive-tree metrics were `python_product=41389`,
  `python_tests=55844`, `python_tools=988`, `python_other=444`, `shell=1832`,
  `js=90`, `toml=14222`, `yaml=797`, `json=305`, `ini=70`, `jinja=670`,
  `diagram=23`, `python_total=98665`, and `global_total=116674`.
- No Python source contains `SCHEMA_VERSION as SCHEMA_VERSION` or
  `SCHEMA_VERSION as _SCHEMA_VERSION`; the retired
  `packages/ethos/src/ethos/adapters/store/state/events.py` path remains absent.

The predecessor full pre-archive executed proof ran on `d0695c3b`, evaluated
`28` gates, returned `state=proven` with no required gaps, and emitted evidence
digest `596ff3b6e474715df18bb7f12ee19d8c64c07a272df29865aeb26dcb169d70a4`.
Its later archive-HEAD proof ran on `db81c4df`, passed the same `28` gates, and
emitted digest `69b356fb8ac1cbd4b0d5360f292f620e25024d1d2c3430236554519098a461f2`.
Both are predecessor evidence, not successor-v3 HEAD-bound proof.

The official OpenSpec archive returned
`archivedAs=2026-07-19-history-residue-closeout-20260719`,
`specsUpdated=true`, and requirement totals `added=10`, `modified=3`,
`removed=3`, `renamed=0`. Canonical fusion changed only the five declared
specs. The only removed requirements were `Structural Blank-Line Contract`,
`Structural blank-line contract`, and
`Reference adapter stays provider-local and constrained`.

## Claim Preservation And Retirement

The predecessor patch deleted 29 claim files without per-claim supersession or
retirement evidence. Successor-v3 preserves the restoration of all 29. This
change narrows the active
`assurance-program-20260712` claim to the provider-neutral receipt/admission
contract and archives the `generic-pre-receive-contract-20260712` and
`physical-topology-reference-adapters-20260712` claims because their bundled
provider executables are intentionally absent.

## Local-State Maintenance Boundary

This continuation did not invoke a real maintenance apply. It did not migrate a
real state database, create the real operator recovery archive, delete any lease
or proof record, remove recovery source material, move a ref, or remove a
worktree. Fixture, temporary-repository, copied-state, and replay tests establish
capability behavior only.

OpenSpec validation, archive, candidate land, accepted-root closeout, or publish
readiness must not be interpreted as evidence that any of those local-state
effects occurred.

## Remaining Lifecycle Boundaries

- The Claim remains `active` with `freshness.mode=historical`.
- The OpenSpec carrier is archived at the dated path recorded above; the active
  change path is absent.
- The Work Lane was started from candidate/accepted `f05bdacd` through the
  official lane-start path; any later candidate advancement requires another
  owner-bound refresh or a new fail-closed successor decision.
- Predecessor full quality, the complete test suite, claims, generic parity,
  and proof are recorded above only as bounded inputs. Successor-v3 strict
  OpenSpec and canonical fusion have been rechecked; its full quality, parity,
  and proof remain separate next transitions.
- The next transition is archive-HEAD parity and full proof; this record does
  not pre-claim their outcome.
- Archive-HEAD proof, candidate land, accepted-root closeout, local publish
  readiness, remote publication, hosted CI observation, and official Work Lane
  retirement remain separate later receipts.
