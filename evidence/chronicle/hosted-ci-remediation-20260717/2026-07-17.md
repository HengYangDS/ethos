---
subject: quality:hosted-ci-remediation
role: evidence
state: active
relations:
  supports: evidence/claims/hosted-ci-remediation-20260717.toml
---

# Hosted CI Remediation Chronicle

## Observation

At `82cf943a`, GitLab pipeline 2866 failed because a GitHub-emulator fixture
reached unavailable host Docker. Pipeline 2867 disconnected before `pip-audit`
produced JSON. Neither failure was a vulnerability verdict.

## Decision

The fixture now stubs its Docker-context helper. The existing audit owner script
retries once only for explicit transport disconnects. Vulnerabilities, malformed
JSON, and unclassified errors remain final failures. Temporary diagnostics and
failed raw output are removed.

## Evidence

- The focused provider and dependency architecture suites passed: 27 tests.
- The retry regression verifies transient success plus one-attempt vulnerability
  and non-transient failures.
- Strict OpenSpec and lifecycle validation passed before implementation proof.

## Boundary

This is local evidence only: no hosted success, remote publication, historical
pipeline repair, OSV coverage, or foreign-lane authority is claimed.
