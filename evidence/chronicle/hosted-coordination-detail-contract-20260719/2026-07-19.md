# Hosted Coordination Detail Contract — 2026-07-19

## Boundary

This carrier corrects a product test that treated `deferred` as the only valid
coordination detail state. It does not assert hosted success, alter external
provider behavior, or grant authority over any foreign Work Lane.

## Evidence

- `tests/unit/product/test_orient.py` validates summary/payload alignment and
  the nullability contract for observed coordination detail.
- An isolated repository test establishes that an `exact` detail observation
  with no foreign Work Lanes is valid.
- The OpenSpec delta retains every existing scenario in the modified
  `Work Lane Coordination Read Model` requirement and adds the observed-state
  projection scenario.

## Acceptance Scope

Local proof and subsequent hosted validation must bind a committed accepted
HEAD. GitLab availability is outside this carrier.
