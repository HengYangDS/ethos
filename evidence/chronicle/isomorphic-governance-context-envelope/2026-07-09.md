---
subject: ethos:isomorphic-governance-context-envelope
role: evidence
state: active
relations:
  supports: evidence/claims/isomorphic-governance-context-envelope.toml
---

# Isomorphic Governance Context Envelope Evidence

Date: 2026-07-09

## Scope

This evidence supports the claim that ETHOS primary command JSON results expose a
shared governed-repository context at the result envelope while preserving native
command data contracts.

Changed surfaces:

- `packages/ethos-core/src/ethos_core/result.py`
- `system/schemas/kernel/result.schema.json`
- `packages/ethos/src/ethos/repository/context.py`
- `packages/ethos/src/ethos/surface/cli/root/inspection.py`
- `packages/ethos/src/ethos/surface/cli/root/planning.py`
- `packages/ethos/src/ethos/surface/cli/root/proof.py`
- `packages/ethos/src/ethos/surface/cli/root/lifecycle.py`
- `tests/unit/cli/test_contracts.py`
- `tests/unit/governance/validation/test_schemas.py`
- `docs/reference/command-plane.md`
- `openspec/changes/archive/2026-07-09-isomorphic-governance-context-20260709/`

## Proof So Far

Focused regression after implementation:

```text
uv run pytest \
  tests/unit/cli/test_contracts.py::test_primary_commands_expose_top_level_governance_context \
  tests/unit/cli/test_contracts.py::test_primary_commands_use_same_context_for_adopted_repository \
  tests/unit/governance/validation/test_schemas.py::test_result_payload_accepts_governed_repository_context \
  -q
# 3 passed
```

Live command smoke in the work lane showed top-level `governance_context` for
`status`, `plan`, `prove`, `land`, `publish`, `orient`, and `report`; `status.data`
remained free of `governance_context`.

## Boundary

This evidence does not claim remote publication, hosted CI success, or semantic
correctness beyond the tested command-envelope contract. It also does not change
mutation authority, branch roles, Work Lane ownership, or provider adapters.

## OpenSpec Archive

The OpenSpec carrier was archived as
`openspec/changes/archive/2026-07-09-isomorphic-governance-context-20260709`.
The accepted repository-governance spec was manually fused after archive so the
new primary-command envelope scenario did not delete the pre-existing audit,
proof, report, and first-glance documentation obligations.

## Final Local Proof Before Commit

The following gates passed locally after OpenSpec archive and spec-fusion repair:

```text
uv run pytest tests/unit/cli/test_contracts.py tests/unit/cli/test_contracts_proof.py tests/unit/cli/test_contracts_land.py tests/unit/governance/validation/test_schemas.py tests/architecture/test_product_design_contract.py -q
# 129 passed

tools/ci/scripts/run-python-lint.sh
# passed: Ruff check, Ruff format check, and Ruff ratchet

tools/ci/scripts/run-config-lint.sh
# passed

tools/ci/scripts/run-markdown-lint.sh
# passed: 0 markdown errors

uv run --package ethos ethos quality claims --json
# ok=true, state=clean, required_gaps=[]

uv run --package ethos ethos quality schemas --json
# ok=true, state=clean, required_gaps=[]

uv run --package ethos ethos openspec --lifecycle --json
# ok=true, state=clean, required_gaps=[]

uv run --package ethos ethos report --json
# ok=true, state=ready, score=16/16, governance_gap_count=0, parity_pending_count=0

uv run --package ethos ethos prove --execute --expect-head $(git rev-parse HEAD) --json
# ok=true, state=proven, gate_count=19

tools/ci/scripts/run-python-tests.sh
# 1394 passed, coverage 100.00%
```

The head-bound proof above is valid for the pre-commit lane HEAD. A final
head-bound proof is required after committing this evidence and claim digest.

## Post-Commit Proof And Closeout Binding

After committing the envelope implementation and generic parity refresh, the
lane HEAD was:

```text
1a2312a6a39cf5eb38f5204728c0fea9ab2d8945
```

The following local, HEAD-bound proof passed at that HEAD:

```text
uv run --package ethos ethos prove --execute --expect-head 1a2312a6a39cf5eb38f5204728c0fea9ab2d8945 --json
# ok=true, state=proven, gate_count=19
```

The final dry-run land check after that proof was blocked only by this archived
OpenSpec task list still containing unchecked closeout tasks:

```text
uv run --package ethos ethos land --json
# ok=false, state=blocked
# required_gaps=[
#   "openspec_archive_tasks_incomplete:2026-07-09-isomorphic-governance-context-20260709"
# ]
```

This follow-up marks those archived closeout tasks complete and rebinds the
claim digest to this updated chronicle. A fresh head-bound proof is still
required after the follow-up commit before candidate landing.
