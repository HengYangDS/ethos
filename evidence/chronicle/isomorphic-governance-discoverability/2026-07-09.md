---
subject: ethos:isomorphic-governance-discoverability
state: locally-proven
created: 2026-07-09
claim: isomorphic-governance-discoverability
head: pending-final-commit
---

# Isomorphic Governance Discoverability Evidence

## Claim

ETHOS makes its self/adopter governance isomorphism visible in first-glance
reader surfaces without creating a new command plane or product-cloning model.

## Net gain

- Humans and agents can discover the governing shape from the README, Product
  Design Contract, and Glossary before mutating.
- The wording preserves one kernel with profile and adapter boundaries.
- The architecture test prevents later docs from hiding or weakening the
  distinction between isomorphic governance and product cloning.

## Checks

Initial focused checks in this Work Lane:

- TDD red: `tests/architecture/test_product_design_contract.py::test_first_glance_docs_make_isomorphic_governance_discoverable` failed before docs were updated.
- TDD green: the same test passed after the README, Product Design Contract, and Glossary were updated.
- Focused regression: `uv run --group dev pytest -q tests/architecture/test_product_design_contract.py tests/unit/governance/test_profiles.py tests/unit/kernel/test_contracts.py` returned `42 passed`.
- Docs gate: `uv run --package ethos ethos quality docs --json` returned `ok=true`.
- Markdown gate: `tools/ci/scripts/run-markdown-lint.sh` returned `0 error(s)`.
- Ruff focused gate: `uv run --group dev ruff check tests/architecture/test_product_design_contract.py` and `ruff format --check` passed.

- OpenSpec lifecycle: `uv run --package ethos ethos openspec --lifecycle --json` returned `ok=true`, `state=clean`, and no required gaps for `isomorphic-governance-discoverability`.
- Claims gate: `uv run --package ethos ethos quality claims --json` returned `ok=true`, `state=clean`.
- Evidence freshness gate: `uv run --package ethos ethos quality evidence-freshness --json` returned `ok=true`, `state=clean`.
- Generated artifact topology: `uv run --package ethos ethos quality generated-artifacts --json` returned `ok=true`, `state=clean`.
- Command surface: `uv run --package ethos ethos quality command-surface --json` returned `ok=true`, `state=clean`.

## Archive closeout

The official OpenSpec archive command moved the active carrier to
`openspec/changes/archive/2026-07-08-isomorphic-governance-discoverability`
and promoted the repository-governance spec update into
`openspec/specs/repository-governance/spec.md`. The active claim now points to
the archived carrier and promoted live spec rather than the removed active
change path.

Post-archive checks to run before landing:

- `uv run --package ethos ethos quality claims --json`
- `uv run --package ethos ethos openspec --lifecycle --json`
- `uv run --package ethos ethos quality evidence-freshness --json`
- `uv run --package ethos ethos parity gaps --adopter generic --json`
- `uv run --package ethos ethos report --json`
- `tools/ci/scripts/run-python-tests.sh`
- `uv run --package ethos ethos prove --execute --expect-head <head> --json`

## Boundaries

This evidence is local to the Work Lane. It does not claim remote publication,
hosted CI, adopter repository mutation, or full objective completion.
