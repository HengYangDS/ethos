# Chronicle: live Work Lane exceptional-resolution authorization

event: lane_resolution/campaign

The user explicitly authorized completion of the previously identified live
Work Lane reconciliation campaign in this session. That authorization is
bounded to the three lanes named below. It is not inferred from a provider,
model, or actor label, and it does not extend to subsequently observed lanes.

This record was prepared from the accepted root at
`98af8256cba83569cae4635e0570936ddc46387e` on 2026-07-11. Each effect must
nevertheless be preceded by a fresh observation; a changed observation
invalidates its decision and requires a new decision artifact.

## Authorized local resolutions

All three dispositions are `lane_resolution/preserve-retire`. Preservation is
chosen even for a clean lane with unlanded commits: the recovery bundle keeps a
reviewable, importable copy before the exact linked worktree and ref are
removed. The package must contain and verify `repository.bundle`,
`tracked.patch`, any `untracked.tar`, and `manifest.json` before retirement.

| Lane | Concrete holder at observation | Observed state | Authorized disposition |
| --- | --- | --- | --- |
| `work/cel-policy-pilot-20260711` | `agent:codex:thread:current` | ancestor of accepted truth; dirty, landed worktree delta | `lane_resolution/preserve-retire` |
| `work/global-declarative-compression-20260711` | `agent:codex:thread:019f477e-7aaf-7fd0-837b-41606d7a8b5f` | clean, diverged from accepted truth; three unlanded commits | `lane_resolution/preserve-retire` |
| `work/performance-evidence-20260711` | `agent:codex:thread:tong` | clean, descendant of accepted truth; one unlanded commit | `lane_resolution/preserve-retire` |

The user authorization is an exceptional maintainer decision, not a claim that
the listed holders are the same principal or that any provider label can prove
ownership. It does not make the decision reusable for a new lane incarnation.

## Explicit exclusion

`work/lease-normalize-fix` was newly observed during this campaign with the
concrete holder `human:yang:workstation:yang-heng` and a dirty worktree. It is
outside this chronicle's scope. It remains protected: no preserve, retire,
cleanup, or authority claim is authorized by this record.

## Boundary and reconciliation

Each authorized action is local-only and requires an accepted Chronicle,
break-glass decision, fresh recomputation, and irreversible confirmation. Its
machine decision and receipt remain under `build/artifacts/lane-resolution/`;
its retained preservation package is later indexed by a tracked reconciliation
Chronicle. Remote GitLab, hosted CI, and publication are all deferred. This
record neither probes nor publishes to a remote.
