# Local CI Git Bundle Locale Robustness — Initial Record

## Boundary

This record opens a test-only repair on accepted HEAD
`87911a89faeb01d97a29afce1c24e0fc5ed94f2a`. It does not claim passing local
CI, executed proof, archive, candidate landing, accepted-root closeout, remote
publication, hosted CI, or foreign-lane authority.

## RED Observation

The full local CI fallback completed every earlier owner gate and reached
`2231 passed, 1 failed`. The only failure was
`test_cross_host_export_is_content_addressed_and_excludes_sqlite_lease`.
`git bundle verify` exited successfully and reported a complete history in
Chinese, while the test required the English phrase `complete history`.
Coverage still measured line and branch coverage at `100.00%`.

## Decision

Bind only the affected external Git assertion to `LANG=C` and `LC_ALL=C`.
Preserve product behavior, bundle bytes, shared helpers, the complete-history
assertion, and the no-remote boundary.

## Focused Green Observation

The same focused test passed under `LC_ALL=zh_CN.UTF-8` after the single
assertion invoked Git with `LANG=C` and `LC_ALL=C`. Python lint also passed with
all ratchets unchanged. Complete local CI, parity, proof, archive, and landing
remain unclaimed.

## Carrier Correction

Repository authority rejected `docs/superpowers` as a forbidden product
projection. The generic method-pack design and plan files were removed; the
OpenSpec proposal, design, tasks, scope, and quality delta remain the
authoritative change carrier. This correction does not widen product or remote
publication scope.

## Complete Local CI Observation

After refreshing generic parity, `tools/ci/scripts/run-local-ci.sh` passed on
HEAD `4559d7d243844a81914f02d326673561effe92ff`: 2232 tests passed, line and
branch coverage remained 100.00%, and fallback evidence explicitly claimed
neither hosted CI nor remote publication. This is historical evidence for that
exact stable HEAD; later lifecycle commits require fresh HEAD-bound proof.

## Executed Proof Observation

Generic parity was clean and `ethos prove --execute --expect-head` returned
`state=proven` on HEAD `54e61b5374c695d6045a9ac49968220a2e85acb7` with 20
trust-bearing gates and evidence digest
`a84a96cbce2ff524adc94018739629260ac17c97ebebe1a8e44a1e135ec8977d`.
Archive and candidate/accepted closeout remain separate subsequent transitions.

## OpenSpec Archive Observation

The official OpenSpec CLI archived the completed repository change as
`openspec/changes/archive/2026-07-19-local-ci-locale-robustness-20260719` and
fused one added quality requirement into the canonical quality specification.
This archive observation does not itself claim candidate landing, accepted-root
closeout, Work Lane retirement, hosted CI, or remote publication.
