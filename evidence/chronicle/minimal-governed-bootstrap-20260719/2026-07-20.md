---
subject: ethos:minimal-governed-bootstrap
state: active
created: 2026-07-20
claim: minimal-governed-bootstrap-20260719
---

# Minimal governed bootstrap — 2026-07-20

## Observation

Default adoption copied a 66-file governance skeleton even though runtime
adopter recognition is bound by `.ethos/profile.toml`. The extra templates,
manifest, profile variants, overlay classifications, provider projections, and
byte snapshots duplicated optional capability ownership.

## Decision

Adoption owns one strict frozen Pydantic declaration and writes only its TOML
binding carrier. Optional OpenSpec, documentation, skill, release, and hosted
provider surfaces remain explicit operations of their semantic owners. This
change removes the old scaffold, Jinja2, profile selection, overlay, `init`,
aliases, and migration paths rather than retaining them in parallel.

## Evidence

The final pre-commit index for this archived delta adds 2,827 and deletes 6,689
physical lines, a net reduction of 3,862 lines: packages -2,208, tests -967,
tools -119, configuration/system -234, docs/README/rules -397, governance
records +383, and root/hosted/lock/agent surfaces -320. The archived OpenSpec
delta, exact one-file adoption behavior, strict profile contract, and
repository-wide reference cleanup bind the scope. The Chronicle digest binds
this archive carrier only.
Executed HEAD-bound proof, candidate land, accepted-root closeout, Work Lane
retirement, remote publication, remote equality, and hosted CI are excluded;
each requires a separate later receipt.

## Boundary

This record binds only the archived minimal-bootstrap replacement. It does not
claim terminal campaign compression or any excluded post-archive transition.
