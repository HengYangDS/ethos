---
subject: ethos:openspec-archive-logical-identifier
role: chronicle
state: active
---

# OpenSpec archive logical-identifier repair

## Boundary

This change repairs an ETHOS OpenSpec reader boundary: the logical Change ID
`adopter-openspec-lifecycle-continuity-20260719` is distinct from the immutable
archive directory `2026-07-19-adopter-openspec-lifecycle-continuity-20260719`.
It does not alter the archive, any host-private state, or a vendor runtime.

## Initial observation

- The dated archive carrier exists under `openspec/changes/archive/`.
- The active selector currently has no explicit archive-directory category.
- The planned reader must fail closed rather than infer a date, rename history,
  or treat archive state as active lifecycle state.

## Implemented reader boundary

- Owned Work Lane: `work/openspec-archive-logical-identifier-20260719`.
- The lane lease is bound to this claim; tracked-write prewrite admitted the
  reader, CLI, docs, test, claim, Chronicle, and OpenSpec carrier paths.
- Focused regression passed: logical archive resolution, archive-directory
  rejection in the active selector, and archive-query routing without active
  status lookup.
- Focused Ruff check and format check passed.
- `ethos openspec --lifecycle --json` passed and its disposable official archive
  preflight accepted the active Change.

## Official archive

The official OpenSpec archive operation moved the completed carrier to
`openspec/changes/archive/2026-07-19-openspec-archive-logical-identifier-20260719`.
The archived carrier is immutable; the reader resolves it only through the
logical Change ID and does not reclassify it as an active Change.

## Remaining transition boundary

The Work Lane must next replay onto the current candidate, regenerate any
required parity evidence, execute HEAD-bound proof, and use governed land and
closeout. None of those later transitions is inferred by this reader claim.
