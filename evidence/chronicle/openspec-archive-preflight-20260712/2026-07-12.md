---
subject: ethos:evidence:openspec-archive-preflight-20260712
role: evidence
state: active
relations:
  supports: openspec-archive-preflight-20260712
---

# OpenSpec Archive-Preflight Construction Record

Date: 2026-07-12
Recovery Work Lane: `work/openspec-archive-preflight-recovery-20260712`
OpenSpec carrier: `openspec/changes/openspec-archive-preflight-20260712`

## Boundary

This record binds the active carrier to one mechanism: lifecycle review runs
the configured official `openspec archive <change> --yes --json` only against a
disposable copy of `openspec/`. It projects the official result into a
change-scoped required gap without copying OpenSpec's delta parser, rewriting a
delta operation, or mutating the source workspace.

The projection is consumed by changed-scope planning, proof readiness, land,
and accepted-root closeout. The claim is limited to this admission boundary; it
does not assert completed proof, archive, candidate landing, accepted-root
closeout, remote publication, or a durable result after later source changes.

## Construction Evidence

- A regression reproduces an `ADDED Requirement` collision that strict
  validation accepts but the official archive rejects.
- Focused tests cover successful isolated simulation, source immutability,
  diagnostic path redaction, deterministic malformed-JSON handling, copy and
  invocation failure boundaries, and all-active-change lifecycle review.
- The final verification bundle, parity evidence, archive receipt, and
  HEAD-bound proof are intentionally deferred until this active carrier and its
  claim have stabilized on one lane head.

## Recovery Lineage

The original Work Lane was preserve-retired, not landed, by the maintainer's
local break-glass decision `lane-decision:9b731a99-3cca-4f64-9411-99b9ffc183a8`.
Before retirement, its exact commit graph and dirty tracked delta were retained
under `build/artifacts/lane-resolution/` with verified bundle digest
`31bfe7d664ac6f8230c7177ff8210ae7bf4f62f53227173fa293cfdf1dd306c2` and
patch digest `c7b856d3f8f21748fffb122f85e7567f64e6e3d56bd34aba7f46cc23e9b02ded`.

Under the subsequent instruction to continue, this fresh owned lane verified
both digests, replayed the preserved committed graph onto the current candidate
base, and applied the retained tracked patch without touching a foreign lane or
accepted root. The preservation package remains retained; this resumption does
not clear it, retroactively alter the retired lane, or authorize remote
publication.

## Remote Boundary

Remote GitLab publication is outside this record and remains deferred while the
remote is unavailable. Local proof, candidate integration, accepted-root
closeout, and remote publication are separate transitions.

## Executed Verification

At `0cb50c4f2efc277ec084b955ab932b3c9ab88181`, the owned recovery lane completed
the verification bundle without mutating the source OpenSpec workspace:

- `tools/ci/scripts/run-python-lint.sh` passed.
- `tools/ci/scripts/run-ethos-lane.sh quality types --json` returned `clean`.
- Official `openspec validate --all --strict --json` passed all 10 items.
- `tools/ci/scripts/run-ethos-lane.sh openspec --lifecycle --json` returned
  `clean`; changed-scope planning returned `planned`.
- `tools/ci/scripts/run-python-tests.sh` passed 2,070 tests with 100% coverage.
- `tools/ci/scripts/run-ethos-lane.sh parity shadow --adopter generic --target .
  --execute --write-evidence --json` returned `matched`.
- `tools/ci/scripts/run-ethos-lane.sh prove --execute --expect-head
  0cb50c4f2efc277ec084b955ab932b3c9ab88181 --json` returned `proven` with
  no required gaps.

This is local, HEAD-bound execution evidence for the pre-archive carrier. It
does not archive the carrier, land it, close out the accepted root, or alter
the deferred remote-publication boundary.

## Archive Receipt

The official OpenSpec archive command completed after the pre-archive proof:

- Command: `openspec archive openspec-archive-preflight-20260712 --yes --json`.
- Archive carrier: `openspec/changes/archive/2026-07-12-openspec-archive-preflight-20260712`.
- Receipt: `specsUpdated=true`; one `adapters` specification was modified, with
  no additions, removals, or renames.

This receipt was produced by the authoritative OpenSpec CLI. The retained
carrier is now archival history; candidate integration, accepted-root closeout,
Work Lane retirement, and deferred remote publication remain separate actions.

## Archive Task Grammar

The archive-closeout gate correctly rejected the archived carrier while task 3.3
combined carrier completion with candidate landing, accepted-root closeout, and
Work Lane retirement. Those transitions require a valid archive first, whereas
the archive audit correctly rejects unchecked archived tasks.

The carrier task now ends at the work it can truthfully complete before archive:
final evidence and the official archive receipt. Candidate integration,
accepted-root closeout, retirement, and remote publication are explicit
post-archive lifecycle transitions. `openspec/changes/README.md` records that
completion grammar for every future carrier; it prevents both a false checked
claim and an archive-to-land circular block.
