# Ownerless closeout effect-fence Chronicle

date: 2026-07-22
event: lane_resolution/ownerless_effect_fence
change: ownerless-closeout-effect-fence
work_lane: work/ownerless-closeout-effect-fence
initial_accepted_base: f7bcf7f4bc8fe0224da7738b0975f0a174d8edcb
historical_provider_revision: 5137759db0084d158f846169730791afb1079e58
historical_provider_source_sha256: 18b8f92346c437365505a24dbb221d5dc6d8ef87a79875f115b95e36ea356f0c
historical_provider_host_sha256: 18b8f92346c437365505a24dbb221d5dc6d8ef87a79875f115b95e36ea356f0c

## Observation

The predecessor `work/ownerless-lane-first-closeout@80a2c7a` is foreign,
dirty, leased to another holder, and diverged from accepted `dev`. It is
observe-only. Its before-effect external-verifier intent is useful, but its
response validation, effect fencing, accepted-ref CAS, receipt binding, and partial recovery are
insufficient for landing.

## Retired external-verifier contract baseline

The retired external verifier
`main@5137759db0084d158f846169730791afb1079e58` is the consumed read-only
admission contract. The tracked source and installed host adapter have identical
SHA-256 bytes at
`18b8f92346c437365505a24dbb221d5dc6d8ef87a79875f115b95e36ea356f0c`.
That equality proves adapter-file parity only, not publication or effect safety.
The ownerless verifier suite contains 26 tests and the CLI suite contains 4
tests; both suites passed against this baseline. Commit `59ee782` is not an ancestor of
the verifier's main branch and is superseded by the stronger canonical
adapter; it is not a cherry-pick source, and the retired `src/extensions` layout is not restored.

## Decision

Create a current-base successor. The retired external verifier remains
read-only. ETHOS must validate all returned bindings, acquire a target-scoped local fence that
competes atomically with the lease writer, re-observe after fencing, execute a
clean no-force accepted-ref-bound Git CAS, verify postconditions, and persist a
fully bound receipt. Partial transitions retain visible reservations for
same-decision reconciliation.

## Safety closure

- Decision bytes are read into one strictly parsed canonical snapshot. The
  snapshot must equal the initially admitted decision, and external-verifier,
  fence, reservation, receipt, and recovery bindings derive from those same bytes.
- A durable exact receipt after a crash authorizes cleanup only. Retry does not
  rerun the retired external verifier, repeat Git/worktree effects, or rewrite
  the receipt; it re-verifies the decision and all effect postconditions first.
- Receipt-first cleanup can converge when reservation unlink completed before a
  crash. The exact receipt supplies only the cleanup binding and cannot mint new
  effect authority.
- A non-zero worktree-remove result is re-observed across target ref,
  registration, and path. Only a fully unchanged target remains
  `reserved_no_effect`; partial or unknown effects stay visible.
- Target-ref and closeout-fence reads are three-state. Only explicit ref absence
  passes effect postconditions. Pre-effect requires the exact present fence;
  recovery accepts an explicitly absent fence only with the exact immutable
  receipt. Unverifiable state fails closed.
- Cleanup releases the exact fence before deleting the visible reservation. An
  exact receipt may converge an already-released fence and already-absent
  reservation; a different fence, mismatched receipt, or drifted postcondition
  blocks.

## Final security-review closure

Whole-branch review found and closed four late Important gaps rather than
waiving them. A malformed closeout-fence schema now maps to the stable
unverifiable state instead of leaking a raw schema exception. The exact receipt
predicate validates the complete canonical envelope, including version,
identity, completion, and no-authority fields. The ownerless receipt reader
rejects default-filled or otherwise noncanonical on-disk records only on the
effect/cleanup authority path, while ordinary historical unversioned receipt
reading remains one-way compatible. The final reservation-release callee also
validates the complete canonical receipt, JSON Schema, retired state, empty
preservation fields, and exact decision, lane, head, and ownerless binding
before it may unlink the durable reservation.

Each gap was reproduced before its fix. The malformed-schema regressions first
failed in two real probe/recovery cases. Four invalid receipt-envelope cases
first passed incorrectly, and five-field plus unversioned pseudo-receipts first
deleted reservations. The resulting focused ownerless set passed, and the final
security review over index tree
`64fae42516b6feab0daf3dca46b0fbd4baa4148c` reported zero Critical, zero
Important, and zero Minor findings. The final simplicity review reported zero
Critical and zero Important findings. Its two deferred Minor observations are
that `receipts.py` is 497/500 effective lines and `records/core.py` is 485/500,
and that the duplicated exact validators are an intentional minimal two-boundary
defense that avoids a circular dependency. Neither threshold is raised.

## Final staged-snapshot verification

The first full-gate rerun after the initial security repairs was retained as a
failed result: 3 tests failed, 2,929 passed, and coverage was 99.99 percent. The
causes were one unclosed SQLite connection in a regression test, a
`receipts.py` code-size overflow, and one unexecuted canonical-envelope branch.
The repairs explicitly closed the connection, reduced the module without
weakening validation, and added a real noncanonical receipt case.

After the final reader and reservation-release fixes, the official
`tools/ci/scripts/run-python-tests.sh` gate completed with 2,938 passed. It
reported zero failures, zero errors, and zero skipped tests, and covered 22,802
of 22,802 statements and 6,178 of 6,178 branches for 100.00 percent coverage.
The canonical Python lint/format/ratchet gate passed. Types,
schemas, module layout, code size, no-compat, docstrings, Claims, strict
OpenSpec lifecycle, cached diff, and repository report were clean with no
required gaps. All 48 changed paths were staged and there were zero unstaged
tracked paths.

These results bind the staged index tree above. They are local staged-snapshot
verification, not an exact-HEAD proof, OpenSpec archive receipt, candidate land,
accepted-root closeout, publication, hosted CI result, or authorization to
delete a real foreign Work Lane.

## Disposable cross-repository effect acceptance

The retired external verifier baseline remained clean at
`main@5137759db0084d158f846169730791afb1079e58`. The tracked source and
installed host adapter remained byte-identical at
`18b8f92346c437365505a24dbb221d5dc6d8ef87a79875f115b95e36ea356f0c`,
and the drift state remained `in_sync`. The ownerless verifier suite passed 26
of 26 tests and the CLI suite passed 4 of 4 tests. Commit `59ee782` remained
superseded and was not cherry-picked.

The first diagnostic family was intentionally outside the retired verifier's
default projects root. Raw external-verifier admission passed only when
supplied the diagnostic `--projects-root` override; the production ETHOS consumer correctly omitted
that test-only override and the retired verifier rejected the topology before
effect. Moving the disposable family under the default `<projects-root>` topology was the only
acceptance-harness change. No product code changed for that diagnostic.

The final disposable family was
`zz-ownerless-effect-acceptance-d6d79e1e93ef82b2d46ff478`, with an empty
pre-existing prefix inventory. Its accepted `dev` and ownerless target both
started at `b610d683d95c183ae44c100bb0e0bcef99ca2310`, with tree
`44904f96e3c308b40bba6add7e84342bd7253398`. Decision
`lane-decision:0a731403-c842-41c7-bf27-ff015618cdfb` had SHA-256
`c23f66255ad11254810ec2d58b4798ce268d9e9c0cb091fbf8a90d0bf376e348`.
The installed external-verifier admission exited zero with empty stderr,
missing coordination lease/Claim as required for ownerless admission, clear occupancy,
and binding digest
`2414bb967d8dbd86d3f960ec016f6dec10b0ceae2d73b8d66b10328b1bd7c7d9`.

The real ETHOS effect exited zero with `state=retired` and no required gaps.
Receipt `lane-resolution-receipt:50d4cbf0-6eae-4ea0-a2dc-ecd4334dcf03`
was schema version 2, completed, non-authoritative, and had SHA-256
`f014a94ed34866a49ffcc72e7bbc60e028ba5a90558e34a39c08f236742bd115`.
Its `reconciliation_required=true` value is the exact exceptional-decision
contract because the irreversible retire decision carried `break_glass=true`;
it does not assert a partial transition. The historical receipt used
provider-specific field identifiers that are intentionally not reproduced in
current tracked truth. Its recorded semantics and values were:

- executor: `agent:codex:thread:019f69a7-dd6d-7ec0-9f60-e09b25ecf984`;
- provider contract: historical version 1, with the exact identifier redacted;
- decision digest: `c23f66255ad11254810ec2d58b4798ce268d9e9c0cb091fbf8a90d0bf376e348`;
- accepted branch and HEAD: `dev` at
  `b610d683d95c183ae44c100bb0e0bcef99ca2310`;
- provider-response binding digest:
  `2414bb967d8dbd86d3f960ec016f6dec10b0ceae2d73b8d66b10328b1bd7c7d9`;
- target digest:
  `e892fa2e0fc6d78bdb1853b0d1ccbf1813af07aa0030c7b3fd3cf843dd5f6452`;
- target-binding digest:
  `c3e4239106fc3423500a38aa081c7814d56305e68a8e6d24f4f45677ce655c4a`;
- postcondition digest:
  `9f60bd19fb91dd31113c1d721f18a426d2be0d344354c5edc070497e6016ab9d`.

Post-effect verification observed the target ref absent with the canonical
three-state probe (`show-ref --verify --quiet` exit 1 and empty output), the
target path and worktree registration absent, the accepted checkout unchanged
and clean, the fence absent, and the durable reservation absent. Inventory
exited zero with `state=ready`, one immutable receipt, and zero inflight or
partial records. Retired-verifier occupancy was clear. Canonical identity
checks then removed only the disposable repo, worktrees, and records roots; all three were
absent afterward and the unique prefix inventory was empty. No real foreign
Work Lane was selected or mutated.

## Workstation full evaluation and housekeeping

`workstation eval full` completed with exit zero. The full summary
`~/.config/workstation/logs/evals/agentic-eval-full-20260722-234742.json`
passed 25 of 25 gates with zero failures or skips and SHA-256
`1f221e15d2887573592fad129bfed3b699395d837d52871b7506e5a8bc375c03`.
Its stable snapshot was
`~/.config/workstation/backups/stable-20260722-234742`, digest
`21f60ad1fbb28fb8ccad45a2bef4d48f6b137f6c750fc70859f1f65fb7d4f7e9`.
The automatically chained quick summary
`~/.config/workstation/logs/evals/agentic-eval-quick-20260722-235112.json`
passed 19 gates with zero failures and six expected full-only skips, digest
`ed981df512482c1ccb1bc3b9fd0419e481d494381054358f26cc9a79040d184e`.
Evidence bundle `acceptance-20260722-234742-50970` was complete, ordered, and
passed. After completion, `workstation task status --json` reported zero live
leases and zero stale leases. No PyCharm restart, session cleanup, paid upgrade,
or GitLab access occurred.

The host-generated bundle contains local-clock fields after the authoritative
acceptance date. Those future-dated local fields do not mint chronology; this
Chronicle binds the controller/artifact date `2026-07-22`, the content digests,
and the observed command outcomes. Before this evidence update, the retired
external verifier was still clean and in sync, the ETHOS index tree remained
`f85e43d39c55489924ebefe9aa6dd8aed035711f`, and the Work Lane remained 48
staged paths with zero unstaged or untracked residue.

## Final base refresh

date: 2026-07-23
final_refresh_base: 102afdf3b0248b58bfde7aa2d0865109406c2ede
final_refresh_previous_head: 3734c1c0dd560a2eb55091565cb3e12798123c73
final_refresh_result_head: a9d970a6eb0352668413d04ace3cc5212f1fc4dd

The clean signed checkpoint was rebased only through `ethos lane refresh-base
--apply --authorize --expect-head`. The command returned
`state=base_refreshed` with no required gaps. The refreshed commit has parent
`candidate/dev@102afdf3b0248b58bfde7aa2d0865109406c2ede`, remains exactly one
commit ahead, and verifies with the configured SSH signing fingerprint. No
stash or autostash was used.

## Generic parity refresh

date: 2026-07-23
parity_product_head: 563651f2ebb3bd0bf03c97f17f9b61096c17a671
parity_target_head: 563651f2ebb3bd0bf03c97f17f9b61096c17a671

The admitted generic shadow executed all eight external-versus-embedded command
comparisons and returned `state=matched`, no required gaps, and
`false_negative_count=0`. Its first-pass product and target semantic digests
were both
`017fcd47f3de0c3433c16acdc8d0c05e28a032b28292ba418d56076ab8a051fb`.
After this task, Chronicle, and Claim update, the shadow is executed again and
`evidence/parity/generic-shadow.json` is the final tracked artifact. The final
file, rather than this prose, binds the post-update semantic digests and changed
path set.

## Runtime isolation

The armed-hook E2E exposes a private launcher, private `pyvenv.cfg`, and private
site-packages path instead of sharing the caller's writable editable virtual
environment. It snapshots editable `direct_url.json` bindings before and after
the proof and fails if the outer runtime owner changes. This is proof-harness
isolation, not product routing authority.

## Pre-archive implementation proof and archive preflight

date: 2026-07-23
proof_head: ce30f181dc444eb926d95bce162dc32a5e40f605
proof_mode: full
proof_state: proven
proof_gate_count: 30
proof_invocation_evidence_digest: 8845a0c65e1ae04abb381e0bee3d04faa904fdcc476712f411495b1880463230
proof_invocation_output_sha256: 15360250169c50b98bcc8b2b8f7eb14bcb2bc11ef88977bbef6eb99fde7b6150
proof_record_state: proven
proof_record_evidence_digest: 3e114448fa4dfe690dd390a18ea0d070649735476a947a5551e71ba2df2b2c26
proof_record_sha256: 0c3341422836877d4829930a8879fdf6439cf045cd0cd7b601f5721cc77794d7
lifecycle_state: clean
lifecycle_validation: 10/10
archive_preflight_state: ready
archive_preflight_isolated: true
lifecycle_output_sha256: d4d0646c7c04efe65c8daaced7e2a40cdcc3456fe317e2f9f1fbd0e4f481e5bf

The full implementation proof executed against committed and signed HEAD
`ce30f181dc444eb926d95bce162dc32a5e40f605`. It returned `state=proven`,
30 gates, no required gaps, and the per-invocation EvidenceSet digest recorded
above. The same-HEAD persistent proof record then merged the complete available
evidence for that commit, remained `state=proven`, and is separately bound by
its record digest and file SHA-256 above. The official lifecycle command
returned `state=clean`; strict validation passed 10 of 10 items, and the
isolated official archive preflight returned `state=ready` with no required
gaps.

Checking this task and refreshing the Chronicle and Claim are an evidence-only
closure after the final committed implementation/parity proof. A completed
active Change is intentionally reported as
`openspec_completed_change_unarchived:ownerless-closeout-effect-fence`, so a
second pre-archive proof would be both self-referential and lifecycle-blocked.
Official archive therefore follows a fresh isolated preflight; Claim migration,
canonical-spec fusion, post-archive parity, and exact archive-HEAD proof remain
separate later transitions.

## Official OpenSpec archive and canonical fusion

date: 2026-07-23
archive_input_head: 2180d6c2a72f897f2b2d4ac16106ca229da0e312
archive_change: ownerless-closeout-effect-fence
archived_as: 2026-07-23-ownerless-closeout-effect-fence
archive_specs_updated: true
archive_added_requirements: 1
archive_modified_requirements: 0
archive_removed_requirements: 0
archive_renamed_requirements: 0
archive_receipt_sha256: 2fe1c08aad248c404b8c0a48ec9875198b4850e9be4e8e3e2031bbc1a7de8f42
canonical_before_sha256: 72fe465fa0fa00ea731527c9901e3afa4862d2ceab2837e3edfe9f01015a8f95
canonical_after_sha256: 4c47835984bfc7d8635c4045afb02b1d6034505a9eece190b058a7791f62cdcf
archive_time_archived_delta_sha256: 77b4ccaf53b608883e92ab028eeadf2451d3a785cdfd3f96cfc2646cf0e5e97a
current_neutralized_archived_delta_sha256: 1240fb27693f5fd4b927282059dc741beac8bd9576080e34beea67c6b49e0751
canonical_added_lines: 157
canonical_deleted_lines: 0
archived_requirement_count: 1
archived_scenario_count: 15
archive_query_state: resolved
strict_post_archive_validation: 9/9

The official OpenSpec 1.6.0 archive command returned the exact dated carrier
`openspec/changes/archive/2026-07-23-ownerless-closeout-effect-fence`, reported
`specsUpdated=true`, and added one requirement with no modified, removed, or
renamed requirement. The active carrier is absent, the dated archive is
present, and the logical archive query resolves uniquely to that path.

Canonical fusion added the complete ownerless effect-boundary requirement and
all 15 scenarios with zero deleted canonical lines. Strict post-archive
validation passed all 9 canonical specs. The official CLI emitted one extra
blank line at canonical EOF; after fresh path admission, that formatting-only
line was removed. The semantic archive content and the `+157/-0` canonical
diff remained unchanged.

## Transition boundary

Authored implementation, exact-HEAD implementation proof, official OpenSpec
archive, exact-HEAD archive proof, candidate land, accepted-root closeout, local
publish readiness, and remote publication are separate transitions with separate
current receipts. Retired-verifier file equality and component tests do not
substitute for ETHOS proof, land, closeout, or publish evidence. Remote and GitLab publication
remain independent and non-blocking.

## Boundary

This Chronicle records the design, external contract baseline, and bounded
implementation safety invariants. It does not authorize deletion of the foreign
predecessor and does not claim candidate land, accepted-root closeout, remote
publication, GitLab validation, distributed locking, or filesystem locking.
