---
subject: ethos:ownerless-diverged-semantic-resolution-20260724
state: active
created: 2026-07-24
claim: ownerless-diverged-semantic-resolution-20260724
---

# Ownerless Diverged Work Lane Semantic Resolution

## Current observation

Accepted `dev`, `main`, and `candidate/dev` are aligned at
`f1e2481ad0e77247953c81b6b69dfe2de44c2205` and the accepted checkout is clean.
The current foreign inventory contains 79 Work Lanes: eight live-lease lanes,
including this controller's carrier, are protected and 71 missing-lease lanes
are eligible for semantic judgment. All 71
are diverged from accepted; 56 are clean and 15 are dirty. Strict ancestry
reduces the eligible cohort to 47 maximal tips.

This record does not infer ownerlessness from process absence, branch age, or a
missing Claim. `lease_state=missing` is the target boundary. A live lease remains
protected even when its Claim binding is incomplete.

The accepted train advanced from `fe94c0268` to `f1e2481a` through the bounded
GitHub pytest-timeout resilience change and its parity receipts. A fresh full
lane-status observation proves that the 71 missing-lease targets, their 56/15
clean-dirty split, and the 24-member identity lineage are unchanged. The
OpenSpec lifecycle, Claims, strict validation, and 85-test focused suite were
re-executed at `f1e2481a` before retaining the decision below.

## OpenSpec identity-normalization lineage decision

The 24-member lineage ending at
`work/openspec-identity-normalization-successor-v26-20260720@a8946dbb03c2402f96610acf6bb2da833b4a0bb5`
has one authoritative current replacement:
`evidence/chronicle/openspec-identity-normalization-current-20260720/2026-07-20.md`.
That accepted record owns date-free logical Change IDs, singly dated archives,
exact-one archive lookup, duplicate rejection, and the no-alias/no-date-fallback
boundary.

At the current accepted HEAD:

- `ethos openspec --lifecycle --json` is clean;
- `ethos quality claims --json` is clean across 236 Claims;
- strict OpenSpec validation passes all 9 accepted specifications;
- 85 focused OpenSpec identity, Claim, schema, and invalid-state tests pass with
  warnings treated as errors.

The V26 tip's seven untracked records explicitly classify V26 as a stale receipt:
candidate drift occurred before remediation, parity, executed proof, or land,
and the record forbids refresh-base, rebase, cherry-pick, rewrite, manual replay,
or using V26 to claim current remediation. Those seven files are therefore
preserved as recovery evidence. They are not promoted as current product truth.

The historical alternate `packages/ethos/src/ethos/repository/openspec/identity.py`
implementation is rejected rather than merged beside accepted
`identifiers.py`; dual grammar owners would violate the accepted single-owner
contract. The accepted implementation and focused checks are the live product
surface.

Decision: select the dirty V26 tip for exact v2 `preserve-retire`; select its 23
clean ancestry members for exact `retire`. Every effect remains conditional on
fresh same-HEAD, missing-lease reobservation and one-lane postcondition proof.

## Remaining boundary

The other 47 ownerless lanes remain `pending_semantic_analysis`. This Chronicle
does not authorize their retirement yet. It also makes no claim about the seven
leased lanes, remote push, hosted CI, release/tag publication, or distribution.
