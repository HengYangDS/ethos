# Dirty ownerless Work Lane semantic absorption — skill scripts

event: lane_resolution/preserve-retire
target_branch: work/skill-scripts-ruff-20260719
target_head: 87911a89faeb01d97a29afce1c24e0fc5ed94f2a
target_claim: ownerless-skill-scripts-semantic-closeout-20260720

## Fact

At carrier admission the target is a linked, accepted-ancestor Work Lane with
no active lease and four dirty helper-script paths. Current Ruff rejects the
remaining direct `print` calls. The useful part is reimplemented on the current
baseline; stale source assumptions that conflict with current quality tests are
not copied.

## Decision

This Claim and Chronicle bind one later native `lane_resolution/preserve-retire`
transition for the exact target only, after this carrier's own proof, candidate
land, and local accepted closeout. Native resolution must freshly observe and
preserve the exact source state before any destructive effect, then remove only
that source worktree/ref and write a receipt.

## Boundary

The Chronicle does not authorize a batch action, raw deletion, a lease takeover,
a source-history merge, movement of `candidate/dev`, remote mutation, GitHub or
GitLab CI claims, or retirement of any other lane.
