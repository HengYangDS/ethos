# Evidence: parity-evidence-transaction-20260712

## Scope

Repair the parity-proof ordering deadlock without granting candidate or accepted
roots write authority.

## Decision

Configured generic parity is a Work-Lane pre-proof obligation. The valid order
is: commit parity-relevant source; refresh parity in the admitted Work Lane;
commit the evidence record; execute proof; then land to candidate. Candidate
and accepted roots stay protected and never repair this evidence after land.

## Evidence Plan

- Focused parity and evidence-freshness regressions prove stale configured
  evidence becomes an explicit gate result with its Work-Lane refresh package.
- Existing parity validation continues to reject semantic source drift and to
  accept the evidence-recording commit only through semantic-tree equality.
- Full local proof, OpenSpec validation, and lifecycle state are recorded here
  before this active carrier is archived and landed.

## Executed Verification

The first complete Work-Lane proof was executed against
`d08b75d44dde99b4126fca057a699482908f9a16` with:

```bash
ethos prove --execute --full --expect-head d08b75d44dde99b4126fca057a699482908f9a16 --json
```

It returned `ok=true`, `state=proven`, no required gaps, 28 passing gates, and
evidence digest
`90ef659f9572d34752bf6eb7844a1d2e7d3d89d3f903bf1122e5134c90cc3b1e`.
The gate set included the full unit and architecture suite, Python lint,
evidence freshness, strict OpenSpec validation, schema contracts, build, and
generated-artifact policy. `ethos openspec --lifecycle --json` also returned
`state=clean`; `ethos plan --changed --json` returned `state=planned` with no
required gaps.

This chronicle and its claim are parity-relevant truth. They are committed
before their own generic-parity refresh; the refresh is then committed as an
evidence-only record, and a fresh HEAD-bound proof is required before archive
or land. This is the enforced transaction rather than a post-land repair.

## Boundaries

- No foreign Work Lane, candidate checkout, accepted checkout, or remote branch
  is mutated by this mechanism lane.
- Remote publication remains separate and deferred while GitLab is unavailable.
