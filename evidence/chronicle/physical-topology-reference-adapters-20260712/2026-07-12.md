# Reference Adapter Physical Topology Evidence

Date: 2026-07-12
Work Lane: `work/physical-topology-reference-adapters-20260712`
OpenSpec carrier: `openspec/changes/physical-topology-reference-adapters-20260712`

## Scope

This record binds a physical-topology correction only. The optional,
provider-local independent-verification reference source moved from the
unowned root-level `reference_adapters/` directory to the declared
`extensions/independent-verification/` bundle, together with its manifest,
documentation, and focused test.

It does not change policy modes, verifier behavior, provider accounts,
`yheng-agent-ethos`, receipt stores, sandbox configuration, host scheduling,
adopter configuration, or public ETHOS commands. It does not modify, land,
retire, or clean any foreign Work Lane.

## Evidence Boundary

- The reference implementation remains default-off and provider-local.
- The extension bundle is source and documentation, not a dynamically loaded
  product package or a second command plane.
- The root directory was removed without a forwarding module, alias, symlink,
  or compatibility path.
- Earlier assurance evidence retains its historical predecessor path; this
  record attests only the current topology correction.

## Recorded Authoring Evidence

- The extension-path test was added before the source move and failed because
  the target path did not exist.
- After the move, the extension-focused suite passed: 6 tests.
- Ruff passed for the extension, format selection passed for touched source and
  documentation, `git diff --check` passed, and the current non-historical
  tracked tree contains no `reference_adapters/` reference.

Final OpenSpec lifecycle, changed-path plan, executed proof, parity refresh,
candidate land, accepted-root closeout, and local publication readiness are
separate subsequent gates. Remote publication is not claimed.
