# Product-Parity Runtime/Evidence Compression — 2026-07-13

## Baseline

- Work Lane: `work/product-parity-runtime-evidence-compression-20260713`.
- Base: `78efdedb7b0a54177099ef80ca09108ed32851db`.
- Scoped effective Python lines: **1,042** across `snapshots.py`,
  `test_runners.py`, `test_evidence_writing.py`, and `test_report.py`.
- The measure is the repository's canonical
  `ethos_core.measure.effective_code_lines` contract: comments, blanks,
  docstrings, and padding strings are excluded.

## Cutover

- Shared only inert subprocess success setup and Work-Lane lease setup in the
  existing parity snapshot fixture module.
- Unified the three declared embedded backends and the two external invocation
  modes as literal pytest case tables.
- Unified product- and target-parent evidence freshness cases as one literal
  parameterized partition.
- Retained named protected-write, missing-backend, timeout, malformed-output,
  exit-code, external-adopter, profile-evidence-root, and stale-target tests.
- Scoped effective Python lines are **982**: **60 deleted** (**5.76%**).
- The three replaced test modules alone shrink from **888** to **798** effective
  lines: **90 deleted** (**10.14%**); the shared fixture grows by 30 lines.

## Pre-Archive Verification

- Focused runtime/evidence/report tests: `23 passed`.
- Full product-parity suite: `95 passed`.
- Python lint and formatting gate passed after lowering the stale `ARG001`
  ratchet from 55 to its measured 47 findings.
- `tools/ci/scripts/run-python-tests.sh` passed: `2,063 passed`, with
  100-percent line and branch coverage.
- `ethos quality source-budget --json` remained clean. It measured 110,449
  global effective source lines and 88,467 Python effective source lines; the
  terminal targets remain 73,739 and 58,722, respectively, so this bounded
  deletion does not claim terminal compression completion.
- The active carrier passed strict OpenSpec validation and isolated official
  archive preflight. Its delta modifies the existing declarative product-parity
  requirement rather than adding a duplicate requirement.

## Boundary

This record binds only the compact test representation and its pre-archive
verification. The official OpenSpec archive completed as
`openspec/changes/archive/2026-07-13-product-parity-runtime-evidence-compression-20260713`;
it modified the canonical `quality` specification and added no duplicate
requirement. Generic parity was then regenerated only through `ethos parity
shadow --adopter generic --target . --execute --write-evidence --json` at the
archived-carrier head and matched all eight comparisons with zero false negatives.
Final HEAD-bound proof, candidate land, accepted-root closeout, Work Lane
retirement, and remote publication remain separately governed transitions.
Remote GitLab publication remains deferred while the remote is unavailable.
