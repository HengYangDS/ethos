# Product-Parity Semantic Test Compression — 2026-07-13

## Baseline

- Work Lane: `work/product-parity-test-suite-compression-20260713`.
- Base: `75c761eaa4a178ea774e13c9cad164ccfdf56ae9`.
- Scoped effective Python lines: **1,483** across `snapshots.py`,
  `test_semantics.py`, `test_projection_semantics.py`, and `test_gaps.py`.
- The measure is the repository's canonical
  `ethos_core.measure.effective_code_lines` contract (comments, blanks,
  docstrings, and padding strings excluded), calculated at base
  `75c761eaa4a178ea774e13c9cad164ccfdf56ae9`.
- Visible foreign Work Lanes do not claim any scoped path at admission time.

## Cutover

- `parity_payload()` and `write_parity_evidence()` own repeated test-envelope
  construction and evidence-file setup. They do not evaluate parity behavior.
- `accepted_difference()` holds literal public output facts for accepted
  difference kinds; it does not classify inputs or call the product runtime.
- Uniform semantic and projection partitions now use named declarative pytest
  cases. False-negative, process-failure, schema, and integration boundaries
  remain named tests.
- Scoped effective Python lines are **1,268**: **215 deleted** (**14.50%**).

## Verification

- The product-parity suite plus dependent repository-coverage edges passed:
  `108 passed in 8.79s`.
- Repository Ruff check and format passed for all four scoped files.
- The complete Python suite passed: **2,067 passed** in **163.801s**, with
  **17,575 / 17,575** lines and **5,358 / 5,358** branches covered.
- `ethos quality source-budget --json` is clean: global effective source is
  **110,050** against a **105,342** baseline and an explicitly declared
  **5,267** transition allowance; the terminal global target remains
  **73,739**, so the program remains incomplete by **36,311** lines.
- Generic parity was re-executed for this committed source head and matched all
  eight comparison commands with zero false negatives. Its shared tracked
  projection was then regenerated only through `ethos parity shadow
  --adopter generic --target . --execute --write-evidence --json`; no JSON was
  hand-edited. The evidence-only follow-up commit is accepted by the relevant
  source-semantic freshness rule.
- The repository full coverage run validates the final claim digest, then
  executed HEAD-bound proof supplies lifecycle evidence. This slice does not
  claim the terminal compression program is complete.
- An executed proof at `9ca3369f11646b16cbbcb20cbd0a46ee99a0e8df` proved all
  21 required gates with evidence digest
  `f43fffad43dcc622805b277290bfc2457da69c16dd0cc40cb7f8d2487f55cd12`.
  The final OpenSpec archival mutation is followed by one more HEAD-bound proof
  before integration.
