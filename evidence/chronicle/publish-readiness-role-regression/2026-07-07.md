# Publish readiness role regression

The land role-boundary fix was narrowed after final accepted-root validation
showed `ethos publish --json` incorrectly blocked on `protected_root_mutation`.
The corrected reducer keeps role-aware dry-run blocking for normal land while
preserving publish dry-run as the accepted-root publication readiness report.
