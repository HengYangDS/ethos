# Publish synchronized tracking observation — 2026-07-14

## Observation

The read-only `publish --probe-remote` path already read local tracking state
but displayed a generic deferred publication label even when the observed remote
tracking ref equaled the current HEAD.

## Decision

The command now projects that narrow fact as `synchronized` while retaining
`remote_push = "not_performed"` and a deferred mutation verdict. This is an
observation of an existing ref, not evidence that this command published,
created a submit branch, or ran a hosted runner.

## Verification

Focused publication, lifecycle, and coverage regression suites passed. A 21-gate executed proof passed on the pre-refresh lane head; candidate advancement then required a governed refresh-base replay and parity regeneration. The change is archived here as completed local work. Candidate landing, accepted-root closeout, remote push, and hosted CI remain separate governed transitions and are not claimed by this record.
