---
subject: quality:python-arg002
role: evidence
state: active
relations:
  supports: quality-arg002-exemption-elimination-v3-20260720
---

# ARG002 Quality Exception Elimination — 2026-07-20

## Fact

At the candidate-current lane base, `DryRunRunner.run` contributes the only
tracked-corpus `ARG002` diagnostic. The same rule is excluded by both canonical
Ruff exception carriers.

## Change

Dry-run planning now resolves its declared execution root without action
execution. `ARG002` leaves the global ignore list and ratchet baseline, so the
existing canonical Ruff owner directly enforces it. No second policy, command,
or compatibility surface is added.

## Evidence

Focused contracts passed (`45 passed`); the complete tracked corpus reports no
`ARG002` diagnostic; the canonical Ruff owner, Claim validation, and strict
OpenSpec validation passed. The lane was refreshed onto the then-current
candidate head.

A current-HEAD executed proof was also run. It remained gapped only on the
pre-existing generic-parity freshness path (`unit-architecture` and
`evidence-freshness`), whose generic product and target heads no longer match.
That result does not establish candidate integration and is not attributed to
this `ARG002` correction.

The OpenSpec carrier was then archived through the official CLI after its
preflight succeeded.

## Boundary

This carrier proves only the `ARG002` slice. It does not claim terminal
exception-free quality, candidate integration, accepted closeout, publication,
or foreign-lane resolution.
