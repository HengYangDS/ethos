---
subject: ethos:quality-exemption-elimination
role: evidence
state: active
relations:
  supports: quality-exemption-elimination-20260720
---

# Quality Exemption Elimination — 2026-07-20

## Scope

This bounded correction removes one retained Python-quality exception carrier.
It replaces the implicit local-calendar clock
in policy-exception expiry evaluation with an explicit UTC calendar boundary,
then removes the corresponding `DTZ011` Ruff ignore and ratchet record. The
same slice binds type checking to the checkout-local runtime bootstrap and
clears inherited active-venv state, so clean results are neither resolved from
an unmaterialized lane nor obscured by ambient-environment warnings.

The remaining global and per-file Ruff ignores, source suppressions, ratchet
records, and terminal quality-law work remain outside this bounded slice. This
record does not claim terminal zero-exception quality, candidate integration,
accepted-root closeout, remote publication, or hosted CI success.

## Evidence

The following commands were executed in the owned Work Lane before this record
was written:

```bash
uv run --all-packages --group dev pytest -q tests/unit/kernel/rules/test_exceptions.py
uv run --all-packages --group dev pytest -q tests/unit/governance/test_generated_artifact_runtime.py tests/unit/coverage/test_cov_v3_gates_edges.py tests/unit/cli/test_quality_surface_more.py
uv run --all-packages --group dev ruff check --config .config/checks/ruff/ruff.toml --select DTZ011 --ignore-noqa $(git ls-files '*.py' '*.pyi')
uv run --all-packages --group dev ruff format --config .config/checks/ruff/ruff.toml --check packages/ethos/src/ethos/repository/policy/rules/exceptions.py tests/unit/kernel/rules/test_exceptions.py
uv run --all-packages --group dev ethos quality types --json
npx --yes @fission-ai/openspec@1.6.0 validate quality-exemption-elimination-20260720 --strict --json
```

Observed results: the focused policy-exception suite passed (`6 passed`); the
runtime and type-gate regression suites passed (`8 passed` and `24 passed`);
the native type gate reported zero diagnostics for both governed packages with
no ambient-venv warning; the whole tracked Python corpus reported no `DTZ011`
findings under the native Ruff policy; both changed Python files were
formatted; and the OpenSpec change validated strictly.

## Boundary

This Chronicle binds only the UTC/DTZ011 removal slice and its explicit test
surface. A future claim or proof must independently establish any broader
exception-carrier deletion, candidate landing, accepted-root closeout, remote
publication, or hosted-CI result.
