---
subject: ethos:release-local-install-smoke-contract
state: archived
created: 2026-07-19
claim: release-local-install-smoke-contract-20260719
---

# Release Local Install Smoke Contract — Initial Record

## Boundary

This record opens the local release-owner repair at
`8b59c8f17e2952ad1a15dd03a7e7432802e04d3a` in the owned Work Lane
`work/release-local-install-smoke-contract-20260719`.

It does not claim completed implementation, passing proof, candidate landing,
accepted-root closeout, remote publication, hosted CI, registry delivery, or
independent verification.

## Reproduced Facts

- Publication topology declares
  `tools/ci/scripts/run-local-install-smoke.sh` as the installation owner.
- That tracked executable is absent at the opening HEAD.
- Release policy still reports ready because local command surfaces are not
  validated.
- Local CI and the full-proof graph do not execute an install smoke.
- A prototype using offline wheel build/install, a fresh virtual environment,
  dual-package origin checks, and installed CLI help/version completed only in
  ignored runtime scratch; it is diagnostic evidence, not repository truth.

## Planned Proof

- RED/green release-policy and architecture tests.
- Real owner-script execution with HEAD-bound JSON evidence.
- Shell/config/release quality checks and quality audit.
- Official OpenSpec lifecycle, claim freshness, parity, default proof, full
  proof, archive, candidate/accepted closeout, local publication readiness,
  separately authorized remote publication, exact-SHA hosted observation, and
  lane retirement as distinct transitions.

## Implemented Local Contract

Commit `5b91ead571f74bf2449f9dc42d730f3facb4372b` added the fail-closed
release-policy validation, executable offline install owner, local-CI
composition, full-proof gate, tool registration, tests, docs, and official
OpenSpec carrier. Commit `d21d10ecd9713fe0718fda9a05545c4a2515c8dd`
corrected generated-artifact route recognition and covered the non-regular owner
case. Commit `d079a4a26736ef2a0457ffe96cb0c4704f032874` refreshed the generic
parity projection after those semantic changes.

The owner uses only `build/runtime/work/local-install-smoke/**` for disposable
state, builds wheels under `build/artifacts/python/**`, and writes the generated
receipt to `build/evidence/local-install/smoke.json`. It validates both local
command paths without adding a second release configuration owner.

## Pre-Archive Validation Evidence

- The RED batch produced `9 failed, 25 passed` for the deliberately absent
  owner, release-policy gaps, local-CI composition, and full-proof declaration.
- The focused final batch produced `30 passed`.
- Python lint, shell lint, config lint, and the repo-local quality audit were
  clean.
- `ethos quality release-policy`, `ethos quality gates`, generated-artifact
  topology, claims, OpenSpec lifecycle, generic parity, and evidence freshness
  were clean.
- The full Python owner gate at `d079a4a26736ef2a0457ffe96cb0c4704f032874`
  produced `2497 passed` with line and branch coverage `100.00%`.
- The real offline install owner succeeded at that same HEAD. `ethos` and
  `ethos_core` both loaded from the fresh smoke venv; installed CLI help and
  version succeeded; the package version was `0.1.0a1`; and the receipt
  explicitly set hosted CI, remote publication, and registry publication claims
  to false.
- Wheel SHA-256 values were
  `17d412a3fe9553db5812d646440ab4d5578bd474060bf879fb40e453a4d86b50`
  for `ethos-core` and
  `6e625d5d19d4d1b60ab345a658b32e0951770cfb499a585c3be75a535023db8f`
  for `ethos`.

The earlier GitHub runs for accepted HEAD
`8b59c8f17e2952ad1a15dd03a7e7432802e04d3a` failed on an independent
bounded-reader empty-topology regression. This record does not convert that
failed hosted observation into success; it requires a separate governed repair
before the final remote stage.

## Pre-Archive Proof And Official Archive

At committed HEAD `9b42c7aa1619623fbd0b6e56ac88d97ff9110e99`, the
default executed proof passed `19` gates with evidence digest
`97d7e778e1b13d8a17a7a45d648c31a3a17c3fb1280263e092ba0d52fcd1e94a`.
The full executed proof passed `29` gates with evidence digest
`37b1753a7466b174582fae126585d9aebead1af6cb0abc25e5c00f0d57cdf408`;
its `local-install-smoke` action exited zero with `state=proven` and
`verdict=passed`.

The official OpenSpec archive command moved the Change to
`openspec/changes/archive/2026-07-19-release-local-install-smoke-contract-20260719`
and fused one added quality requirement plus one modified
repository-governance requirement into the canonical specifications. It removed
no requirement and performed no remote operation.

Post-archive parity, default/full proof, candidate landing, accepted closeout,
remote publication, hosted CI, and lane retirement remain separate transitions.

## Stop Conditions

Stop local closeout if the Work Lane lease, candidate base, OpenSpec claim,
parity evidence, or proof HEAD becomes stale. Do not mutate accepted, candidate,
or foreign Work Lanes through ordinary writes.
