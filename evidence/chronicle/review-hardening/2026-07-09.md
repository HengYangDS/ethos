---
subject: evidence:review-hardening
state: active
created: 2026-07-09
claim: review-hardening
---

# Review Hardening Evidence — 2026-07-09

## Boundary

This evidence records the active Work Lane carrier for the expert-review
hardening batch. The claim is bounded to source, tests, config, OpenSpec deltas,
and command evidence gathered in the Work Lane. It does not claim remote
publication, hosted CI success, or accepted-root closeout.

## Current Change Contract

The OpenSpec carrier was archived to
`openspec/changes/archive/2026-07-08-review-hardening-2026-07-09`. It covers fail-closed proof,
protected-root shell admission, Work Lane lease/actor binding, candidate ref
movement protection, HEAD-bound claims/freshness, report read-model honesty,
local publish readiness vocabulary, local-ci target-root owner projection, and
release supply-chain hardening.

## Verification Plan

The closeout proof for this claim requires:

- focused pytest suites for proof, admission, lane, land/publish, report,
  quality, parity semantics, and release assets;
- script syntax checks for changed CI scripts;
- `ethos openspec --lifecycle --json`;
- `ethos playbooks route --changed --json`;
- `ethos plan --changed --json`;
- `ethos report --json`; and
- `ethos prove --execute --expect-head <HEAD> --json` before completion.

## No Compatibility Residue

The change intentionally does not preserve retired publish state names or old
private admission helper re-exports. Tests and callers are moved to the current
owner contracts instead of forcing compatibility surfaces to remain.

## Executed Verification

Final Work Lane verification was run from
`<worktree>` with `ETHOS_ACTOR=codex`
on HEAD `1c73ef42774dd05dfba8cd237b58f3aefae4ddc0`.

- `.config/ci/scripts/run-python-lint.sh` passed with ruff ratchets not
  exceeded.
- `.config/ci/scripts/run-config-lint.sh` passed.
- `.config/ci/scripts/run-shell-lint.sh` passed.
- `.config/ci/scripts/run-python-tests.sh` passed: `1256 passed`, line and
  branch coverage `100.00%`.
- Focused regression suite passed: `206 passed`.
- `ethos openspec --lifecycle --json` returned `ok=true`, `state=clean`, and no
  required gaps after archiving `review-hardening-2026-07-09`.
- `ethos playbooks route --changed --json` returned `ok=true`, `state=routed`.
- `ethos plan --changed --json` returned `ok=true`, `state=planned`.
- `ethos report --json` returned `ok=true`, `state=ready`, profile `product`,
  read model `governed_repository_scorecard_v2`, effective score `16/16`,
  terminal control `closed_loop`, hard quality gap count `0`, and coordination
  risk count `0`.
- `ethos prove --execute --expect-head
  1c73ef42774dd05dfba8cd237b58f3aefae4ddc0 --json` returned `ok=true`,
  `state=proven`, no required gaps, and a HEAD-bound expected-head match.
- The retired publish-state and admission-helper vocabulary residue scan
  across `packages`, `tests`, `docs`, `openspec`, `system`, `.config`, and
  `evidence` returned no matches.
