# Quality Portability and Authoritative Scope Specification — 2026-07-17

## Claim

The configuration-lint owner script no longer depends on a host-provided bare
`python` alias. The accepted repository-governance specification now carries
one authoritative adopter material-change scope requirement; it preserves
official OpenSpec lifecycle authority and treats `scope.toml` only as an ETHOS
companion.

## TDD Evidence

1. The portability regression was changed to expose only an explicit semantic
   Python interpreter and a `python3`-capable system path, with no bare
   `python`. It failed at the former inline `python` invocation.
2. The minimal runner change added the bounded interpreter chain
   `ETHOS_PYTHON`, then `PYTHON`, then `python3`, for TOML, JSON, and structural
   whitespace checks. The same regression passed after the change.
3. The canonical-spec regression first failed because the accepted
   repository-governance specification contained both scope-binding headings.
   Removing the earlier duplicate left one authoritative requirement and the
   regression passed.
4. Official OpenSpec validation and a disposable official archive preflight
   succeed for the active Change. The preflight confirms that the delta can
   update the quality and repository-governance specs without losing an
   existing scenario.
5. The isolated owner-script run completed eight shards at
   `/private/tmp/ethos-roadmap-quality-tests-20260717-full-173000`: every
   shard passed, coverage combined, and the reported total was 100 percent.

## Verification Boundary

The local evidence so far covers focused portability and scope tests, owner
lint, strict OpenSpec validation, lifecycle, and an isolated sharded full test
run. Generated test logs remain under the ignored isolated evidence path. The
remaining closeout steps are parity refresh and committed-HEAD proof. This
Chronicle does not claim candidate landing, accepted-root closeout, GitLab
publication, GitHub publication, or hosted CI success.

## Non-Authority Boundaries

- Official OpenSpec remains the sole Change lifecycle carrier.
- `scope.toml` remains an ETHOS-owned companion and is not an official OpenSpec
  workflow-schema extension.
- Superpowers and all method packages remain workflow projections, not
  governance carriers.
