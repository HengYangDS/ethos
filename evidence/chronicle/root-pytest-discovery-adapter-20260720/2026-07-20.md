# Root Pytest Discovery Adapter — 2026-07-20

## Fact

At the authoring baseline `cf437ca3dae182e63ffb67ce4759ffd4caf35f70`, the
repository-owned Python test runner passed
`.config/checks/pytest/pytest.ini` explicitly and routed its cache correctly.
No current repository-controlled test execution path was found to omit that
configuration. The root had no `pytest.ini` and no pytest discovery metadata.

## Decision

Add only the root discovery cache route. Keep behavior and proof ownership in
the existing concern-owned pytest INI and repository-owned runner. During
validation, the quality audit was also found to invoke the public CLI through a
non-workspace `uv` command, which can fail before its checks run when the root
project is not installed as a console script. Route that projection through the
same all-package workspace runtime; this changes no quality policy or proof
authority.

## Validation

Before archive, the direct-discovery scratch execution wrote its cache only to
`build/runtime/tool-cache/pytest` and did not create root `.pytest_cache`.
The focused configuration, architecture, and quality-audit regressions,
including an exact testing-platform contract that admits only that root adapter
shape, passed without coverage or xdist plugins (`43 passed`), and the quality
audit passed
through its repaired workspace-runtime invocation. Python lint, configuration
lint, Markdown lint, strict OpenSpec validation, and OpenSpec lifecycle also
passed. The modified quality-skill package digest was recomputed from its
declared source files before commit. A separate owner-script full test run was intentionally not used as
this Change's focused proof: that runner always selects the repository-wide
`tests/unit tests/architecture` set, and concurrent repository activity makes
such a shared generated-evidence execution unsuitable for a narrow lane claim.

## Boundary

This historical record does not claim a completed full proof, candidate landing,
accepted-root closeout, remote publication, or hosted CI result. The archive
captures the bounded source change only; any later promotion requires its own
current-HEAD evidence.
