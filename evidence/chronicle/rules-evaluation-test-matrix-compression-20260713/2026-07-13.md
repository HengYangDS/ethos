# Rules Evaluation Test-Matrix Compression — 2026-07-13

## Baseline

- Work Lane: `work/rules-evaluation-test-matrix-compression-20260713`.
- Base: `1b4f82954d2451b9546c0441dfe805e4bc0c95b5`.
- Scoped effective Python lines: **703** across `snapshots.py`,
  `test_evaluation.py`, `test_cov_v2_policy_edges.py`, and
  `test_cov_v3_policy_edges.py`.
- The lane had no overlapping tracked path scope with visible foreign Work
  Lanes at admission time.

## Cutover

- `fact()` and `READY_FACT_OWNERS` express stable rule-fact envelopes once;
  `complete_snapshot()` projects those declarations instead of repeating
  mutable-looking dictionaries.
- Eight coverage-only helper-edge bodies now live in the canonical
  rules-evaluation test surface; duplicated imports and test bodies are absent.
- A generic pytest callable matrix was rejected: it increased effective lines
  for heterogeneous helper signatures. The chosen representation is the smaller
  immutable input declaration plus one canonical edge test.
- Scoped effective Python lines are **612**: **91 deleted** (**12.94%**).
- The repository source budget after the cutover is `python_total=88,288`
  (terminal `58,722`, remaining `29,566`) and `global_total=110,163`
  (terminal `73,739`, remaining `36,424`). This bounded slice is not a claim
  that the terminal compression program is complete.

## Verification

- Focused rules tests: `44 passed`.
- Ruff check and format check passed for all four touched test files.
- `tools/ci/scripts/run-python-tests.sh` passed: `2,064 passed in 205.88s`;
  line and branch coverage were both `100%` (`17,563/17,563` lines and
  `5,356/5,356` branches).
- A post-rebase executed proof at `b12f56ff7da814e4b276874f0b14659af544ba25`
  proved all 21 required gates with evidence digest
  `2bb6508bdce9fe19508cdf20c9867fc677bb986d87b7836399bf7896fbd3cded`.
  The archival mutation is followed by one final HEAD-bound proof before land.
- The official OpenSpec archive command moved the carrier to
  `openspec/changes/archive/2026-07-13-rules-evaluation-test-matrix-compression-20260713`
  and promoted its delta requirement into `openspec/specs/quality/spec.md`.
