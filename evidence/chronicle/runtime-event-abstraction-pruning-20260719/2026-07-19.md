# Runtime and event abstraction pruning

## Fact

ETHOS carried lane Runtime-composition forwarding, seven dependency containers,
event declarations without dataflow, disposable lease-schema compatibility machinery,
and standards-adapter records without implementations or consumers.

## Boundary

This change deletes those abstractions while preserving lifecycle mutation
safety, Chronicle evidence, pure projections, Git hooks, and explicit adapter
boundaries. Runtime dependency containers and dependency-injection parameters
are zero. Ignored SQLite state creates only its current lease table. It does not
introduce DI, an event bus, a plugin manager, aliases, wrappers, shims, or
compatibility behavior in the bounded runtime/event surface.

## Evidence

- Active OpenSpec carrier:
  `openspec/changes/archive/2026-07-19-runtime-event-abstraction-pruning-20260719`
- Runtime dependency inventory: `RuntimeClass=0`; no dependency-injection
  `runtime` parameter remains.
- Committed implementation diff: `+467/-1957`, net `-1490` lines.
- Source-budget measurement: Python `92645 -> 92456`, global
  `109966 -> 109717` versus the prior live Work Lane measurement.
- Broad runtime, retirement, state, workflow, CLI, OpenSpec and standards regression: `442 passed` with warnings treated as errors.
- Ruff, ty, config lint, strict OpenSpec validation and `git diff --check`
  completed successfully.
- Generic shadow parity is matched, claims are clean, and the OpenSpec carrier is archived. Final HEAD-bound proof remains the local closeout step.

## Decision

Proceed through one owned Work Lane. Prefer deletion and direct owner calls.
Remote publication remains deferred to the campaign boundary.
