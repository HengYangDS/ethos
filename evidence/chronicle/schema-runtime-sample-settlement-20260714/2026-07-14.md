---
subject: quality:runtime-schema-sample-settlement
state: active
---

# Runtime Schema Sample Settlement — 2026-07-14

## Decision

Delete product-runtime JSON Schema samples that only manufactured synthetic
payloads for `ethos quality schemas`. Keep schema-file validation and live
producer validation in the runtime command; retain direct schema assertions at
the existing producer test boundaries.

## Boundary

This change does not alter any JSON Schema, command payload, or producer
semantics. It removes duplicate fixture logic only. The exact final HEAD and
proof digest are recorded after parity and executed proof succeed.
