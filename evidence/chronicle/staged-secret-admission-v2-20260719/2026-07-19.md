# Staged Secret Admission V2 — Initial Local Record

## Boundary

This record opens the local-only `staged-secret-admission-v2-20260719` Change
from accepted/candidate HEAD `fe774c994c5641b60f49a3e60a968ed8eba6fbee`
in the owned Work Lane `work/staged-secret-admission-v2-20260719`.

It does not claim completed implementation, passing focused tests, archive,
parity, HEAD-bound proof, candidate landing, accepted-root closeout, remote
publication, or hosted CI.

## Reproduced Facts

- The accepted pre-commit hook checks staged Python formatting and ETHOS write
  admission but does not scan the staged index for secrets.
- The full secret gate scans a tracked-tree mirror and Git history, installs or
  locates gitleaks, and writes quality evidence; it is not a commit-time path.
- The historical staged-secret lane invokes inline
  `gitleaks protect --staged`. Gitleaks 8.30.1 omits `protect` from its advertised
  top-level command list but still accepts it as an unadvertised compatibility
  command; `gitleaks git --help` explicitly documents `--staged` as the current
  pre-commit interface.
- A local scratch probe with a high-entropy synthetic GitHub-token-shaped value
  showed both unadvertised `protect --staged` and advertised `git --staged`
  returning the configured finding exit while `--redact=100` kept the value out
  of stdout and stderr. The exact command shapes, exit codes, redaction markers,
  and output digests are recorded in the non-authoritative scratch summary
  `/tmp/ethos-reeval-live-20260719/gitleaks-interface-probe-summary.json`
  (SHA-256 `9ad5d2b890d92e4033ecffa64272831139a54e158c254b09f02ce30dbe0a86a8`).
  The probe wrote only under `/tmp` and is not final proof.
- A deeper direct-porcelain audit found `.config/checks/secrets/README.md` dirty
  in a foreign Work Lane with no valid lease. That file is excluded from this
  Change. The remaining carrier and implementation paths have no exact dirty
  foreign-path intersection; the clean historical staged-secret lane remains a
  semantic donor only and is not mutated.
- During re-evaluation, `candidate/dev` first advanced from the initial baseline
  to `e3ed55ec0ae1603ef9368eafeeed3aff67282608`, then to
  `1fca210fd3550bcd62d127a3f9910ed1ff80523c`. At the
  `2026-07-19T02:48:44Z` observation, `main`, `dev`, and `candidate/dev` had
  converged at `1fca210fd3550bcd62d127a3f9910ed1ff80523c`, while this lane
  remained at `cccb124e17689721053066aea02e52acbd9bd2dd` (`dev...lane =
  11/1`). These are time-bound observations; the lane remains authoring-admitted
  but must refresh its base and re-observe all heads before land.
- Post-correction lifecycle checks returned OpenSpec `clean`, claim validation
  `clean`, changed-path plan `planned`, and an empty `git diff --check`. The
  not-yet-created runner remains intentionally absent from promotion targets
  until implementation creates that path and evidence refresh can validate it.

## Planned Proof

- Red/green runner tests for missing tool, version mismatch, exact command
  arguments, exit propagation, and clean success.
- Static ordering plus executable hook regressions proving scanner failure
  prevents Ruff and ordinary write admission while a clean scan preserves both.
- Focused pytest, shell lint, claim validation, strict OpenSpec lifecycle,
  changed-path plan, parity when required, and HEAD-bound executed proof.

## Stop Conditions

Stop mutation if the lane lease, accepted/candidate base, editor root, declared
scope, or target-path overlap becomes stale. Do not mutate any foreign lane, do
not auto-install gitleaks from the hook, and do not infer remote or hosted
success from local proof.
