# Staged Secret Admission V2 — Local Implementation Record

## Boundary

This dated record covers the local implementation commit
`80a7fae4eed9fc10ab3ebc38d8b056d3e83ae0e3` (parent
`23c8a67f6ab9cab509f2a9752e58951ce7568dc8`) in the owned Work Lane
`work/staged-secret-admission-v2-20260719`.

It records the bounded staged-index runner, hook integration, red/green results,
stable-HEAD focused verification, and independent diff review. It does not claim
strict post-refresh lifecycle completion, parity, archive, HEAD-bound proof,
candidate landing, accepted-root closeout, lane retirement, remote publication,
or hosted CI.

## Reproduced Facts

- The accepted pre-commit hook checked staged Python formatting and ETHOS write
  admission but did not scan the staged index for secrets.
- The full secret gate scans a tracked-tree mirror and Git history, installs or
  locates gitleaks, and writes quality evidence; it is not a commit-time path.
- The historical staged-secret lane invokes inline `gitleaks protect --staged`.
  Gitleaks 8.30.1 omits `protect` from its advertised top-level command list but
  still accepts it as an unadvertised compatibility command; `gitleaks git
  --help` explicitly documents `--staged` as the current pre-commit interface.
- A local scratch probe with a high-entropy synthetic GitHub-token-shaped value
  showed both unadvertised `protect --staged` and advertised `git --staged`
  returning the configured finding exit while `--redact=100` kept the value out
  of stdout and stderr. The non-authoritative scratch summary is
  `/tmp/ethos-reeval-live-20260719/gitleaks-interface-probe-summary.json`, SHA-256
  `9ad5d2b890d92e4033ecffa64272831139a54e158c254b09f02ce30dbe0a86a8`.
- `.config/checks/secrets/README.md` is dirty in a foreign Work Lane without a
  valid lease. It was excluded and was not modified.
- The lane refreshed to baseline
  `1fca210fd3550bcd62d127a3f9910ed1ff80523c` before implementation. Protected
  refs are concurrent live state and must be re-observed before proof or land.

## Implemented Contract

Commit `80a7fae4eed9fc10ab3ebc38d8b056d3e83ae0e3` changes exactly:

- `.githooks/pre-commit`
- `tools/ci/scripts/run-staged-secrets-scan.sh`
- `tests/unit/cli/test_hooks_runtime.py`
- `openspec/changes/staged-secret-admission-v2-20260719/tasks.md`

The repository-owned runner requires a local `gitleaks` reporting exactly
version `8.30.1`, then executes:

```text
gitleaks git --staged --config <root>/.gitleaks.toml --redact=100 --no-banner <root>
```

A missing or mismatched binary exits 1 with stable non-secret diagnostics. The
scanner exit is propagated unchanged. The hook invokes the runner after the
unchanged empty-index guard and before Ruff and `ethos.cli hook admit pre-tool`.
No installation, network access, report write, tracked-tree mirror, history
scan, retry, fallback, or credential-governance invocation was added.

## Red, Green, And Review Evidence

- TDD RED command:
  `tools/ci/scripts/with-python-runtime.sh -- uv run --all-packages --group dev pytest -q tests/unit/cli/test_hooks_runtime.py`.
  Result: exit 1, `7 failed, 4 passed in 3.10s`. Raw output:
  `/tmp/ethos-staged-secret-v2-red.txt`, SHA-256
  `fd5711c628d5a2ebe3230967e8b8654db60bf52656b39ccb42fff1c2b1bea617`.
- Fresh stable-implementation-HEAD verification on
  `80a7fae4eed9fc10ab3ebc38d8b056d3e83ae0e3` returned `11 passed in 9.17s`;
  repository-configured Ruff format reported `1 file already formatted`; Ruff
  check reported `All checks passed!`; `bash -n` and focused shell lint passed.
  HEAD was identical before and after and the worktree remained clean. Raw
  output: `/tmp/ethos-staged-secret-v2-stable-head-verification.txt`, SHA-256
  `ea1a9a32eb9dff02bbe121293196f1f7c3fcd268f40d810e94ff0c70c9c4f134`.
- The real commit hook used local gitleaks 8.30.1, then repository-configured
  Ruff and ETHOS admission, without bypass. The implementation report is
  `/tmp/ethos-staged-secret-v2-task-1-report.md`, SHA-256
  `3bf77c7f298d43c5c1c4e18284e2db913c621b9dc1ebb2885bcd6d250c384a13`.
- Independent review approved the supplied brief, report, and
  `23c8a67f..80a7fae4` diff with no Critical, Important, or Minor findings. The
  review did not rerun tests or inspect other repository state. Review:
  `/tmp/ethos-staged-secret-v2-task-1-review.md`, SHA-256
  `703de7a8534ea5b211f802de65b851e65b471b34270c70ba246478fc0c771fd2`.

## Governed Closeout Boundary

Claim/lifecycle/changed-path validation is rerun after this carrier refresh.
Task 3.2 remains open for strict lifecycle, generic parity, and official archive.
Task 3.3 remains open for HEAD-bound executed proof, candidate landing,
accepted-root closeout, and holder-bound retirement.

Task 3.4 is an explicit deferral boundary: this work performs no remote probe,
no push, no release, and no hosted-CI observation or success claim.

## Stop Conditions

Stop mutation if the lane lease, candidate base, editor root, declared scope, or
target-path overlap becomes stale. Do not mutate any foreign lane, do not weaken
the fail-closed scanner boundary, and do not infer remote or hosted success from
local evidence.
