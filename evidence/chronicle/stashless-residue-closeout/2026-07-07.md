---
subject: ethos:stashless-residue-closeout
role: evidence
state: active
relations:
  supports: stashless-residue-closeout
---

# Stashless Residue Closeout Evidence - 2026-07-07

## Boundary

ETHOS no longer treats `git stash` as an admissible backup, handoff, residue, or
closeout carrier. Dirty work must either become visible repository truth through
an owned Work Lane or be reverted from the protected root after classification.
Observation-only stash reads remain available for forensics.

## Verification

Focused verification for this lane:

```bash
uv run --group dev pytest tests/unit/coverage/test_cli_admission_final_edges.py -q
.config/ci/scripts/run-repository-hygiene.sh
.config/ci/scripts/run-shell-lint.sh .config/ci/scripts/install-lychee.sh .config/ci/scripts/run-repository-hygiene.sh
uv run --group dev pytest tests/architecture/test_release_assets.py -q
openspec validate --all --strict
```

Final proof must be rerun after this record is committed.

## Archive Closeout Note

The OpenSpec archive operation initially replaced the existing context-bound
mutation admission scenarios in the live spec. The final closeout fused the
new stashless scenarios with the prior implicit-root, manual-prewrite, and
worktree-root-binding obligations so archive application did not silently delete
accepted requirements.
