---
subject: quality:adoption-retirement-test-matrix-compression
state: active
---

# Adoption-Retirement Test-Matrix Compression — 2026-07-23

## Baseline

- Work Lane: `work/20260723-t8-adoption-retirement-test-compression`.
- Base HEAD: `f7bcf7f4bc8fe0224da7738b0975f0a174d8edcb`.
- Scope: `tests/unit/adoption/test_retirement.py` and
  `tests/unit/adoption/retirement/fixtures.py`.
- Formatter-clean `scc --dryness` baseline: **968 Code**, **506 ULOC**,
  **462 repeated-line approximation**, and **1,125 physical lines**.

## Cutover

- Finite profile, backend-control, lifecycle, drift, CLI, and rollback
  partitions use literal domain-named matrices while Git, filesystem, CLI, and
  rollback-manifest effects remain directly observable.
- One comprehensive rollback manifest covers mismatch, missing-field,
  out-of-root, missing-path, untracked-evidence, unreachable-head, and missing
  scenario behavior without parallel fixture implementations.
- The existing fixture module uses `tomli-w` and shared inert clean envelopes;
  no product code, dependency, compatibility path, suppression, or generated
  test source was added.
- Final scoped measurement: **668 Code**, **496 ULOC**, **172 repeated-line
  approximation**, and **740 physical lines**. Relative to baseline this is
  **-300 Code**, **-10 ULOC**, **-290 repeated lines**, and **-385 physical
  lines**.
- Repository source budget is `python_total=95,800` and
  `global_total=112,279`, respectively **296** and **242** below the accepted
  base. The terminal targets remain unmet by **37,078 Python** and **38,540
  global** ELOC; this slice does not claim campaign completion.

## Current verification

- Focused warnings-as-errors result: **16 passed**.
- Complete adoption result: **52 passed** with the retirement package at
  **100% statements and branches** (`247` statements, `58` branches).
- Scoped Ruff format/check passed; the repository Ruff ratchet returned to its
  declared `PLR0913 29/29` ceiling without an exemption or budget increase.
- Ponytail review removed two single-use fixture wrappers, narrowed the rollback
  fixture to its exercised contract, and collapsed safe literal repetition; no
  unresolved deletion or YAGNI blocker remains in the scoped diff.
- Strict OpenSpec validation passed for all **10/10** current items.
- Source-budget evaluation returned `ok=true`, `state=advisory`, and no required
  gaps.
- Generic shadow parity matched with zero required gaps and zero false
  negatives; its tracked evidence was committed before proof execution.
- The clean pre-archive HEAD `cef98b203a13978813c57bf7f704919f6d826a99`
  passed the executed **28-gate full proof** with evidence digest
  `9d2a33061ed570f34ffb471acfb47075da64a0237723ee55d2d03f99f23e2e22`.
- A pre-binding full test run reached **2,665 passed** and **100% repository
  coverage**; its ten failures were all the expected
  `openspec_claim_binding_missing:t8-adoption-retirement-test-matrix-compression`
  lifecycle gap. After claim binding, the full owner script passed with
  **2,675 tests** and **100% line and branch coverage**. Independent review then
  tightened shadow-effect discrimination, public rollback-gap propagation, and
  malformed-profile fail-closed coverage. Archive mutates the carrier and
  canonical specification, so archive-HEAD parity, exact-HEAD proof, candidate
  land, accepted-root closeout, and retirement remain separate required steps.

## Boundary

This Change modifies test representation and its evidence carrier only. It does
not change retirement product behavior, profile or rollback schemas, public
JSON, dependencies, quality thresholds, remote state, or hosted CI truth.
