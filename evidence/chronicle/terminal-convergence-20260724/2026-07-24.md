---
subject: ethos:terminal-convergence-campaign-carrier
role: chronicle
state: active
---

# Terminal Convergence Campaign Carrier

## Fact

The owned Work Lane `work/20260724-terminal-convergence` was started from
`candidate/dev` at `c94670980d96fd08537a2b30bf8abaf249eabb91`. The canonical
terminal design was replaced with one terminal product contract, and the active
OpenSpec change `terminal-convergence` contains proposal, design, tasks, scope,
and nine capability deltas.

## Evidence

- `openspec status --change terminal-convergence --json` reported all planning
  artifacts present.
- `openspec validate terminal-convergence --strict --json` reported one valid
  change and zero issues.
- `ethos lane prewrite` admitted the design, OpenSpec, claim, and Chronicle
  paths for the current holder, lease generation, editor root, and HEAD.

## Boundary

This record proves only that the approved terminal campaign has a governed,
strictly valid design and planning carrier in the owned Work Lane. It does not
prove any implementation task, terminal source budget, full test suite,
provider CI/CD, landing, publication, release, or campaign closeout.

## Next

Bind the active claim to the Work Lane, commit this recovery anchor, then execute
Phase 0 truth repair followed by deletion-first convergence.
