---
subject: quality:declarative-cli-contract-fixture-compression
state: active
---

# Declarative CLI-Contract Fixture Compression — 2026-07-14

## Baseline and cutover

- Work Lane: `work/test-contract-declarative-compression-20260714`.
- Scoped formatter-clean ELOC: **1,619 -> 1,296** (**-323**).
- The scope is `tests/support/contract_helpers.py`,
  `tests/unit/cli/test_contracts_land.py`, and
  `tests/unit/governance/validation/test_schemas.py`.
- The helper adds **51** effective lines while deleting **148** repeated CLI
  lifecycle lines and **226** duplicated schema-envelope lines; the scoped
  result remains a net deletion.

## Preserved contracts

- Each land and publish test still owns its command invocation, expected state,
  required gaps, and payload assertions.
- The typed helper creates only literal adopted-repository topology and commits
  literal fixture files; it does not classify or normalize product payloads.
- The schema acceptance case uses the canonical complete workspace-status
  sample. The rejection case changes only the forbidden UI field.

## Focused verification

- `tests/unit/cli/test_contracts_land.py`: **19 passed**.
- `tests/unit/governance/validation/test_schemas.py`: **24 passed**.
- Scoped Ruff format and lint checks passed.
- Strict OpenSpec validation passed before claim binding; full parity and
  HEAD-bound proof remain required before promotion.

## Boundary

This changes test representation only. It does not alter production lifecycle
behavior, command payloads, schema semantics, dependencies, quality thresholds,
or remote publication policy.
