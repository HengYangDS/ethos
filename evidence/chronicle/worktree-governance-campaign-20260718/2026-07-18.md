# Repo-First Worktree Governance Campaign Bootstrap — 2026-07-18

## Claim Boundary

This record covers only the bootstrap of the dedicated `repo-first-worktree-governance-v2` campaign and its read-only, selector-bounded campaign-closeout surface. It does not claim that any recovery package, preservation record, deletion admission, worktree movement, worktree retirement, branch deletion, foreign-lane resolution, remote publication, or hosted verification has completed.

## Authority Boundary

- **Git:** committed history, refs, patches, and linked-worktree registration.
- **ETHOS:** policy, ownership, admission, lifecycle gates, receipts, and local-closeout evidence.
- **Future backup:** may back up sealed records independently; it is not a lifecycle or retirement authority.

The frozen topology for subsequent slices is repository-adjacent: `/Users/yheng/projects/<repo>-worktrees/<yyyymmdd>-<task-slug>` and `/Users/yheng/projects/<repo>-records/{.staging,evidence,recovery}/<timestamp>-<purpose>`. State labels belong in manifests, not topology.

## Recorded Implementation

- Added a dedicated strict-serial campaign with one active bootstrap and nine planned successor slices.
- Added `--campaign` to the read-only `ethos campaign closeout` command and selected campaign report path.
- Added focused regressions for a selected active campaign and an unknown selector.
- Added a repository-governance delta and scoped documentation for independent campaign authority.

## Pre-Commit Verification

The following focused verification ran on the implementation tree before its first commit:

- `tools/ci/scripts/with-python-runtime.sh -- uv run --all-packages --group dev pytest -q tests/unit/cli/test_contracts_proof.py::test_campaign_closeout_scopes_to_selected_campaign tests/unit/cli/test_contracts_proof.py::test_campaign_closeout_unknown_selector_reports_gap tests/unit/cli/test_contracts_proof.py::test_campaign_closeout_reports_local_campaign_packages tests/unit/governance/validation/test_schemas.py::test_campaign_schema_accepts_lane_closeout_steps` passed: 4 tests.
- `openspec status --change repo-first-worktree-governance-campaign-20260718 --json` reported all four planning artifacts complete.
- `tools/ci/scripts/run-ethos-lane.sh lane prewrite ... --json` admitted every tracked bootstrap path under the owned Work Lane and named holder.

These are pre-commit checks only. After evidence binding was refreshed, strict OpenSpec lifecycle validation and claim validation were rerun and passed on the implementation tree. The HEAD-bound proof remains intentionally pending until the bootstrap content is committed; it is not represented here as completed local proof. No uncommitted result is presented as final local proof.

## Temporary Publication Boundary

On 2026-07-18, the user authorized GitHub-only publication while the configured
GitLab plane is unreachable from the current network. A fresh read-only probe
returned a connection-reset failure for the configured GitLab Git remote and a
timeout for its HTTP health endpoint. The GitHub remote was reachable and its
`dev` and `main` refs matched the accepted baseline before this bootstrap.

Accordingly, after a successful accepted-root local closeout, this campaign may
publish the exact accepted Git refs to GitHub only. GitLab is neither pushed nor
rewritten; its provider status remains unavailable until a fresh recovery probe
succeeds. GitHub publication is an authority-specific receipt and does not prove
GitLab synchronization, GitLab CI, a GitLab release, or dual-forge completion.

## Non-Actions

- No foreign, unbound, unknown, App-managed, or runtime-managed lane was modified.
- No GitLab runner or active proof/test process was stopped or repurposed.
- No credentials, sessions, databases, `.env` content, or runtime-private data were captured.
- No remote, release, tag, branch cleanup, worktree prune, filesystem removal, or Git stash operation was performed.
