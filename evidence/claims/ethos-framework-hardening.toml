[claim]
id = "ethos-framework-hardening"
subject = "ethos:product"
state = "active"
summary = "Harden ETHOS from bootstrap skeleton into a verifiable framework core."

[evidence]
dated = "evidence/chronicle/hardening/2026-06-30.md"
sha256 = "ccc626c5ee7051763462a50cd99dfa49c036cd5eff45db13df6767a0fb6d90a5"
evidence_ids = ["evidence:ethos-framework-hardening"]
binding = "digest-bound evidence binding"
verifier = "digest_only"
tests = [
  "tests/unit",
  "tests/architecture",
]

[evidence.freshness]
mode = "historical"
[boundary]
owner = "ethos-repository"
scope = "Framework proof, evidence, docs, assistants, and mutation contracts"

[carriers]
openspec = "openspec/specs/repository-governance/spec.md"

fallback = "Keep local deterministic proof gates and schema validation as the rollback path."
kill_signal = "Framework proof depends on untracked host or assistant state."

[promotion]
targets = [
  "evidence/chronicle/hardening/2026-06-30.md",
  "system/schemas/kernel/proof-run.schema.json",
  "system/schemas/kernel/evidence-set.schema.json",
  "openspec/specs/repository-governance/spec.md",
]
