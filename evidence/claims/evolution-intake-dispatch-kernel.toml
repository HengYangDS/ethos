[claim]
id = "evolution-intake-dispatch-kernel"
subject = "ethos:evolution-intake-dispatch-kernel"
state = "active"
summary = "ETHOS records a planned Agentic Evolution Kernel design that keeps external sensors, issue trackers, managed-agent platforms, automation canvases, and coding-agent runtimes as adapters while preserving repository truth, admission, proof, claims, chronicle, and upstream guardrail promotion as the governance center."

[evidence]
dated = "evidence/chronicle/agentic-evolution-kernel/2026-07-10.md"
sha256 = "1ec8577c43883d86032b89ff136f775739409b099eca43dc25b255087260f60a"
evidence_ids = ["evidence:evolution-intake-dispatch-kernel"]
binding = "Refined terminal organ model and boundary evidence for agentic intake, issue projection, dispatch, proof, chronicle, and upstream guardrail promotion"
verifier = "semantic"
readiness_checks = [
  "ETHOS_ACTOR=codex uv run --package ethos ethos status --root <work-lane-root> --json",
  "ETHOS_ACTOR=codex uv run --package ethos ethos playbooks route --changed --root <work-lane-root> --mode v2-strict --json",
  "ETHOS_ACTOR=codex uv run --package ethos ethos plan --changed --root <work-lane-root> --json",
  "ETHOS_ACTOR=codex uv run --package ethos ethos quality docs --root <work-lane-root> --json",
  "ETHOS_ACTOR=codex uv run --package ethos ethos openspec --lifecycle --root <work-lane-root> --json",
  "ETHOS_ACTOR=codex uv run --package ethos ethos quality claims --root <work-lane-root> --json",
  "ETHOS_ACTOR=codex uv run --package ethos ethos prove --root <work-lane-root> --json",
  "ETHOS_ACTOR=codex uv run --package ethos ethos prove --execute --expect-head <work-lane-head> --root <work-lane-root> --json",
  "ETHOS_ACTOR=codex uv run --package ethos ethos report --root <work-lane-root> --json",
]

[boundary]
owner = "ethos-governance"
scope = "Design-only boundary for sensors, agentic issue intake, managed-agent platforms, execution backends, evidence binding, chronicle judgment, and bounded repository evolution guardrails"

[carriers]
openspec = "openspec/changes/archive/2026-07-09-agentic-evolution-kernel"

fallback = "Keep external sensors and agentic tools as manual context, signal sources, and execution aids until issue projection, dispatch admission, result import, and guardrail promotion are implemented in later Work Lanes."
kill_signal = "An external sensor finding, board, issue state, agent assignment, task completion, pull request, merge request, or agent narrative is treated as repository truth, mutation authority, proof, publication, chronicle, or upstream guardrail without ETHOS admission, evidence, and claim binding."

[promotion]
targets = [
  "docs/plans/agentic-evolution-kernel.md",
  "openspec/changes/archive/2026-07-09-agentic-evolution-kernel/.openspec.yaml",
  "openspec/changes/archive/2026-07-09-agentic-evolution-kernel/proposal.md",
  "openspec/changes/archive/2026-07-09-agentic-evolution-kernel/design.md",
  "openspec/changes/archive/2026-07-09-agentic-evolution-kernel/tasks.md",
  "openspec/changes/archive/2026-07-09-agentic-evolution-kernel/specs/repository-governance/spec.md",
  "evidence/chronicle/agentic-evolution-kernel/2026-07-09.md",
  "evidence/chronicle/agentic-evolution-kernel/2026-07-10.md",
  "evidence/claims/evolution-intake-dispatch-kernel.toml",
]
