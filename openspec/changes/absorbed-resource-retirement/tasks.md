## 1. Prove The Boundary

- [x] 1.1 Add public-command and installed-hook regressions for absorbed topic
  refs and clean linked topic worktrees; observe RED.
- [x] 1.2 Preserve root-resource, dirty-state, Lease, stale-CAS, and raw-delete
  rejection with concrete state assertions.

## 2. Replace The Incorrect Eligibility

- [x] 2.1 Reuse one branch-role distinction in ref and linked retirement and the
  reference-transaction transport; retain existing effects and recovery.
- [x] 2.2 Update command projection and the existing terminal route with current
  closeout evidence and remaining historical-lane dispositions.
- [x] 2.3 Run focused tests, affected static checks, and reference closure without
  restoring historical Commitment, prefix exceptions, or a cleanup registry.

## 3. Verify The Source Atom

- [x] 3.1 Freeze a signed exact HEAD and pass the complete repository proof before
  official archive. Post-archive acceptance and resource retirement remain in
  the existing terminal route until their independent evidence is observed.

## Verification Observations

On 2026-09-07, the initial public retirement regressions produced six failures
and one pass; the historical-worktree identity regression separately failed
after removal while constructing its Attestation. The corrected affected set
passed 236 tests, including installed hooks, retirement recovery, exact-CAS,
Lease boundaries, and native worktree failure cases. Five additional Git-effect
identity and invalid-profile checks passed. Ruff check, format check, source ty,
and diff whitespace checks passed.

The first affected run's 22 OpenSpec failures were an unprovisioned test-process
supply, not failing product assertions. Reusing the existing lock-validated Node
supply resolved them without installation or a new cache. Its one remaining
failure used an incomplete policy substitute; using the real BranchRolePolicy
preserved the committed-intent failure assertion. All test roots from these
verification runs were removed on exit. Local logs are under
`build/evidence/debug/retirement-*`; they are diagnostic observations, not
exact-HEAD proof, accepted-runtime evidence, or historical-resource retirement.

The first full proof at `0a5c23b6` blocked: 18 tests failed because its temporary
root was inside the checkout and non-repository fixtures discovered the parent
Git repository. All 18 passed unchanged with an external isolated temporary
root. Five synthetic ref-intents were identified by exact bytes and nonexistent
test OIDs, recorded, and removed through the existing intent owner; real refs
remained unchanged. Full proof must use a temporary root outside every worktree.

The same run exceeded the independent test-source limit by 122 ELOC. Five
repetitive landed-plan tests were replaced by one real Git/Lease matrix covering
valid, expired, and missing Lease state. It preserves exact facts and no-proof
assertions and now executes admission for every state. Linked-topic CLI cases
share one setup while retaining all six outcomes. This removes 138 test ELOC;
the 38,300 limit remains unchanged and measurement is 38,284. The revised
affected set passed 234 tests. The two-case count reduction reflects five
plan tests becoming three matrix cases, not removal of a required invariant.
Revised signed HEAD `1d603d0a8b64c172c3cb2c7790e34fffbadef25c` passed all 25
gates: 2,102 tests passed with one platform skip and total line/branch coverage
93.10%, above the unchanged 93% floor. Proof Attestation
`6fac9d8372de08f9a63050e10c8a3d51f1b250f9c5ff93e07e8a8e22eeae5849`
binds that exact HEAD. The external test root was removed and real ref-intent
storage remained empty. This completed task records that proved source; the
task-record commit, archive, and accepted/runtime transitions still require
their own current evidence.
