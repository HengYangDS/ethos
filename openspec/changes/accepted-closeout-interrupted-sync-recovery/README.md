# accepted-closeout-interrupted-sync-recovery

Recover only the exact accepted-worktree residue left after a native closeout
has already promoted the accepted ref but its Git worktree synchronization was
interrupted by a verified stale index lock.
