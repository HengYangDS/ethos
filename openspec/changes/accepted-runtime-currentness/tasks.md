## 1. Contract and RED

- [x] 1.1 Record the single source-currentness invariant and forward-only manifest migration.
- [x] 1.2 Add RED coverage for an intact runtime from an older source identity and one exact repair action.
- [x] 1.3 Add RED coverage that package-only installation reads build identity without a source checkout.

## 2. Single owner implementation

- [x] 2.1 Add exact source commit/tree to the existing non-editable wheel build data.
- [x] 2.2 Bind source identity into the one runtime digest and schema-2 manifest.
- [x] 2.3 Compare the runtime with accepted-source or package-source expectation in the existing hook binding.
- [x] 2.4 Keep every mutation consumer on that binding and delete the integrity-only authorization path.
- [x] 2.5 Project one fully bound repair action and post-verify it through `ethos hook install`.

## 3. Verification and closeout

- [x] 3.1 Run focused hook runtime, failure-branch, lifecycle-retirement, and isolated-wheel tests.
- [x] 3.2 Run type, Ruff, module/import, schema, and strict OpenSpec gates.
- [x] 3.3 Run stale-generation to repair to post-verification as a real Git black-box case.
- [ ] 3.4 Run full proof, archive, land, accepted closeout, and retire with exact-HEAD evidence.
