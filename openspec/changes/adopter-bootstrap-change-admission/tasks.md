## 1. Runtime-owned Work Lane bootstrap

- [x] 1.1 Add a failing public regression proving `lane start` returns the selected immutable package-runtime command in an adopter without an `ethos` project entrypoint, then implement the minimal runtime-owned projection and verify the focused lane-start suite passes.

## 2. Exact first-Change admission

- [x] 2.1 Add a failing public regression for an exact absent `openspec/changes/<change>` request, then implement the minimal scope reduction that returns the exact `.openspec.yaml` prewrite command without directory authority or archived fallback and verify the focused admission suites pass.

## 3. Contract and closeout

- [x] 3.1 Update the canonical repository-governance requirements, run strict OpenSpec validation, changed-scope planning, focused tests, formatting and affected gates, and record package-only adopter evidence for both repaired paths.
