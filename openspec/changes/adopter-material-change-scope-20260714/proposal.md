## Why

An active Change alone must not authorize unrelated material adopter paths.
The universal lifecycle repair is intentionally delivered first because the
current source-budget policy cannot admit the full portable scope reader in the
same lane without unrelated repository-wide compression.

## What Changes

Design and implement a portable declarative material-path to active/archiving
OpenSpec Change companion binding in a separately governed Change.

## Boundary

This carrier is planned follow-up work. It does not alter current lifecycle,
code-correctness gates, official OpenSpec schema, or method-package authority.

## Capabilities

- `repository-governance`: subject=adopter-material-change-scope; reuse=extend;
  change=modify; facet:lifecycle=authoring,validation; facet:surface=profile,hook,
  openspec,test,docs; facet:authority=source,test,openspec,claim,evidence

## Out Of Scope

The universal adopter lifecycle correction, official OpenSpec schema changes,
code-correctness gate substitution, and method-package authority.
