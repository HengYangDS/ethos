## 1. Contract and regression

- [x] 1.1 Define the continuation-driven thin-entrypoint behavior in the
  assistant-projections delta and verify the Change with
  `openspec validate agent-entrypoint-convergence --strict --json`.
- [x] 1.2 Add a focused architecture regression that verifies the positive
  entry contract.
- [x] 1.3 Add a focused admission regression proving that remaining official
  Change artifacts stay writable after partial Commitment compilation.

## 2. Projection convergence

- [x] 2.1 Replace `AGENTS.md` with the minimal current-state router and verify
  its links and commands through the existing documentation gates.
- [x] 2.2 Align `rules/agents.md` and the repository-governance skill with the
  current result, and delete the duplicate Change-lifecycle skill.
- [x] 2.3 Remove fixed five-command lifecycle narration from active product,
  command, adopter, forge, and discovery documentation; delete the remaining
  handwritten driver; update package digests; and verify `playbooks-v2`.
- [x] 2.4 Make official bootstrap admission independent of partial Commitment
  availability and verify the exact artifact-only boundary.

## 3. Verification

- [x] 3.1 Run focused tests, strict OpenSpec validation, current changed-scope
  planning, and the selected focused gate for the completed implementation.
