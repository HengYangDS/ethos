# Tasks

## 1. Exact Archive Consumer Repair

- [x] 1.1 Reproduce the post-archive public prewrite block with a tracked Markdown link, then verify code-fenced, unrelated, missing-target and absent-Attestation counterexamples.
- [x] 1.2 Reuse the existing archive reference parser and material-scope owner to admit only the requested consumer; verify exact patch admission and the native pre-commit hook.

## 2. Qualification

- [x] 2.1 Replay the selected repair paths read-only against the adopter's exact archived HEAD; verify strict OpenSpec validation, focused lifecycle tests, Ruff, types, size and Markdown gates without adopter mutation.
