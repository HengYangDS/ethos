# Advisory Signal Readmodel

## Problem

ETHOS can detect non-blocking small signals such as protected branch OpenSpec
residue, but if those signals only live deep inside command payloads they remain
hard for humans and agents to discover during first-glance governance.

## Change

Expose advisory signals through the report scorecard summary, gap layers, and
orientation reader view without promoting them to blocking required gaps.
