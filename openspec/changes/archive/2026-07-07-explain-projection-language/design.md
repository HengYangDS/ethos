# Design

Only reader-facing projection language changes. The command semantics, taxonomy,
result fields, and transition loop remain unchanged. The old `gap` parameter name
stays for compatibility; docs describe it as `<gap-or-signal>` so users do not
infer that advisory signals are required gaps.
