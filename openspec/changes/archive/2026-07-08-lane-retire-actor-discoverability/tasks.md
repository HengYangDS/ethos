# Tasks

- [x] Reproduce a leased landed-lane retirement blocked by missing actor binding.
- [x] Expose `actor_source`, `actor_bound`, and `required_actor` in the mutation payload.
- [x] Emit a bounded next action for actor-authority retirement blocks.
- [x] Preserve owner-only retirement semantics and existing head-bound deletion rules.
