# Tasks

- [x] Add dependency hygiene config, runner, CI/template projection, runbook row,
  and tests.
- [x] Add prose spelling config, runner, CI/template projection, runbook row, and
  tests.
- [x] Add JSON Schema metaschema config, runner, CI/template projection, runbook
  row, and tests.
- [x] Add hosted provider observation config, runner, evidence envelope, CI/template
  projection, runbook row, and tests.
- [x] Extend tool profiles with active adapters and optional Nox/Pixi/Pants/task
  ledger/agent method-pack boundaries.
- [x] Keep vulnerability scanners planned with an explicit non-trust-bearing
  boundary record.
- [x] Run focused gate and architecture tests.
