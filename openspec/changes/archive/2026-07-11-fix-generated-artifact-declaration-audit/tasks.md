## 1. Regression Contract

- [x] 1.1 Add an adopter-shaped failing test where cleanup, ignore, and exclusion declarations contain denied-home tokens.
- [x] 1.2 Add a negative-control test where an executable Pixi task writes to a denied home and remains blocked.

## 2. Parser Correction

- [x] 2.1 Extract executable producer commands from supported structured manifest task tables.
- [x] 2.2 Keep shell and CI entrypoint checks unchanged and preserve malformed-input fail-closed behavior.

## 3. Verification And Integration

- [x] 3.1 Run focused generated-artifact unit and CLI tests.
- [x] 3.2 Re-run alphasim generated-artifact audit against the corrected product runner.
- [x] 3.3 Run head-bound ETHOS proof, land to `candidate/dev`, and retire this Work Lane without remote publication.
