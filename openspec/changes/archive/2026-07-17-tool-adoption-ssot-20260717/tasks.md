# Tasks

- [x] 1. Add red regressions for catalog-derived tool profiles and required
  adoption states.
- [x] 2. Replace the static Python adapter table with a root-bound TOML catalog
  projection and route quality commands and schema validation through it.
- [x] 3. Declare adoption states in `system/tools.toml` and enforce the
  required contract fields in JSON Schema.
- [x] 4. Pass focused CLI, kernel, and schema tests plus configuration lint and
  focused Ruff checks.
- [x] 5. Refresh parity, run HEAD-bound proof, bind final claim/Chronicle
  evidence, archive the change, and prepare candidate landing.
