## 1. Protected runner selection

- [x] 1.1 Classify an `accepted_ff` release mirror as a protected prepared
  transition alongside the accepted branch.
- [x] 1.2 Preserve the incumbent fail-open path for independent release branches
  and ordinary refs.

## 2. Regression proof

- [x] 2.1 Strengthen the real armed-hook integration test: raw `dev` and `main`
  moves block, while a sanctioned atomic closeout succeeds after the incumbent
  semantic reducer is made unusable.

## 3. Authoring closeout readiness

- [x] 3.1 Run focused hook tests, strict OpenSpec validation, and claim
  validation for the implementation tree.
- [x] 3.2 Prepare this carrier for official archive; candidate landing,
  accepted-root closeout, and the later HEAD-bound proof remain independent
  lifecycle transitions.
