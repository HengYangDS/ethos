## 1. Campaign State

- [x] 1.1 Reconcile the completed bootstrap as a retired campaign step with its exact local closeout evidence.
- [x] 1.2 Activate only the retirement fail-closed successor and bind its claim and OpenSpec carrier.

## 2. Ordinary Retirement Safety

- [x] 2.1 Reobserve selected linked-lane path, ref, HEAD, and dirty state immediately before effect.
- [x] 2.2 Remove linked worktrees without force before the head-bound ref deletion.
- [x] 2.3 Block ordinary unbound-ref deletion pending exceptional deletion admission.

## 3. Verification And Closeout

- [x] 3.1 Add focused dirty, stale-observation, partial-transition, and unbound-preservation regressions.
- [x] 3.2 Run focused tests, lint, OpenSpec lifecycle validation, claim validation, and parity refresh for the implementation carrier.
- [x] 3.3 Archive the completed carrier.
- [x] 3.4 Record the mandatory post-archive transition sequence: run a fresh exact-HEAD proof, land to the local candidate, and obtain candidate-external accepted-root closeout without treating this archived checklist as evidence that those later effects already occurred.
- [x] 3.5 Record that only the current lease holder may retire this owned lane after an independent local closeout receipt exists, and that the successor carrier must reconcile the resulting retired state with exact candidate and accepted heads.
- [x] 3.6 Repair the post-archive full-proof regressions: shrink the stale ignored-rule ratchet after an in-scope unused parameter was removed, and update the retired lifecycle edge fixture to model the required immediate reobservation before a non-forced worktree removal.
- [x] 3.7 Update the remaining retirement edge fixture so its mocked ref, checkout HEAD, and clean status satisfy the new effect-time reobservation contract before exercising worktree-removal failure and head-bound ref-deletion failure.
- [x] 3.8 Remove the now-unreachable ordinary unbound-ref deletion effect and lease revocation path; the command has no destructive branch until the later exceptional deletion-admission slice owns it.
