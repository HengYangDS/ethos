## 1. Exact source judgment

- [x] 1.1 Re-observe the exact source head, linked dirty paths, missing lease,
  and accepted-ancestor relation.
- [x] 1.2 Record only current-contract-compatible helper-script output routing;
  retain current quality-audit behavior required by focused tests.
- [x] 1.3 Refresh the four affected skill-package digests.

## 2. Authority carrier

- [x] 2.1 Add the plan, Claim, Chronicle, and OpenSpec delta for one exact later
  native `lane_resolution/preserve-retire` transition.
- [x] 2.2 State that preservation is recovery evidence, not absorption.

## 3. Lifecycle

- [x] 3.1 Validate the carrier, refresh generic parity, and execute exact-HEAD
  proof.
- [x] 3.2 Delimit candidate land, accepted closeout, native source
  `preserve-retire`, and carrier retirement as separately re-observed
  post-archive transitions; do not claim them from this archive task list.

## Post-archive transition boundary

The carrier may proceed only through its own fresh candidate land and accepted
closeout. Only then may a fresh native `lane_resolution/preserve-retire`
decision affect the named source; only after that receipt may this carrier be
retired. Each transition is outside this archived Change's completed authoring
checklist and remains unclaimed until its own receipt exists.
