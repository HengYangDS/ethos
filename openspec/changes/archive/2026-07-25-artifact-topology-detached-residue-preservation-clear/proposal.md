## Why

The exact ownerless artifact-topology residue is absorbed and natively retired,
but its verified preservation package remains. The package is not a cache and
must not be removed with raw filesystem commands. Its payload adds no behavior
beyond accepted truth, so an exact-manifest native clear is now appropriate.

## What Changes

- Bind one decision id, manifest SHA-256, patch SHA-256, prior receipt, and
  accepted behavior comparison.
- Authorize only native `lane_resolution/clear-preservation` after this
  carrier's local accepted closeout.
- Retain the original decision and completion receipt, emit a clear receipt,
  and retire this temporary carrier after the single effect is checked.

## Capabilities

- `repository-governance`: subject=artifact-topology-detached-residue-preservation-clear; reuse=extend; change=modify; facet:lifecycle=validation,archive,retention; facet:surface=docs,openspec,evidence; facet:authority=git,test,openspec,claim,evidence.

## Out Of Scope

- Any other package, branch, worktree, lease, decision, receipt, remote, hosted
  execution, batch clear, raw deletion, or source reconstruction.
