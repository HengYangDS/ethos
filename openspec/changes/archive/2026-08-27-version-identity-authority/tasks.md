## 1. Contract and RED

- [x] 1.1 Specify one product authority, identity layers, unique development
  builds, accepted non-reuse, and public JSON; strictly validate OpenSpec.
- [x] 1.2 Add RED coverage for parallel literals, source-unique builds, no new
  `0.1.0a2`, package-only provenance, and identity conflicts.

## 2. Single authority and projections

- [x] 2.1 Implement `VERSION` with standards-based parsing and derive Python/npm
  projections; remove static manifest owners.
- [x] 2.2 Derive unique work/candidate identities from exact source coordinates;
  verify wheels from two commits differ.

## 3. Provenance and admission

- [x] 3.1 Bind package/runtime identity to source, artifact, interpreter,
  platform, lock, channel, and acceptance; prove package-only inspection.
- [x] 3.2 Enforce release monotonicity/non-reuse and projection agreement across
  local-only and configured Forge topologies with zero-effect rejection.
- [x] 3.3 Emit one UTF-8 human/JSON version projection without double encoding.

## 4. Closure verification

- [x] 4.1 Run focused quality, release/runtime, package-only, schema, and strict
  OpenSpec checks without building a legacy artifact.
- [x] 4.2 Run the full applicable proof once on the frozen implementation HEAD.
  Archive/reproof, candidate/accepted CAS, accepted package-only delivery, and
  Work Lane retirement remain separate governed lifecycle effects proved by
  their own exact receipts.
