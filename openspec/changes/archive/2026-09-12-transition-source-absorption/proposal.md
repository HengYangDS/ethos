## Why

One historical transition lane remains with 388 committed or dirty paths. Its
valid obligations overlap accepted implementations, obsolete authority models
and still-missing capabilities. Whole-branch integration would restore parallel
state; discarding it by file similarity would lose distinct requirements.

## What Changes

- Reconcile the retained proposal, design, specifications and changed source by
  semantic responsibility against the existing product contract and plan.
- Verify retained behavior through current counterpart tests or a minimal
  counterexample; explicitly preserve unimplemented obligations and source.
- Record accepted signature delivery and source retirement in the canonical
  plan, replacing stale present-tense completion statements.
- Produce exact content, recovery and retirement readiness evidence without
  treating source classification as implementation or deletion authority.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

None. This bounded absorption review preserves existing requirements and
classifies historical implementation. Official `skip_specs: true` applies;
new product behavior belongs in a separately scoped implementation Change.

## Impact

Official Change artifacts and the existing canonical terminal plan. Repository
and quality governance own admission and verification. The historical source is
read-only until separately admitted retirement. No new lane, roadmap, permanent
feedback store, adopter mutation, dependency or obsolete model is introduced.
