## Why

Two stable dependency releases appeared after the preceding supply checkpoint.
The user requests current stable supply without weakening verification or
confusing wrapper freshness with the versions embedded in installed runtimes.

## What Changes

- Update the declared filelock minimum and exact lock to 3.32.7.
- Update the existing transitive virtualenv lock to 21.7.10.
- Exercise existing lock, bootstrap, audit and package consumers on these bytes.
- Preserve explicit remaining preview and embedded Node/npm gaps in the
  canonical terminal plan rather than claiming complete supply convergence.

## Capabilities

No new or modified specification requirement. This is a supply refresh under
the existing quality and runtime-activation contracts. `skip_specs: true`
declares that no specification delta is necessary.

## Impact

The native Python declaration and lock, existing verification consumers and
canonical terminal plan. No new dependency, tool owner or adopter mutation.

## Non-Goals

No blanket preview admission, Node packaging redesign, unrelated pending-merge
repair, historic identity rewrite or claim of remote publication.
