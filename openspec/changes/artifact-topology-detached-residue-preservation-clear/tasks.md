## 1. Exact retention boundary

- [x] 1.1 Bind the decision id, package, manifest digest, patch digest, source
  head, and immutable prior receipt.
- [x] 1.2 Confirm the tracked patch matches the pre-effect capture, the index
  patch is empty, no untracked archive exists, and the bundle contains the
  exact source ref with complete history.
- [x] 1.3 Bind the accepted absorption carrier, focused tests, and executed
  proof as the no-unique-behavior basis.

## 2. Clearance authority

- [x] 2.1 Add the single-package Claim, Chronicle, plan, and OpenSpec delta.
- [x] 2.2 Require native `lane_resolution/clear-preservation`, exact manifest,
  non-empty reason, break-glass, and irreversible confirmation while retaining
  the original decision and completion receipt.

## 3. Validation and archive closeout

- [x] 3.1 Run strict OpenSpec, claims, docs, focused behavior, inventory, and
  package-integrity checks.
- [ ] 3.2 Commit, refresh generic parity, and run executed HEAD-bound proof.
- [ ] 3.3 Archive the carrier, refresh parity, rerun proof, refresh base, and
  establish exact land readiness.

## Post-archive transition boundary

After accepted local closeout, native clear may affect only decision
`lane-decision:56a318b4-18e5-4a73-9051-80202eb99576` at manifest
`d4f4f64ad401a4f0131bc5eaa9d40cb267d28c6f41e26a9b94afadb23140a2ed`.
The clear effect, receipt verification, final inventory, and carrier retirement
are later transitions rather than unfinished tasks in the archived Change.
