---
subject: ethos:format-selection-chronicle-inventory-admission-20260716
reuse: extend
change: modify
facet:lifecycle: validation
facet:surface: quality,evidence
facet:authority: config,source,test,openspec,claim,evidence
---

# Format Selection Chronicle Inventory Admission

## Why

The fail-closed format-selection audit rejects the exact curated JSON inventory
that records the frozen Work Lane cohort, because the JSON carrier policy does
not yet name its path. The inventory is tracked Chronicle evidence, not a
generated artifact or a broad exception to the format boundary.

## What Changes

- Admit only the exact convergence inventory JSON as a declared JSON carrier.
- Preserve the default block for all other JSON outside schemas, parity,
  distribution manifests, and this one evidence record.
- Record the policy rationale, focused evidence, and lifecycle boundary.

## Capabilities

- `quality`: subject=format-selection-chronicle-inventory-admission-20260716;
  reuse=extend; change=modify; facet:lifecycle=validation;
  facet:surface=quality,evidence,test,openspec;
  facet:authority=config,source,test,openspec,claim,evidence

## Out Of Scope

- No broad `evidence/chronicle/` JSON allowance.
- No change to JSON formatting, source-budget policy, or generated-artifact
  topology.
- No remote publication or foreign Work Lane mutation.
