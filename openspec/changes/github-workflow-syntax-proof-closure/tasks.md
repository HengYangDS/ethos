## 1. Prove the missing boundary

- [x] 1.1 Extend the existing CI provider architecture contract to require the
  legal repository-owned Python supply coordinate, one `github-workflow-syntax`
  full-proof selection, and the exact existing actionlint owner command; run it
  and verify it fails on the accepted declaration and projection.
- [x] 1.2 Run the existing actionlint owner and retain its exact failure proving
  that job-level `runner.temp` is rejected before changing the workflow.

## 2. Close the unique owners

- [x] 2.1 Add `github-workflow-syntax` to `system/gates.toml`, select it exactly
  once after `config-quality` in canonical full proof, declare its existing
  supply/write behavior truthfully, and verify gate-registry contracts pass.
- [x] 2.2 Change the canonical GitHub template and generated workflow together to
  `${{ github.workspace }}/build/runtime/python`; verify template equality and
  actionlint both pass without adding another workflow or supply owner.

## 3. Prove the bounded atom

- [x] 3.1 Run the focused CI projection regression, gate registry contracts,
  actionlint owner, `ci_templates`, and configuration-quality checks.
- [x] 3.2 Run strict OpenSpec validation, Ruff, format check, module-layout,
  source-budget, and repository-wide references for the new gate identity.
- [x] 3.3 Confirm current status, planning completion, Lease binding, signing
  readiness, and pre-commit admission have no unresolved gap for this Change.

## Lifecycle Transition Boundary

After every task above is complete, the implementation requires exact-HEAD full
proof before official archive. Archive creates a distinct signed HEAD that then
requires post-archive proof, candidate and accepted exact CAS, runtime readback
if the packaged gate resource changes, deterministic publication of that exact
accepted OID to every declared peer, fresh hosted-provider observation, and
retirement of this Work Lane and its owned resources. These are mandatory
terminal state transitions after the checklist, not self-referential pre-commit
tasks, and MUST NOT be claimed before their exact observations exist.
