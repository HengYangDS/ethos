## Context

The source-policy root contains fifteen added test definitions across package
boundaries, reference closure, and source budgets. Its committed HEAD is an
ancestor of current accepted history. The three dirty file hashes remain equal
to the existing inventory; all unique meaning still requires adjudication.

## Preservation Decisions

| Historical obligation | Current owner and disposition |
| --- | --- |
| Nine reference/budget definitions | Same AST in maintained tests; retain the current definitions without copying. |
| Sorted unknown references and native-module exclusion | Same inputs and assertions; the current function is named `product_reference_gaps`, not `reference_gaps`. Keep the current owner. |
| Explicit npm distribution allowlist | Already present in the architecture boundary test. Do not duplicate it. |
| Package attribution, workspace privacy, bin declaration, and file scope | Extend the existing policy boundary fixture to check normalized allowed entries and distinct forbidden/unknown entries by exact findings, not only a set of kinds. |
| Malformed package metadata does not invent semantic leak findings | Add the missing input boundary to the same test owner. This does not declare malformed packages valid; native format/schema checks retain validation authority. |
| Release-visible history scanning | Exercise both archived OpenSpec and documentation history, including path-derived and content-derived findings. |
| Historical contributor roles and identity placeholders | Superseded by the current three-field commit-policy compiler and signer trust boundary. Existing tests reject malformed declarations and old unknown identity fields; do not restore the removed role taxonomy. |

## Alternatives And Boundary

Wholesale merge would reintroduce obsolete policy and duplicate assertions.
Deleting the old dirty increment after temporary probes would discard durable
regression coverage. Selective absorption into the existing owners preserves
the valid scenarios with no new product entity or compatibility path.

Retirement is separate from test preservation. Current prewrite correctly lacks
authoring intent for the historical Change, while retirement and handoff export
currently require clean content. Do not solve that by restoring old carriers or
pretending the dirty root is clean. Preserve it until a public exact effect can
bind its content, ownership, retained evidence, and disposal preconditions.

## Verification

Compare all added definitions against the current tree, then run the affected
boundary/reference/budget/commit-policy tests. Demonstrate that the strengthened
boundary tests reject lost distribution normalization and historical scanning.
Keep independent product/test limits at 40000 ELOC and combined coverage at
95 percent. Full proof and official archive are lifecycle checks, not tasks
whose completion would require modifying the already-proved source snapshot.
