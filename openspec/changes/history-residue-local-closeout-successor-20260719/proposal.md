## Why

The dated predecessor archive intentionally proves tracked capability work only.
A separate local operator action subsequently archived the July 9 recovery set,
applied exact lease and proof retention, and removed the verified source copy.
The integrated Work Lane also still exceeds five campaign growth limits. Those
facts need one successor carrier that binds the real local effect, settles the
active overages without policy relaxation, and carries the resulting HEAD through
local proof, candidate land, and maintainer break-glass closeout.

## What Changes

- Preserve the predecessor archive byte-for-byte and retire its stale active
  duplicate rather than attempting to archive the same logical change twice.
- Bind the verified operator recovery and maintenance receipts as historical
  local-effect evidence, distinct from current local-state inventory.
- Reduce product, test, aggregate Python, shell, and TOML metrics to their active
  campaign limits through real deletion or consolidation.
- Complete full tests, 100% branch coverage, owner gates, parity, and a HEAD-bound
  executed proof before archiving this successor.
- After archive, land locally, produce the required external control-replacement
  receipt, run `maintainer_break_glass_local` closeout, align local refs, and
  verify publish readiness without probing or mutating any remote.

## Capabilities

### New Capabilities

- None.

### Modified Capabilities

- `repository-governance`: subject=history-residue-local-closeout-successor; reuse=extend; change=modify; facet:lifecycle=validation,runtime,archive,release; facet:surface=cli,openspec,evidence; facet:authority=source,test,openspec,evidence,claim.
- `quality`: subject=history-residue-hard-budget-settlement; reuse=extend; change=modify; facet:lifecycle=authoring,validation,release; facet:surface=source,test,config,evidence; facet:authority=source,test,openspec,evidence,claim.

## Out Of Scope

- Fetch, push, remote probe, hosted CI claims, or provider-side mutation.
- Reset, stash, clean, rebase, force update, or mutation of foreign/unbound lanes.
- Rewriting the predecessor official archive or treating it as evidence of the
  later real local-state apply.
- Relaxing baselines, terminal targets, campaign limits, expiry, or active debt.
- Retiring r7 or any lane not owned by `agent:openai:thread:root`.

## Impact

The change affects the successor OpenSpec carrier, its claim and Chronicle,
source-budget carriers, state-maintenance integration, tests, parity, and local
lifecycle evidence. Remote publication remains deferred and unclaimed.
