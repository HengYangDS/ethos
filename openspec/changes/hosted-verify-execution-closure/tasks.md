## 1. Execution-Supply Boundary

- [x] 1.1 Add focused regressions proving Hosted Linux supplies every declared
  native test prerequisite, and verify the current bootstrap fails the new
  contract for process observation.
- [x] 1.2 Add focused regressions proving the privilege-boundary environment
  removes run-as controls and projects the locked UV cache by absolute path, and
  verify both regressions fail against the current test owner.
- [x] 1.3 Extend the existing bootstrap and Python test owner only at their
  current semantic boundaries, then verify the focused execution-supply tests
  pass without a fallback, wrapper, or second cache authority.
- [x] 1.4 Remove the publication fixture's ambient OpenSpec substitution and
  verify the focused publication behavior uses the source-bound resolver.

## 2. Contract And Reference Closure

- [x] 2.1 Validate the official Change strictly and verify repository-wide
  references contain no descended run-as control leak, relative declared UV
  cache, or test-only bare OpenSpec resolver at the affected boundary.
- [x] 2.2 Run the affected architecture, publication, runtime-activation,
  version-identity, and local-install tests, including the exact 132-node hosted
  failure set under the bootstrap-supplied locked execution environment.
- [x] 2.3 Replace the misleading OpenSpec-only Node supply coordinate with one
  session-frozen lock-bound package supply, migrate every Node consumer and
  declaration without an alias or fallback, and prove the focused quality,
  build, runtime, and installation paths.

## Lifecycle Transition Boundary

After every task above is complete, the implementation requires one signed
commit and exact-HEAD full proof before official archive. Archive creates a
distinct signed HEAD that then requires reproof, candidate and accepted exact
CAS, fresh immutable package-only runtime materialization and readback, exact
publication of the same accepted object to GitLab and GitHub, independent
hosted-plane observation, and retirement of this Work Lane and its owned
temporary resources. These are mandatory terminal transitions after the
checklist, not self-referential pre-commit tasks, and MUST NOT be claimed before
their exact observations exist.
