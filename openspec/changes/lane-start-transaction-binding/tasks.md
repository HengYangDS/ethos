## 1. Contract and scope

- [ ] 1.1 Expand the active Commitment through the public rebind receipt so the repository reader and focused consumer tests are uniquely in scope; verify `ethos plan --changed --json` reports no uncovered path.
- [ ] 1.2 Add repository-reader regressions for absent, unreadable, unsupported-schema, semantic-invalid, and identity-mismatch carriers; verify each new test fails for the intended old classification.

## 2. Pre-effect lane transaction

- [ ] 2.1 Add a real-repository regression proving lane-start dry-run and apply return the same invalid-Commitment blocker before ref, worktree, Lease, or Change-carrier effects.
- [ ] 2.2 Add rollback regressions proving zero-effect failure retains the original blocker, reports no residue, and emits no cleanup or compensation failure gap.
- [ ] 2.3 Make lane planning and apply consume one exact repository-bound Commitment observation and verify the focused fresh-lane suite passes.

## 3. Shared producer-to-consumer semantics

- [ ] 3.1 Remove the broad repository Commitment exception translation and map strict parser states in the single reader owner; verify repository boundary tests pass.
- [ ] 3.2 Verify plan, proof, publication, and lane consumers preserve the producer's precise blocker without local missing-only reinterpretation.
- [ ] 3.3 Search the repository for obsolete `repository_commitment_missing` assumptions and delete or narrow every superseded branch; verify reference closure and affected contract tests pass.

## 4. Closeout

- [ ] 4.1 Run format before lint, focused tests, affected static and architecture gates, OpenSpec strict validation, and changed proof on the exact HEAD.
- [ ] 4.2 Run the full applicable test and coverage proof once, archive the Change, land through the public lifecycle, and verify accepted/candidate/runtime exact identities.
