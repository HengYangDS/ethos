## 1. Contract and scope

- [x] 1.1 Expand the active Commitment through the public rebind receipt so the repository reader and focused consumer tests are uniquely in scope; verify `ethos plan --changed --json` reports no uncovered path.
- [x] 1.2 Add repository-reader regressions for absent, unreadable, unsupported-schema, semantic-invalid, and identity-mismatch carriers; verify each new test fails for the intended old classification.

## 2. Pre-effect lane transaction

- [x] 2.1 Add a real-repository regression proving lane-start dry-run and apply return the same invalid-Commitment blocker before ref, worktree, Lease, or Change-carrier effects.
- [x] 2.2 Add rollback regressions proving zero-effect failure retains the original blocker, reports no residue, and emits no cleanup or compensation failure gap.
- [x] 2.3 Make lane planning and apply consume one exact repository-bound Commitment observation and verify the focused fresh-lane suite passes.

## 3. Shared producer-to-consumer semantics

- [x] 3.1 Remove the broad repository Commitment exception translation and map strict parser states in the single reader owner; verify repository boundary tests pass.
- [x] 3.2 Verify plan, proof, publication, and lane consumers preserve the producer's precise blocker without local missing-only reinterpretation.
- [x] 3.3 Search the repository for obsolete `repository_commitment_missing` assumptions and delete or narrow every superseded branch; verify reference closure and affected contract tests pass.

## 4. Closeout

- [x] 4.1 Run format before lint, focused tests, affected static and architecture gates, OpenSpec strict validation, and changed proof readiness.
- [x] 4.2 Run the full applicable test and coverage gates once. After this checklist is complete, execute the exact-HEAD proof, archive, land, and accepted-runtime verification as separate governed transitions.
