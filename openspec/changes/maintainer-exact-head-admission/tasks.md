## 1. Contract and RED

- [x] 1.1 Record complete deltas for accepted-head closeout, hook consumption,
  external proof binding, local-only profiles, and executable next actions.
- [x] 1.2 Add a black-box RED proving an archived, signed, exact proved candidate
  can advance accepted through public closeout without hook bypass.
- [x] 1.3 Add RED cases for exact external receipt binding and stale, forged,
  wrong-subject, wrong-role, and wrong-verifier rejection.
- [x] 1.4 Add producer-to-consumer RED proving pre-push accepts the exact object
  admitted by closeout without a circular local-ref equality prerequisite.
- [x] 1.5 Add RED proving a zero-Forge profile uses local proof only and that
  status, plan, land, and hook surfaces project one complete command.

## 2. Single authority implementation

- [x] 2.1 Introduce one typed transient closeout resolution under the existing
  land/mutation owner and bind exact commit/tree, proof plane, signature,
  optional external receipt, ref intent, and next action.
- [x] 2.2 Make dry-run and apply consume the same resolution, recheck coordinates
  at effect time, execute the existing exact Git CAS, post-observe, and attest.
- [x] 2.3 Make reference-transaction and pre-push consume the prepared decision;
  delete hook-local protected-role proof/topology/local-ref reconstruction.
- [x] 2.4 Route status, plan, closeout, and hook remediation through one command
  renderer; remove prose-only and placeholder closeout actions.
- [x] 2.5 Preserve developer proposal fail-closed behavior and local-only profile
  semantics without adding a Forge state machine or seventh root command.

## 3. Verification and closeout

- [x] 3.1 Run focused closeout, admission, external evidence, hook, status, and
  plan regressions with exact identity assertions.
- [x] 3.2 Run Ruff, ty, module/import/schema/docstring gates and strict OpenSpec.
- [ ] 3.3 Run the full test and coverage matrix at the final candidate HEAD.
- [ ] 3.4 Execute public proof, archive, candidate/accepted land, and accepted
  runtime currentness verification; record any unverified remote plane.
