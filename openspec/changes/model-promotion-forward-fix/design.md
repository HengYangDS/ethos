# Design
Exact accepted-HEAD repository proof owns publication; conflicts fail closed. Source runtimes install the offline `uv.lock` closure, then the wheel with `--no-deps`, without fallback or second SSOT.
