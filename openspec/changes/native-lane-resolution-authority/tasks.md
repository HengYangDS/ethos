## 1. Successor authority carrier

- [x] 1.1 Record the approved native authority cut, exact material scope,
  provider-neutral Claim, Chronicle, design, and executable implementation plan.
- [x] 1.2 Supersede the predecessor Claim without reopening its archived Change.
- [x] 1.3 Pass strict official OpenSpec and ETHOS lifecycle/Claim validation before
  product implementation.

## 2. Current records and contracts

- [ ] 2.1 Add failing tests for one versioned current root, immutable historical
  roots, decision-only inventory, and blocking invalid current payloads.
- [ ] 2.2 Add failing tests for receipt version 3, reservation version 2, clear
  version 1, exact provider-neutral fields, and 40/64-character Git object IDs.
- [ ] 2.3 Implement concrete record-root and reservation modules, migrate direct
  imports, and keep every package `__init__.py` declaration-only.
- [ ] 2.4 Reconnect current receipt, clear, recovery, release, and inventory paths
  to the versioned current root with no historical fallback.

## 3. Native admission and effect

- [ ] 3.1 Add failing tests for decision/Chronicle drift, configured Work Lane
  roles, exact Git registration/incarnation, clean ownerless state, accepted
  ancestry, and fence-held re-observation.
- [ ] 3.2 Implement native admission and reconnect it to the existing fence,
  durable reservation, recovery, no-force worktree removal, Git CAS, and
  receipt-first cleanup path.
- [ ] 3.3 Delete the retired external adapter and callback seam, delete its
  provider-response tests, and update all direct imports.
- [ ] 3.4 Preserve zero-effect retry, accepted-head descendant rebinding,
  crash-window recovery, partial-transition visibility, and three-state probes.

## 4. Configured role and generic coupling governance

- [ ] 4.1 Add failing tests proving native admission accepts the existing custom
  `work_branch_prefix` policy and rejects branches outside that role.
- [ ] 4.2 Add failing generic tests that detect undeclared mandatory lifecycle
  executables while preserving unrelated optional configured adapters.
- [ ] 4.3 Update the coupling declaration and audit implementation so observed
  lifecycle bindings must be declared.

## 5. Truth convergence and proof

- [ ] 5.1 Replace the canonical requirement and command reference, retire the old
  active plans, and normalize retired provider vocabulary from all current
  tracked source, schemas, tests, Claims, Chronicle, and archived carriers while
  preserving historical meaning.
- [ ] 5.2 Run focused red/green suites, full tests, lint, types, schemas,
  module-layout, code-size, no-compat, import, coupling, Claim, OpenSpec, and
  provider-neutral residue gates.
- [ ] 5.3 Execute generic shadow parity, commit all evidence, and run exact-HEAD
  `ethos prove --execute` with no required gaps.
- [ ] 5.4 Complete the Chronicle and Claim, officially archive the Change, update
  the Claim carrier, and rerun archive-HEAD proof.

## 6. Land and housekeeping boundary

- [ ] 6.1 Land the proven HEAD to candidate and perform audited accepted-root
  closeout as separate transitions.
- [ ] 6.2 Report local publish readiness without remote push.
- [ ] 6.3 Retire the landed successor lane and the two task-owned predecessor
  mistake lanes through native exact closeout or Chronicle-bound
  preserve-retire when absorption cannot be proven.
- [ ] 6.4 Remove only task-generated caches and scratch artifacts, verify all
  protected roots and task-owned lanes are clean/retired, and leave historical
  local records, foreign lanes, virtual environments, and session metadata
  untouched.
