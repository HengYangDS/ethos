## Why

Exact post-archive proof of the preceding status repair fails in native runtime
activation before its business assertion. The fixture replaces generation
validation with selector inspection only, omitting the real prefix observation
and smoke execution that production materialization performs before activation.

## What Changes

- Keep one test-owned exclusive Python image for runtime mutation cases.
- Establish its native executable, prefix and module prerequisites before the
  fixture exposes it, including the requested new-generation smoke check.
- Reject unusable fixture images during materialization and preserve cleanup.
- Replay the exact failing installation cases before full proof.

## Capabilities

No specification change: repair the test fixture's fidelity to existing runtime
generation behavior. The official `skip_specs` marker declares zero deltas.

## Impact

The existing runtime fixture, its boundary tests and canonical terminal plan.
The current Work Lane carries both sequential Changes; no new worktree is needed.

## Non-Goals

No runtime-policy change, deadline increase, extra worker, cached verdict,
schema change, shared production inode or adopter edit. A passing focused replay
does not prove that all native startup latency has been eliminated.
