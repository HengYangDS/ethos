## 1. Contract and RED

- [x] 1.1 Preserve the real owned empty-Work-Lane prewrite failure after official
  Change metadata creation and prove proposal authoring is blocked by incomplete
  Commitment compilation.
- [x] 1.2 Add focused regressions proving only official metadata, proposal, specs,
  design, and tasks under the exact selected Change root are admitted; product,
  archive, unrelated Change, and unofficial sidecar paths remain blocked.

## 2. Unique owner replacement

- [x] 2.1 Extend the existing current OpenSpec resolution to project one transient
  material-scope report from the official single-Change artifact graph.
- [x] 2.2 Make prewrite consume that scope only while Commitment compilation is
  incomplete, then automatically return to ordinary Commitment attribution.
- [x] 2.3 Project the exact official artifact instruction as the single next action
  and delete stale `lane start-change` requirements, fixtures, and vocabulary.

## 3. Closure

- [x] 3.1 Pass strict OpenSpec validation, focused current-resolution/prewrite/CLI
  tests, reference closure, Ruff, type/import/module-layout, and affected gates.
