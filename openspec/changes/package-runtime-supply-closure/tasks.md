## 1. Establish the failing boundary

- [x] 1.1 Preserve the accepted GitLab exact-HEAD evidence showing
  `npm ci --offline` failed on absent `zod-4.4.3.tgz` after the lock had already
  been installed.
- [x] 1.2 Add focused RED tests proving a valid prepared production closure
  requires no npm execution or per-build temporary dependency tree, while
  absent or drifted supply fails before artifact construction.

## 2. Converge package supply ownership

- [x] 2.1 Compile production package roots from the exact source
  `package-lock.json` and validate the prepared supply paths and versions.
- [x] 2.2 Project the same selected roots directly into wheel and sdist build
  data without invoking npm or copying the closure into another work tree.
- [x] 2.3 Replace the two Node/npm build variables with one prepared-supply
  binding and update every source-build caller.
- [x] 2.4 Delete the temporary supply lifecycle, per-build npm installation,
  and their obsolete tests and documentation.

## 3. Verify semantic closure

- [x] 3.1 Prove focused hook, delivery, version-identity, runtime-input, wheel,
  sdist, and isolated package-install behavior.
- [x] 3.2 Prove CI/provider projections still prepare the lock once before
  package work and run package construction offline.
- [x] 3.3 Run repository-wide reference closure for retired build variables,
  `npm ci` in the build hook, and `ethos-openspec-supply-*`; run Ruff, format,
  module-layout, and diff checks.
- [x] 3.4 Update the existing terminal plan with the closed supply boundary and
  retain hosted process-tool and Windows conformance failures as independent
  successors.
