## 1. RED

- [x] Reject reasonless public `unknown` and `block`.
- [x] Reproduce healthy `lane status` as reasonless `unknown`.

## 2. Replacement

- [x] Close reasons in `EthosResult`; preserve internal decision `why`.
- [x] Delete the facts-only verdict input and stale fixtures.

## 3. Closure

- [x] Pass focused and CLI caller-closure tests plus strict OpenSpec validation.
- [x] Freeze for exact-HEAD full proof and archive.
