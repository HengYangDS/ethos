## 1. Carrier And RED Baseline

- [x] 1.1 Create and bind the active claim, dated Chronicle, and exact material
  scope.
- [x] 1.2 Add failing release-policy regressions for missing, non-executable,
  and repository-escaping local command owners.
- [x] 1.3 Add failing architecture contracts for the install owner, full-proof
  registration, local-CI composition, and HEAD-bound negative-claim evidence.

## 2. Local Installation Owner

- [x] 2.1 Implement fail-closed local command validation in release policy.
- [x] 2.2 Implement the fresh offline wheel-install owner with isolated runtime
  state, dual-package origin checks, CLI help/version checks, wheel digests, and
  stable-HEAD evidence.
- [x] 2.3 Register the active tool and trust-bearing full-proof gate and compose
  the owner into local CI.
- [x] 2.4 Update release governance without introducing another design or
  configuration truth center.

## 3. Evidence And Governed Local Closeout

- [x] 3.1 Run focused tests, real install smoke, shell/config/release checks, and
  the quality audit.
- [x] 3.2 Refresh the claim, Chronicle, and generic parity evidence on a stable
  committed HEAD.
- [ ] 3.3 Execute default and full HEAD-bound proof, then archive this Change
  through the official OpenSpec CLI.
- [ ] 3.4 Re-run post-archive parity and both proof graphs on the final lane HEAD.

Candidate landing, accepted-root closeout, local publication readiness, remote
publication, hosted CI, and Work Lane retirement are separate post-archive
transitions and are not pre-asserted by this checklist.
