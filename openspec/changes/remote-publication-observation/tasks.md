## 1. Establish the exact failing boundary

- [x] 1.1 Preserve the current dual-peer failure receipt and prove GitHub
  observation can be unavailable while the live ref is an ancestor of the
  selected source.
- [x] 1.2 Add focused RED tests proving timeout/unavailable observations remain
  unknown and never emit a non-fast-forward judgment.

## 2. Converge remote observation ownership

- [x] 2.1 Bound the existing exact-ref observer and retain command, cwd,
  timeout/exit state, and stderr without adding a new carrier.
- [x] 2.2 Prevent admission, ancestry, and update compilation when an exact
  target fact is unavailable.
- [x] 2.3 Derive the public verdict directly from the exact effect observation,
  without consulting remote-tracking state or general reachability.

## 3. Verify semantic closure

- [x] 3.1 Prove present, absent, unavailable, fast-forward, divergent, apply
  preflight, and post-observation cases through focused tests.
- [x] 3.2 Run repository-wide reference closure for false
  unavailable-to-divergent coercion and unbounded exact-ref probes; run Ruff and
  diff checks.
- [x] 3.3 Update the existing terminal plan with the closed gap and remaining
  publication-order successor boundary; create no second roadmap or ledger.

## 4. Establish archive readiness

- [x] 4.1 Validate official OpenSpec strictly and pass the complete focused
  publication, Git-adapter, result-algebra, module-layout, Ruff, format, and
  reference-closure checks on the final source tree.
- [x] 4.2 Keep exact-HEAD proof, official archive, candidate/accepted CAS,
  runtime activation, remote publication, and Work Lane retirement in their
  existing Attestation and lifecycle owners rather than creating self-dependent
  tracked progress state.
