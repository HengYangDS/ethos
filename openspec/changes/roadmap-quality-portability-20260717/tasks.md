## 1. Contract and regression

- [x] 1.1 Add a standalone runner regression that removes the bare `python`
  alias while preserving an explicit `python3` runtime.
- [x] 1.2 Consolidate the accepted scope-binding requirement into one
  authoritative OpenSpec requirement and retain its fail-closed scenarios.

## 2. Implementation and verification

- [x] 2.1 Replace bare inline interpreter calls with the bounded runtime
  interpreter chain.
- [x] 2.2 Run focused portability, scope, architecture, and lint checks.
- [x] 2.3 Run the isolated sharded full test owner script without weakening its
  coverage threshold.

## 3. Governance closeout

- [x] 3.1 Bind the active Claim and dated Chronicle to the completed carrier.
- [ ] 3.2 Run strict official OpenSpec validation, ETHOS lifecycle, parity,
  and final committed-HEAD proof before archive or land.
