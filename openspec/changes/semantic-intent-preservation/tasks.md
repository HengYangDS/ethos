## 1. Restore semantic authority

- [x] 1.1 Reconcile every recovered terminal domain into the Product Design
  Contract and verify that current meaning has one owner without a parallel
  feedback ledger.
- [x] 1.2 Reconcile every unfinished obligation into the dependency-ordered
  Terminal Governance Product Design and verify that no archived Change or
  temporary file acts as the current queue.
- [x] 1.3 Define documentation and agent intake rules for direct guidance,
  delegated evidence, supersession, uncertainty, and Decision Record admission;
  verify the rules link to the two canonical owners instead of copying them.

## 2. Prove coverage and retire recovery material

- [x] 2.1 Validate this Change strictly and run the focused documentation,
  repository-governance, reference, and semantic-closure gates.
- [x] 2.2 Audit the declared recovery boundary and verify every distinct
  obligation is accepted into one owner, explicitly superseded, pending with a
  named evidence gap, or rejected with reason.
- [x] 2.3 Verify the tracked owners are self-contained and record the exact
  eight-file deletion boundary for the temporary recovery inputs.
- [x] 2.4 Delete those inputs, verify their absence and zero tracked dependency,
  then run the final exact-HEAD proof required before official archive.
