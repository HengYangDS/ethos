## Boundary

The official exporter remains the exact-tree binding verifier. The semantic
graph remains a projection, not permission to change product meaning. This
Change repairs current derived bindings without reopening the archived Change
or changing its bytes. Its no-spec archive must leave canonical specs unchanged.

## Source Disposition

| Archived requirement | Existing semantic owner | Disposition |
| --- | --- | --- |
| Accepted signature repair command | Effect admission, fresh effect-time recheck, recovery anchor and scoped Attestation | Readiness has no effect; apply needs current authorization; unknown outcomes are observed before recovery; proof and publication remain separate. Retain existing graph meaning. |
| Signature-only payload preservation | Exact Git substrate and effect-time expected-state bindings | Payload equality and trusted identity remain independent conditions. CR and ordered-parent details belong in the native Git owner, not new diagram entities. |
| Bounded accepted signature replacement | Selected-ref effect admission, linked worktrees, recovery anchor and non-authorizing Attestation | Ref, policy, trust, index and worktree drift block the affected effect. Independent refs remain separate; completed effects are observed rather than replayed. |

Only `repository_governance` and `command_plane` source bytes changed during
this archive. Preserve every node, relation, view, copy and invariant. Refresh
these two digests only after checking all declared sources; this is a reviewed
binding repair, not proof of full product implementation or renderer acceptance.

## Verification

The committed exporter first reproduces the stale-source failure. Existing
actual-source and exporter regressions then validate candidate bytes, unknown
or stale bindings, static assertions, full target scope and exact export schema.
After commit, run the actual exporter twice and compare output bytes, then the
existing architecture gate. Official strict validation and exact repository
proof remain independent checks.

## Remaining Lifecycle Defects

Archive does not yet compile downstream source-projection closure into its
admitted effect scope. The archive preflight also predicted a host-local date,
while the official child used UTC. This binding repair does not claim either
mechanism fixed. Preserve both counterexamples in the existing terminal plan
for a bounded owner-level repair, without delaying the current signature-lane
absorption behind a broader lifecycle redesign.
