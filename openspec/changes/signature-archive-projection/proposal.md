## Why

Official signature-repair archive changed two canonical specification inputs.
The existing terminal graph still binds their prior bytes, so exact export
fails before accepted closeout. Archived scope correctly prevents an unrelated
projection write; a bounded official tooling Change provides its own scope.

## What Changes

- Compare the three archived requirements with existing graph responsibilities.
- Refresh only the two reviewed source bindings, retaining graph meaning,
  visible assertions and the distinction between target and implemented state.
- Record actual proof and archive results in the existing terminal plan; keep
  receipts in the existing ignored evidence home.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

None. This projection maintenance introduces no new product requirement.
Official `skip_specs: true` is appropriate; no placeholder delta is needed.

## Impact

The existing terminal semantic graph and canonical terminal plan. Repository
and quality governance remain the routing owners. No new lane, renderer change,
product implementation, dependency, gate relaxation or adopter mutation.
