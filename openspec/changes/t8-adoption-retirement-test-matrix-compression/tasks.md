## 1. Baseline And Partition Design

- [x] 1.1 Record the clean focused-test and two-file `scc` code/ULOC baseline.
- [x] 1.2 Classify equivalent profile, backend-control, rollback, parity, and
  shadow partitions while preserving direct effect-boundary tests.

## 2. Destructive Test Compression

- [x] 2.1 Introduce only the minimum inert fixture builders and literal case
  tables needed by the classified partitions.
- [x] 2.2 Delete superseded setup, invocation, and assertion bodies without
  changing product code or public retirement contracts.
- [x] 2.3 Ruff-format the scoped files and remove any obsolete import or helper.

## 3. Verification And Measurement

- [x] 3.1 Run the complete adoption-retirement focused suite with warnings as
  failures and preserve every distinct public state/gap/effect boundary.
- [x] 3.2 Prove the two-file scope is at most 700 `scc` code lines and 250
  repeated lines, with no unique-line growth.
- [x] 3.3 Run changed-scope lint/type/coverage, source-budget, Ponytail review,
  parity, and HEAD-bound executed proof.

## 4. Evidence And Lifecycle

- [x] 4.1 Bind the exact scoped delta, measurements, tests, and proof to the
  claim and Chronicle without claiming remote or hosted success.
- [x] 4.2 Complete the implementation carrier; archive, candidate land,
  accepted-root closeout, and lane retirement remain separate native steps.
