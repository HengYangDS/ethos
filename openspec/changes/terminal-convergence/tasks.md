## 0. Truth And Campaign Binding

- [x] 0.1 Bind the Work Lane, exact accepted/candidate heads, foreign lane dispositions, canonical design, concern matrix, and this OpenSpec change.
- [x] 0.2 Add failing tests that hard source-budget gaps force `block`, `ok=false`, and non-ready lifecycle states.
- [x] 0.3 Remove score-derived readiness, fold orient facts into status, delete report, and enforce default payload caps.

## 1. Deletion-First Surface Collapse

- [x] 1.1 Replace source-budget worker/protocol/parser/replay/shadow paths with one direct deterministic measurement owner and delete superseded tests and schemas.
- [ ] 1.2 Delete empty Jinja/template machinery, copied CI/C4/config parsers, command/schema dual truth, compatibility normalizers, wrappers, aliases, re-exports, and coverage-only tests.
- [x] 1.3 Remove every active dependency not admitted by the product boundary,
  including its literals, imports, schemas, tests, documents, and evidence,
  while preserving native fail-closed lane authority.
- [x] 1.4 Measure and record product/test/tool/document/config deletion by canonical ELOC
  ownership class. Against `5883f078b1a539b1cbc84f9bac367f3a91a380a8`, `scc --by-file`
  records product `-908`, tests `+75`, tools `-54`, documents `-824`, config `-475`,
  and total `-2186` code lines; direct terminal measurement is Python `48401/54000`
  and global `64837/68000` (`49406` and `65881` after the independent scc cross-check).
- [x] 1.5 Block repository-local shadow plan/task/progress/report authority from
  optional method packs and runtimes, including ignored paths, and remove the
  observed `.superpowers` residue after preserving any unique OpenSpec intent.
- [ ] 1.6 Apply absorptive retirement to documentation, rules, skills, specs,
  schemas, and other knowledge carriers: inventory independent meaning, integrate
  it into one authoritative organization, escalate model gaps, verify preserved
  scenarios and links, then retire only the misleading or fully redundant residue.
- [x] 1.6.1 Remove the runtime closeout-residue read model and its schema/CLI
  projections, route status actions from one stage-gate owner, supersede obsolete
  active plans without rewriting historical archives, and keep unbound/unknown
  Work Lane state observe-only.

## 2. Semantic Kernel Cutover

- [x] 2.1 Add failing contract, determinism, replay, authority, and hash tests for ChangeContract, Attestation, RepositoryFacts, and PlanIR.
- [ ] 2.2 Make ChangeContract and Attestation the only persistent semantic entities; absorb Claim, ProofRun, EvidenceSet, RuleAttestation, review, exception, decision, and receipt meanings into Attestation and delete their models, schemas, gates, tests, and current readers.
- [x] 2.3 Bake off the existing CEL engine against official cel-expr-python, select one, and delete the loser and all dual-run code.
- [ ] 2.4 Keep one `src/ethos` distribution and delete every superseded package, model, schema, graph, adapter, test, import, shim, and re-export in the same cutover.
- [ ] 2.5 Keep RepositoryFacts freshly observed and PlanIR transient; remove stored rule-fact/evaluation, Claim, Chronicle, Campaign/Evolution, parity, and proof-record planes from current verdicts while preserving historical bytes unchanged.

## 3. Lifecycle, Collaboration, And Integration Train

- [ ] 3.1.1 Limit the strict lifecycle declaration to transition policy, disposable lease operations, and PlanIR actions; delete Campaign/Evolution CEL, workflow-run, runtime read-model, and every parallel transition owner.
- [ ] 3.1.2 Complete one idempotent Git effect adapter with exact CAS and generic Attestation replay over the pure transition reducer; delete adapter-specific Decision/Receipt/Chronicle stores.
- [ ] 3.2 Implement Worktree Family, cooperative slots, at most two competitive variants, canonical-head selection, intent amendments, absorption, and retirement.
- [x] 3.3.1 Use one isolated, non-locking Git profile for lifecycle observation
  without changing effect or handoff mutation environments.
- [ ] 3.3.2 Implement vendor-neutral actor references, shared-inbox projection,
  transcript-free handoff/takeover, orphan recovery, and lost-session unknown
  boundaries.
- [ ] 3.3.3 Implement exact authorized Lease takeover with source-state truth,
  dirty-content binding, epoch CAS, and generic Attestation evidence; use it to assume and
  close the currently authorized campaign lanes without holder impersonation.
- [x] 3.3.4 Cut over to one strict terminal LaneLease wire with immutable
  candidate-HEAD `base_change_contract_digest`, four-state observation,
  full-row exact-CAS reissues, base-bound prewrite/PlanIR/proof/closeout, and
  cross-host digest preservation; delete the current Claim command, fields,
  readers, fallbacks, and aliases.
- [x] 3.3.5 Keep candidate/dev, dev, and main free of active Change carriers;
  make lane start require one explicit `--source-root`, materialize exactly one
  source Work Lane carrier into the destination initialization commit, and bind
  the Lease to that final HEAD and digest without aliases or repair fallbacks.
- [ ] 3.4 Implement adaptive backpressure, parallel proof scheduling, candidate queue policy, and a short serialized candidate CAS update.
- [x] 3.5 Replace submit roles with proposal roles across config, source, schemas, tests, docs, hooks, CI, and release policy without compatibility aliases.
- [ ] 3.6 Prove lifecycle and race invariants with stateful property tests and a
  bounded formal transition model; admit an external model checker only when a
  concrete invariant cannot be expressed or falsified by the native model.
- [ ] 3.7 Define the minimal replaceable execution-substrate contract and
  conformance boundary. Keep every backend optional and admit one only when a
  real consumer proves pause/resume/handoff, token or time gain, authority
  isolation, uninstall cleanliness, and zero repository shadow state.

## 4. Quality, Formatting, And Supply Chain

- [ ] 4.1 Make `system/tools.toml` declare exactly one admitted owner for every
  quality property, route all local and hosted gates through those owners,
  record durable admit/defer/reject rationale only as judgment Attestations, and
  delete duplicate wrappers or unconsumed tools.
- [ ] 4.2 Make warnings errors everywhere and remove all production fmt/noqa/type-ignore suppressions.
- [ ] 4.3 Reach 95% global branch coverage and 100% authority/CAS/reducer coverage while deleting branch-reachability tests.
- [ ] 4.4 Apply bounded mutation testing to critical pure reducers where
  fault-seeding demonstrates signal; keep duplicate semantic ownership a hard
  architecture invariant rather than adding an advisory detector by default.
- [ ] 4.5 Provide singular admitted owners for reproducible CI binaries, SPDX
  SBOM, DSSE/SLSA provenance, versioning, and bounded local provider replay that
  emits only local-plane proof Attestations.
- [ ] 4.6 Enforce Python ELOC <=54,000, global owned-source ELOC <=68,000,
  payload caps, no hard-gap green verdict, and direct exact-merge-base
  per-coordinate admission without the retired source-admission stack.
- [ ] 4.7 Enforce semantic/physical isomorphism: eliminate ambiguous modules,
  make command declarations singular, and block concept, authority, public-symbol,
  import-root, and size drift without compatibility facades or naming exemptions.

## 5. Product Homomorphism

- [ ] 5.1 Prove a Python package adopter through inspect/adopt/status/plan/prove/land/publish/handoff/recovery/offline install.
- [ ] 5.2 Prove a Node/polyglot adopter through the same lifecycle without Python layout or OpenSpec requirements.
- [ ] 5.3 Prove a docs/infra adopter through the same lifecycle with native formatting, gates, and release carriers.
- [ ] 5.4 Prove uninstall cleanliness, profile isolation, provider neutrality, and the ETHOS self/adopter homomorphism.

## 6. Portable Product Boundary

- [ ] 6.1 Publish language-neutral schemas for ChangeContract, PlanIR, Attestation, adapters, permissions, and pack manifests.
- [ ] 6.2 Ship a conformance kit for determinism, authority isolation, offline behavior, permissions, protocol compatibility, and uninstall cleanliness.
- [ ] 6.3 Prove Python SDK, subprocess, and optional agent-protocol adapters consume
  the same contracts and permissions without owning repository truth. Do not
  ship catalogs, runtimes, packs, or scaffolds until a concrete adopter proves
  the need in a separately admitted change.

## 7. One Terminal Closeout

- [ ] 7.1 Complete local format, lint, type, unit, property, model, mutation, integration, adopter, package, source-budget, security, SBOM, provenance, and release proof at one immutable HEAD.
- [ ] 7.2 Advance local `candidate/dev` and local protected `dev` through audited
  closeout at that HEAD, then absorb or retire every campaign Worktree Family
  through native lifecycle commands and verify immutable records, manifests,
  hashes, and indexes.
- [ ] 7.3 Produce one `proposal/terminal-convergence` from the locally closed
  terminal HEAD and verify independent GitLab and GitHub CI/CD attestations.
- [ ] 7.4 Publish protected `dev`, release protected default `main`, and verify
  identical signed tag and artifact digests on both providers.
- [ ] 7.5 Archive this change as `YYYY-MM-DD-terminal-convergence` only after every terminal acceptance criterion is proven.
