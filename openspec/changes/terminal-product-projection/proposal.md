## Why

The exact accepted architecture export rejects seven stale source bindings.
Its graph and copy also narrow the product to transaction execution, and the
exporter drops semantic attributes and invariant contracts. Synthetic tests
miss the real source failure. Both independent renderers need one faithful input.

## What Changes

- Reconcile the existing graph and visible copy with the complete product
  contract, preserving the small kernel and replaceable capability boundary.
- Preserve typed semantic attributes, target maturity and invariant contracts in
  the exact-tree export; reject omitted or hidden-only required witnesses.
- Replace renderer-specific coordinates and obsolete text-count limits with
  readable static semantic obligations and actual transformed-geometry checks.
- Exercise the actual source selection as well as negative export fixtures.

## Capabilities

### Modified Capabilities

- `quality`: exact terminal projection export preserves meaning and requires
  explicit, statically readable witnesses for required assertions.

## Impact

The five existing terminal projection documents, exact-tree exporter, export
schema, projection tests and existing terminal plan. This intentionally evolves
that export contract; the two renderer owners consume the same immutable input.

## Out of Scope

Neither renderer repository, product kernel behavior, new product entities,
additional roadmaps, full visual acceptance or unrelated semantic analysis.
Candidate exportability is not accepted integration or a rendering quality claim.
