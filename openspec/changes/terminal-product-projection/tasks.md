## 1. Source and export closure

- [x] 1.1 Add failing regressions for actual source export, semantic preservation
  and omitted or hidden-only required witnesses.
- [x] 1.2 Reconcile graph, copy, view and quality constraints with complete
  terminal product meaning and explicit target maturity.
- [x] 1.3 Preserve semantic attributes and contracts in the versioned exporter
  and schema; retain strict exact-source checks.

## 2. Verification and delivery

- [x] 2.1 Pass focused projection tests, native specification validation and
  affected static checks.
- [x] 2.2 Export an immutable candidate twice; verify schema, exact bindings and
  deterministic digest, and expose the candidate coordinates for renderer import.
