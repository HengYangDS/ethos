# three-layer-dual-remote-release-topology-20260715

Declare remote-independent local verification plus GitLab and GitHub
peer-complete repository and CI/CD planes, while retaining GitLab primary
publication authority and separate provider evidence boundaries.

`candidate/dev` remains local-only; GitLab and GitHub accept only `dev`,
`main`, and `submit/*`.
