## Boundary

The selected historical source is `work/20260811-repository-transition-model`,
HEAD `84ba160bea96bb59391d1563c59771a5d27ed4ac`, including its retained dirty and
untracked content. The existing content receipt binds 388 distinct paths and
source hashes. This Change does not revive that source's runtime or intent model.

Current product meaning comes from the Product Design Contract. The existing
terminal plan remains the only cross-Change route. Review receipts under the
existing ignored quality evidence home support comparison, not progress authority.

## Semantic Review

Read each responsibility's actual source delta, including tests and native
carriers. Assign it to an existing owner or record a concrete missing capability.
Use hashes and AST indexes only to locate correspondence, never to infer complete
behavioral equivalence. A source deletion requires review of the deleted meaning.

The first review includes all 26 historical tasks, the complete proposal/design
and 35 delta requirements with 48 scenarios. Persistent Commitment scope,
controller-expanded Lease, a second lifecycle graph and command-private recovery
stores are superseded. Fresh operation admission, preservation, recoverability,
complete product paths and non-authorizing results remain required.

The source's full-DAG identity audit retains a distinct obligation: explicit
bijection, exact trees and ordered parents, timestamp and unknown-header
preservation, stable boundaries, declared allowed changes and independent trust.
Current signature-only tip repair cannot satisfy it. Its old effect composition
also depends on retired Lease/Commitment fields and needs redesign, not copying.

Dynamic private imports expose another live gap: the current layout report finds
static cross-owner imports but misses literal and aliased dynamic imports. The
old recognizer identifies spellings, not bindings, and would misclassify local
shadowing. The existing alias/scope/program-point observation work must supply
one resolution owner to both consumers, not a second string-based recognizer.

## Verification And Retirement

Each reviewed family retains its source hashes, current owner, supersession
reason, executable counterpart or precise unresolved claim. Current tests prove
only their asserted behavior. Failed or unknown probes remain evidence and are
not relabeled passing absorption.

Retirement may proceed only after all selected content has a justified fate,
necessary unique bytes are recoverable, actual writers are quiescent, and the
public exact-content preflight and effect succeed. A classified requirement is
not an implemented capability. Unresolved unique material blocks disposal until
its current owner or explicitly retained recovery consumer can carry it.

## Work Boundaries

Review and existing-counterpart verification stay here. Do not turn every
historical feature into implementation inside this Change. Required product
repairs remain in the existing terminal plan with exact acceptance boundaries;
they cannot disappear merely because the obsolete checkout later retires.
No archive, accepted delivery or physical retirement is a task checkbox claiming
that a future lifecycle effect already occurred.
