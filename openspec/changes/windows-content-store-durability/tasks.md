## 1. Contract and RED

- [x] 1.1 Add the distribution requirement and a focused regression that fails
  when Windows attempts to open the package-store directory.

## 2. Unique owner repair

- [x] 2.1 Restrict the parent-directory durability barrier to supported POSIX
  hosts while preserving file flush, atomic link, and collision verification.

## 3. Closure

- [x] 3.1 Pass strict OpenSpec validation, focused store/runtime tests, Ruff, and
  repository diff checks before freezing the implementation commit.
