## 1. Contract and RED

- [x] 1.1 Add a regression proving valid LF and CRLF SSH signature statuses
  produce identical trusted observations while malformed status remains blocked.
- [x] 1.2 Add a regression proving package smoke includes exact publication
  required gaps when the expected transition plan is unavailable.

## 2. Unique owner repair

- [x] 2.1 Repair the existing Git-object trust parser at its line-boundary owner
  and verify the focused trust tests pass.
- [x] 2.2 Preserve existing publication gaps in the package-smoke failure and
  verify its focused architecture tests pass.

## 3. Closure

- [x] 3.1 Pass strict OpenSpec validation, focused tests, Ruff, types, and
  repository hygiene.
- [x] 3.2 Freeze a signed source commit and pass exact-HEAD full proof.
- [x] 3.3 Confirm public archive readiness with no unresolved implementation or
  verification task remaining in the active Change.
