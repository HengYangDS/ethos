## 1. Establish the failing boundary

- [x] 1.1 Capture GitHub exact-HEAD Windows Python 3.12, 3.13, and 3.14 evidence showing the common `hook_runtime_python_not_relocatable` failure.
- [x] 1.2 Add and run a focused RED proving that equivalent Windows prefix spellings are one runtime path.

## 2. Restore standalone interpreter relocation

- [x] 2.1 Replace raw prefix-string equality with one platform-native path identity predicate while keeping different roots fail-closed.
- [x] 2.2 Prove the copied interpreter reports a path-identical generated image as both Python prefixes without wrappers, environment overrides, or fallback paths.
- [x] 2.3 Run focused runtime selection, image, generation, hook, and isolated-wheel tests and close repository-wide references.

## 3. Prove pre-archive closure

- [x] 3.1 Complete exact-HEAD full proof before official archive.
