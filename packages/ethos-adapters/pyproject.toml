[project]
name = "ethos-adapters"
version = "0.1.0a1"
description = "ETHOS provider adapters"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
