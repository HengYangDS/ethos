[project]
name = "ethos-adapters"
version = "0.1.0a1"
description = "ETHOS provider adapters"
requires-python = ">=3.12"
dependencies = [
  "ethos-contracts",
  "ethos-core",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
