[project]
name = "ethos-adopt"
version = "0.1.0a1"
description = "ETHOS project adoption and profile planning"
requires-python = ">=3.12"
dependencies = ["ethos-kernel"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
