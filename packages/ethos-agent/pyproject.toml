[project]
name = "ethos-agent"
version = "0.1.0a1"
description = "ETHOS agent context and projection contracts"
requires-python = ">=3.12"
dependencies = ["ethos-kernel"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
