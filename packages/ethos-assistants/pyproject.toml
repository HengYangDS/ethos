[project]
name = "ethos-assistants"
version = "0.1.0a1"
description = "ETHOS assistant and context boundaries"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
