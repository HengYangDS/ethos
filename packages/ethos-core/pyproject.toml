[project]
name = "ethos-core"
version = "0.1.0a1"
description = "Pure ETHOS core kernel primitives"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
