[project]
name = "ethos-governance"
version = "0.1.0a1"
description = "ETHOS governance, command registry, and self-audit"
requires-python = ">=3.12"
dependencies = ["ethos-contracts", "ethos-kernel", "jsonschema>=4.23"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
