[project]
name = "ethos-governance"
version = "0.1.0a1"
description = "ETHOS governance, command registry, and self-audit"
requires-python = ">=3.12"
dependencies = ["ethos-kernel"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
