# ethos-kernel

Subject: pure ETHOS kernel semantics.

This package owns Subject, Commitment, Change, Evidence, Chronicle, Evolution,
stable result payloads, and deterministic action graph algebra. It has no
workspace, profile, subprocess, SQLite, or assistant imports.
