[project]
name = "ethos-kernel"
version = "0.1.0a1"
description = "Pure ETHOS kernel and action graph algebra"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
