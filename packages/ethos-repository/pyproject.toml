[project]
name = "ethos-repository"
version = "0.1.0a1"
description = "ETHOS repository lifecycle semantics"
requires-python = ">=3.12"
dependencies = [
  "ethos-contracts",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
