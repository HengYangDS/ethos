[project]
name = "ethos-test"
version = "0.1.0a1"
description = "ETHOS conformance and parity proof host"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
