# ethos-workspace

Subject: deterministic workspace operation.

This package owns Git/workspace inspection, local SQLite state, action run
state, land readiness, and publish readiness. Local state remains ignored and
does not replace durable Evidence.
