# ethos

Subject: public command plane and UX shell.

This package owns the `ethos` CLI. It composes kernel, governance, workspace,
agent, and adoption packages without becoming a second truth center.
