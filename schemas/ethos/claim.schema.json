{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ethos.local/schemas/claim.schema.json",
  "title": "ETHOS Claim",
  "type": "object",
  "required": ["claim", "evidence"],
  "properties": {
    "claim": {
      "type": "object",
      "required": ["id", "subject", "state", "summary"],
      "properties": {
        "id": {"type": "string", "minLength": 1},
        "subject": {"type": "string", "minLength": 1},
        "state": {"type": "string", "minLength": 1},
        "summary": {"type": "string", "minLength": 1}
      },
      "additionalProperties": false
    },
    "evidence": {
      "type": "object",
      "required": ["dated", "sha256", "tests", "evidence_ids", "binding", "verifier"],
      "properties": {
        "dated": {"type": "string", "minLength": 1},
        "sha256": {"type": "string", "minLength": 64, "maxLength": 64},
        "tests": {"type": "array", "items": {"type": "string"}},
        "evidence_ids": {
          "type": "array",
          "minItems": 1,
          "items": {"type": "string", "minLength": 1}
        },
        "binding": {"type": "string", "minLength": 1},
        "verifier": {"type": "string", "enum": ["digest_only", "semantic"]}
      },
      "additionalProperties": false
    },
    "scope": {"type": "object"}
  },
  "additionalProperties": false
}
