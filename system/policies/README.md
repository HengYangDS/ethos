# system/policies/

Optional CEL, Rego, or other policy sources — the Instrumentation layer's
declarative policy inputs (used when plain TOML predicates are insufficient).

Authority: docs/plans/terminal-governance-product-design.md (§`system/`).
Populated as declarative gates and guards migrate here (Phase E).
