# system/projections/

Templates for agent and host projections. Combined with `skills/` and
`system/surfaces.toml` to generate host projections that carry a source digest
and fail drift checks when stale.

Authority: docs/plans/terminal-governance-product-design.md (§`system/`, §`skills/`).
Populated when projection generation lands (Phase E3 / Stage 6).
